node_modules/
node_modules
dist/
*.log
memory.md
.DS_Store
.env
.env.*
.certs/

# capacitor: regenerated by cap sync
apps/web/ios/App/App/public/

# desktop: bundled daemon sidecar (built by apps/desktop/scripts/bundle-daemon.mjs)
apps/desktop/dist-daemon/

# pipeline: per-agent sandbox permissions — rewritten at every agent spawn
# (writeSandboxConfig / writeAuxSandboxConfig); never a repo file (P3-437 r2)
opencode.json
