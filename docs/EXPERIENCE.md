# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When an aggregate CI check fails, do name the red sibling checks already carried in the same statusCheckRollup (name=CONCLUSION format, excluding the aggregate itself) instead of emitting a bare "aggregate failed" message — the evidence is… (fonte: P3-405)
- When unit-testing secret-redaction code, do build token-like fixtures at runtime by concatenation (e.g. prefix + "x".repeat(40)) so no literal secret in the test source trips the no-secrets invariant scan (fonte: P3-405)
- When an Electron menu item displays an accelerator that is already registered system-wide via globalShortcut, mark it registerAccelerator:false (display-only) or the OS shortcut and the local menu accelerator both fire the same action (fonte: P3-406)
- When a React parent must trigger a one-shot focus/action in a child that remounts (tabs, session switches), use absolute bump counters that the child consumes via a callback the parent uses to reset the tick to 0 — booleans or unconsumed t… (fonte: P3-406)
- When a secret-scrubbing rule already exists for one output (sidecar log), do extract it into one shared pure function reused by every other surface — copied regexes drift and the un-updated copy becomes the leak — and pass machine-derived… (fonte: P3-407)
- When saving a user-facing file from Electron, do write atomically (temp file in the target folder + rename, 0600 mode) and return a status-only result ({ok, canceled}) — never the path — treating a dismissed save dialog as its own terminal… (fonte: P3-407)
- When a decision can hold a shared machine resource (sleep blocker, lock, shutdown veto), do fail-safe to RELEASE on any invalid input and order the hermetic-test-session check FIRST, pinned with a source-reading assertion over the real mai… (fonte: P3-409)
- When a renderer-inferred signal drives OS-level behavior in main, do derive it in a pure module, ship only a validated primitive over a dedicated one-way channel (renderer→preload→main), and re-validate bounds in main before consulting the… (fonte: P3-409)
- When persisting a default-on user preference, do write atomically (sibling tmp 0600 + rename, following quitstore) and degrade every read failure to the documented default unless an explicit false is stored, keeping ENOENT silent and the f… (fonte: P3-409)
- When a vertically centered flex column can outgrow its viewport (full-screen gates, wizards, hero cards), do not rely on plain justify-content: center — centered overflow clips content above the scroll origin so the wordmark renders sheare… (fonte: P3-423)
- When pinning a header with position: sticky over scrolling cards, give it an opaque page-color background, an explicit z-index, and move the screen's top inset/safe-area padding onto the header itself so the scrollport starts flush at the… (fonte: P3-423)
- When a scroll container has a sticky header, set scroll-padding-top (header height plus safe-area inset) on the scrollport — programmatic focus() scrolls, like an empty-submit refocusing the paste/code box, otherwise land the focused eleme… (fonte: P3-423)
- When a card on a shell screen would enumerate the same items the adjacent rail already lists, drive the variant off the existing state prop that expresses the rail's presence (e.g. `panesReachable`) and render a one-line note instead of th… (fonte: P3-422)
- When condensing one surface's variant, keep the full rendering as the other branch of the same component instead of deleting it — rail-less surfaces like the manual pairing ceremony still need the complete map, so one prop (`reachable`) mu… (fonte: P3-422)
- When handling untrusted wire envelopes (relay/local WS frames), do validate all clear-text metadata (`seq`/`from`/`payload`) with one pure verdict function at the single choke point both transports share, before any crypto or session state… (fonte: RT-424)
- When wiring an async handler into `ws.on("message")`, never leave a floating rejection via `void fn()` — route it through a single `.catch()` backstop (Node 22 kills the process on one unhandled rejection) and log only `error.name`, never… (fonte: RT-424)
- When accepting attacker-controlled numeric fields, do fail-closed with `Number.isSafeInteger(n) && n >= 0`, normalizing only `null`/`undefined` to the historical default — JSON `1e999` parses to `Infinity` and seq ≥ 2^53 wraps modulo 2^64… (fonte: RT-424)
- When replacing a text-only progress indicator (e.g. "Step 1 of 3") with purely visual glyphs like dots or bars, keep the original copy as an aria-label on a role="group" wrapper and mark each glyph aria-hidden — visual simplification must… (fonte: P3-421)
- When a polish task removes or restyles visible copy, keep a copy-independent state hook (data-welcome-step on the root element) for e2e harnesses to query, since flows like test:desktop-flow select by attribute and would break if pinned to… (fonte: P3-421)
- When asserting token-scale visual states a DOM test can't see (accent pill vs strong vs resting border per dot state), pin them with source-level assertions reading the component and CSS files, including which var() token each state class… (fonte: P3-421)
- When a card's heading and body both describe the same state, branch the heading on that state too — reuse the state hook the markup already exposes (the `data-mission` attribute) instead of adding new state, so the label can never contradi… (fonte: P3-418)
- When introducing a new CSS class variant beside enumerated siblings (e.g. `.mission-active-note` next to `.mission-active-src`), update every responsive selector list that names those siblings (`.app-root.mobile ...`) and reserve the mono/… (fonte: P3-418)
- When pinning a label-by-state fix a DOM test can't see, assert the exact source ternary plus a negative CSS check (the new rule must NOT contain `--font-mono`), and add the new i18n key to the both-locales test loop — missing either fails… (fonte: P3-418)
- When dispatching an async handler from an event-emitter callback (e.g. `ws.on("message")`), never leave `void handler()` floating — route it through a single `.catch()` backstop that logs only the error NAME (the message could carry frame… (fonte: P3-419)
- When a wire envelope's metadata is attacker-controlled, validate the full shape (object, string from/payload, non-negative safe-integer seq) with a pure verdict function at the single choke point both transports pass through BEFORE any cry… (fonte: P3-419)
- When mobile-only chrome (like a leading back arrow) reads wrong in the desktop split view, keep the element rendered and demote it with `order` inside the ≥1024px media query instead of deleting it — it can be the pane's only exit path whe… (fonte: P3-419)
- When parsing attacker-controlled wire envelopes (relay frames from an untrusted relay), do validate the full envelope shape in a pure verdict function at the single choke point both transports pass through, before any crypto or session sta… (fonte: P3-420)
- When attaching an async handler to a `message` event, do route it through an explicit `.catch` backstop that logs only the error NAME (message text could carry frame content) instead of `void fn()` — a floating rejection kills the Node pro… (fonte: P3-420)
- When a primary action button sits next to a visually heavier sibling (solid dark CTA), do paint the enabled state with a surface fill plus firm border instead of a transparent ghost — a washed-out resting style next to a heavy button reads… (fonte: P3-420)
- When one card shows two live time displays for the same wait (retry counter + escalation title), do drive both from the same clock helper (escalationMinutes()) and switch seconds→minutes at a pinned threshold tested at the boundary (90 sta… (fonte: P3-417)
- When adding any user-visible i18n key, do add it to both locale dicts AND the pinned per-locale resolution set in scripts/unit.test.ts (the retryElapsedMin addition) — a key present in one locale only ships a raw-key fallback that the exis… (fonte: P3-417)
- When a card heading is hardcoded but its body is state-dependent, do derive the heading from the same state (the existing data-mission attribute already encodes it) and reserve mono styling for real meta lines, using interface font for con… (fonte: P3-417)
- When a visible control is a silent no-op in one component state (reload with no loaded page just returns/catches), branch the affordance on the state flag that already exists (`started`) instead of only guarding the handler — show the acti… (fonte: P3-416)
- When adding a button whose action consumes the adjacent free-text input (Go → `go(input)`), disable it while the input is empty/whitespace-only, otherwise a stray click runs the URL-rejection path and paints the bar red with a named error… (fonte: P3-416)
- When pinning a state-conditional affordance a DOM test can't reach before interaction, assert at source level — slice the component at a stable markup marker (the enclosing `className="browser-bar"` div) and require the state ternary, both… (fonte: P3-416)
- When one screen must differ by platform capability (desktop shell vs phone), do add an opt-in boolean prop defaulting to the legacy render (phone keeps the fully locked map) and thread it from a single bridge check at each mount site inste… (fonte: P3-413)
- When a pane's empty state must match a sibling's composed pattern (icon + title + hint), extend the sibling's CSS selector lists (`.browser-empty-icon, .artifacts-empty-icon`) instead of writing parallel one-off rules — duplicated styles d… (fonte: P3-414)
- When porting an empty-state style into a different layout context (in-flow child of `.list` vs the browser's absolute overlay), adapt the fill mechanism (`flex: 1` + `margin: 0 auto` here) instead of copying the CSS verbatim — the copied p… (fonte: P3-414)
- When a UI skeleton waits for an artifact that a dependency already known to be down cannot produce (QR minted by a dead local agent), do fail fast by deriving the doomed state from the same signal another card renders (`kind`/`busy`) and n… (fonte: P3-412)
- When a timeout-based error window guards an async resource, do reset the timer at each moment the wait becomes meaningful (mount, dependency recovery, retry) so a stretch where the dependency was known-down never consumes the budget — and… (fonte: P3-412)
- When a component branches on error causes that e2e harnesses must distinguish, do expose the branch via copy-independent data attributes (`data-qr-state`, `data-qr-cause`) instead of letting tests pin UI strings, and update source-level ex… (fonte: P3-412)
- When a full-screen ceremony replaces the entire shell (pairing, wizard, onboarding — no sidebar or persistent nav), do pin its exit control to the sticky brand header (absolute, top-left) instead of the end of scrollable content — an end-o… (fonte: P3-411)
- When fixing a below-the-fold layout regression, do encode the structural invariant as source-level checks in the same change (exit markup inside the sticky header + the CSS rule pinning position: absolute) — visual position is invisible to… (fonte: P3-411)
- When adding a control into an existing sticky header, do position it absolutely against the positioned header with an env(safe-area-inset-top) top offset rather than inserting it into the flex flow, so it never disturbs the centered brand… (fonte: P3-411)
- When a form component is hosted on multiple surfaces and at least one host pins the parent state (the add-machine screen pins phase="unpaired"), do render validation verdicts from local state inside the form itself instead of relying on a… (fonte: P3-410)
- When a parser signals two distinct failure modes (parsePairingUri returns null for garbled text but throws for unsupported protocol version), do wrap the call in try/catch and map each outcome to its own error kind with its own localized r… (fonte: P3-410)
- When adding recovery listeners (online/visibilitychange) to a WebSocket client whose attach() runs on every reconnect, register them once per client in the constructor with an idempotent detach on intentional close — per-attach registratio… (fonte: P3-425)
- When a network event anticipates a pending reconnect backoff, zero the reconnect attempt counter before dialing, or the next failure resumes the climbed backoff and the anticipation saves only one wait. (fonte: P3-425)
- When wiring reconnection to connectivity events, act on `online` only and let `offline` force nothing — dialing without a network burns attempts with no chance of success. (fonte: P3-425)
- When adding decision logic to a daemon entry file that boots main() on import (apps/daemon/src/index.ts), extract the rules into a new I/O-free pure module (no imports, no Date.now, no timers) that returns stable reason strings, so unit te… (fonte: P2-327)
- When a reconnect loop schedules its next dial with setTimeout, keep the timer handle and a dial-in-flight flag at module scope (set true at socket creation, cleared on BOTH open and close) so an external trigger can clearTimeout + redial e… (fonte: P2-327)
- When exposing an endpoint that anticipates reconnect waits, record which floor the pending wait carries — a floor from the relay's own close code (capacity/rate-limited) must always be honored while only local dial-error floors and plain b… (fonte: P2-327)
- When classifying Electron net.fetch failures, do normalize on the DOMException NAME first ("TimeoutError"/"AbortError" — the prose message matches no net-error regex), then cause.code, then message, and treat the "Redirect was cancelled" r… (fonte: P2-328)
- When adding a reachability test for a ws:// address, do probe the derived plain http(s) /healthz on the same host and port instead of opening a WebSocket, re-validate the raw URL with the same validator in the privileged caller before dial… (fonte: P2-328)
- When a user-triggered probe fetches a remote body, do read it through the reader with a hard byte ceiling (only for the statuses that need it) and keep at most one probe in flight by caching the shared Promise so concurrent callers await t… (fonte: P2-328)
- When handling a custom OS protocol (deep link) in an Electron shell, wire every delivery path — macOS `open-url`, `second-instance` argv, AND the first process's `process.argv` for Windows cold start (consulted once inside the single-insta… (fonte: P2-329)
- When a decision depends on multiple exclusions (harness session, dev build, platform), extract it into a pure, electron-free function with the rule order fixed and documented in the header, and pin every branch plus the real wiring (exactl… (fonte: P2-329)
- When new code runs at module-evaluation time (outside `app.whenReady`, e.g. the single-instance lock branch), any module-level `let` it touches must be declared earlier in the file — a handler writing `lastDeepLink` declared thousands of l… (fonte: P2-329)
- When a new mitigation can soften an existing destructive path (e.g. blocking a task after infra-starvation), make every evidence source fail-closed — probe errors, empty output, unexpected shapes and unparseable details must degrade to the… (fonte: P2-334)
- When decision logic is a first-match-wins rule chain, isolate it in a pure module (no network/git/fs) with stable reason strings, and make its parsers enumerate every real format the producers emit (legacy per-job and aggregate forms alike… (fonte: P2-334)
- When an input's placeholder would carry a concrete example value (e.g. "http://localhost:3000") while the adjacent action stays disabled on empty input, do use a generic format hint (e.g. "https://…") so users never read the placeholder as… (fonte: P3-448)
- When fixing user-visible copy in apps/web, do pin the invariant in scripts/unit.test.ts with a paired assertion — new string present AND old pattern absent (e.g. includes('placeholder="https://…"') && !includes("localhost")) — so the deter… (fonte: P3-448)
- When a change alters user-visible behavior, do update both README.md and README.pt-BR.md in the same commit, annotating the task id inline, so the localized docs never keep describing the old behavior (fonte: P3-448)
- When a primary CTA could get a bespoke style (inverted `--fg` fill, one-off hover), reuse the shared `button.primary` accent identity instead — parallel primary actions in the same user journey (wizard "Começar" vs reconnect card, minutes… (fonte: P3-450)
- When superseding a design decision that was encoded in computed-style gate assertions (P3-371's `bgIsFg` neutral-solid paint), migrate the matching `desktop-flow.test.ts` assertion in the same commit — probe elements + `getComputedStyle` a… (fonte: P3-450)
- When resolving a consistency finding either way (unify OR declare deliberate), record the ruling in docs/PRODUCT.md ("accent = highest action on the screen") so future fable/explorer reviews don't re-flag the same split as an accident (fonte: P3-450)
- When differentiating states of a tiny indicator (≤6px dots, pills, chips), do not rely on neutral-strength token steps (`--border` vs `--border-strong`) — they render perceptually identical at that scale; use a hue-channel change instead,… (fonte: P3-451)
- When restyling any CSS class, first grep `scripts/unit.test.ts` for the class name — the invariant battery string-pins rule bodies via `indexOf` slices, and the check must be updated in the same commit keeping both the positive assertion (… (fonte: P3-451)
- When adding a translucent accent surface, reuse the established `color-mix(in srgb, var(--accent) N%, transparent)` recipe already used by the drag overlay in index.css instead of minting a new token or raw rgba. (fonte: P3-451)
- When a shared component renders both a "real content" variant and a quiet caption/note variant, split them at the JSX boundary with early returns (note outside the styled container) instead of conditionally rendering children inside the sh… (fonte: P3-447)
- When a text-only element sits beside live navigation that lists the same items, strip all card affordances (border, background, radius) from it — any bordered/filled block reads as clickable regardless of actual interactivity, and duplicat… (fonte: P3-447)
- When fixing a "looks like a button but isn't" finding, pin the structural invariant in the unit battery by asserting on source shape (branch order in the TSX, chrome tokens only in the full-card CSS branch via regex), so a future refactor… (fonte: P3-447)
- When writing empty-state or hint copy that directs the user to another pane (chat, browser…), do check whether that pane is reachable behind the current gate — a pre-pairing mount must show a "after pairing…" deferral variant instead of po… (fonte: P3-446)
- When copy depends on gate state, do thread an explicit opt-in boolean prop from the top-level shell down through the component tree with the legacy string as the default (P3-413 pattern) — never introduce a second source of truth or mutate… (fonte: P3-446)
- When adding i18n keys, do update the per-locale resolution test lists and assert the variant's semantic (contains "After pairing"/"Depois de parear") plus a bounded count of opt-in mounts — so drift between locales and accidental propagati… (fonte: P3-446)
- When building an upstream URL from an attacker-controllable string with `new URL(input, base)`, validate the raw string's shape before any parsing — WHATWG parsing silently discards the base when the input carries a scheme or authority (`h… (fonte: RT-453)
- When adding a security boundary the codebase already styles as a "guard", follow the house pattern — a pure fail-closed verdict module (no I/O, clock, or logging inside, since unit tests import it without booting the daemon) wired at the s… (fonte: RT-453)
- When you constrain a passthrough behind a hand-maintained allowlist, add a source-reading regression test asserting exactly one occurrence of the dangerous URL construction plus the guard call in the wiring file (any future second passthro… (fonte: RT-453)
- When skinning a native `<select>` to match a designed card, do set `appearance: none` (with `-webkit-` fallback) and render any drawn affordance (chevron) as a real SVG on a wrapper span — `<select>` is a replaced element, so `::after`/`::… (fonte: P3-445)
- When a visual fix hinges on a CSS declaration that DOM tests cannot observe, do pin it with source-reading assertions that regex-match the rule block and assert the exact `var()` token per state (rest border, hover escalation, surface fill… (fonte: P3-445)
- When an overlay/affordance sits on top of an interactive control, do give it `pointer-events: none` so clicks pass through to the underlying select/button (fonte: P3-445)
- When styling a disabled action button inside a designed card, dim only the label (color: var(--muted)) and keep the firm border/surface — never apply opacity to the whole control, or it reads as a broken ghost beside the enabled CTA instea… (fonte: P3-449)
- When a screen needs its main action, reuse the shared accent primary (button.primary) instead of inventing a one-off paint like inverted fg/bg, so one user journey never shows two competing primary-action dialects; keep warn tones on the s… (fonte: P3-449)
- When a change supersedes a previously pinned visual invariant, update the computed-style assertion that encodes it (e.g. scripts/desktop-flow.test.ts checking bgIsFg → bgIsAccent) in the same commit, or the gate keeps enforcing the old des… (fonte: P3-449)
- When two cards in the same visible journey repeat the same promise, do state it once in the card where the user acts and let the other card say only what happens next (fonte: P3-436)
- When fixing copy in `apps/web/src/lib/i18n.ts`, do mirror the edit across both the en and pt dictionary entries in the same commit — the parallel dictionaries keep the removed duplication alive in the unedited locale (fonte: P3-436)
- When an earlier task pinned a component's TSX shape against a visual regression, do also pin the CSS side — extract the class's rule block and assert it carries no card-affordance tokens (border/background/radius/box-shadow), because a lat… (fonte: P3-438)
- When a CSS class must stay scoped to one variant (note vs full card), do assert its occurrence count and source order in the component file (exactly one use, appearing before the card element) so the class can never leak back onto the vari… (fonte: P3-438)
- When a caption/note variant shares a component with a card variant, do treat "zero chrome on captions, affordances only in the card branch" as a durable invariant worth its own battery check — the explorer shot shows a one-line note wearin… (fonte: P3-438)
- When building a passthrough URL from an attacker-controllable path with `new URL(path, base)`, validate the raw string before any parser touches it (must start with `/`; reject `//`, any backslash, control chars/whitespace the WHATWG parse… (fonte: P3-452)
- When tightening an existing passthrough with an anchored allowlist, first inventory every script that already probes the route (live soak, smoke, chunk regression, e2e) and admit their exact literals — invariants/smoke/chunk all send `GET… (fonte: P3-452)
- When a client feature starts calling a new opencode route through the daemon tunnel, add an anchored entry to `UPSTREAM_ROUTES` in apps/daemon/src/pathguard.ts in the same change — the allowlist is hand-maintained and unlisted routes fail… (fonte: P3-452)
- When a split/multi-column pane renders its secondary column only with data, gate the whole grid on content existence — a primary-column `border-right` still paints a full-height divider beside a dead half-pane when the sibling renders noth… (fonte: P3-444)
- When adding an empty state to a pane, reuse the composed house pattern (centered icon + `-empty-title` + `-empty-hint` stack, muted tokens) instead of new chrome, and keep the pane's auxiliary action card rendered inside the empty world so… (fonte: P3-444)
- When mounting an existing card inside a new empty-state container that sets `color: var(--muted)`, add a scoped override restoring full text color and container-appropriate margins — grid-context margins and inherited muted color silently… (fonte: P3-444)
- When a caps section label and the card title directly beneath it repeat the same action phrase, do rename the label to name the direction/category and keep the action verb only in the card title, so identical copy never stacks 40px apart i… (fonte: P3-435)
- When renaming user-visible copy that an e2e beat asserts as exact literal strings, do update the expected strings for both locale variants (en|pt) in the same commit, or the desktop-flow gate fails on the stale text (fonte: P3-435)
- When docs quote literal UI copy (README journey walkthroughs), do update those quotes in the same commit as the i18n change, or the documentation drifts from the shipped interface (fonte: P3-435)
- When building an upstream URL from a request field a peer can seal (e.g. `req.path` into `new URL(path, base)`), validate the shape first — the URL constructor silently discards the base whenever the value carries a scheme or authority, so… (fonte: P3-442)
- When rejecting attacker-controllable input, log and audit only a structured rejection reason and return 400 — never echo the raw path/query (log-injection) and never attribute it to auth failure, which would train clients to retry as auth (fonte: P3-442)
- When a gated/disabled primary CTA reads as broken in a static screenshot, put the explanation in a visible caption directly under the control (tooltips vanish in shots and distant footer copy gets ignored) and dim only the label over a fir… (fonte: P3-442)
- When a one-off CSS override carries a comment justifying it by page context (e.g. "so the host card dominates the gate"), re-verify that context still exists before preserving the override — if a later change removed that layout, delete th… (fonte: P3-433)
- When demoting a CTA's visual rank for hierarchy on one screen, inventory every surface that renders the same class combo first — the demotion silently applied to the standalone manual ceremony where the paste form IS the main action, readi… (fonte: P3-433)
- When superseding a pinned style decision, flip its battery assertion to outcome terms — assert the one-off override block is ABSENT and the className combo remains — because token-level matches (exact `var(--bg)`, `var(--border-strong)` in… (fonte: P3-433)
- When interpolating values into AppleScript `do script` (or any host that hands text to a user's shell), quote BOTH layers by construction — POSIX single-quote the shell command (`'…'` with inner `'` as `'\''`) and pass that command to osas… (fonte: RT-439)
- When validating an id or path that is interpolated into a fetch URL or command, enforce its full shape with a strict regex (e.g. `^ses_[A-Za-z0-9]{4,64}$`) rather than a prefix check like `startsWith("ses")` — prefix checks let traversal/m… (fonte: RT-439)
- When hardening injection-prone handlers, extract validators/quoting/argv assembly into a pure module (no I/O, clock, logging) so they are unit-testable in isolation, and never log the rejected input value — logging attacker-controlled stri… (fonte: RT-439)
- When styling error feedback for recoverable user mistakes (a paste error, an empty submit), reserve full danger treatment (danger border + tinted banner) for true incidents and reuse the house `.degraded-upstream` tone instead — neutral `v… (fonte: P3-432)
- When a task's acceptance is purely visual (colors, borders, backgrounds), DOM tests can't observe it — pin the rule blocks by reading `index.css` text (slice from `.selector {` to the next `}`) and asserting the CSS tokens, while keeping s… (fonte: P3-432)
- When demoting an action button from danger styling to the neutral bordered language, add explicit hover feedback (`color-mix(in srgb, var(--fg) 6%, transparent)`) — a transparent-background neutral button otherwise reads as static text and… (fonte: P3-432)
- When a form field needs an error visual state, set `aria-invalid` from the same state variable that renders the inline error message and style it with `[aria-invalid="true"]` (plus `:focus`/`:focus-visible` variants) — one attribute serves… (fonte: P3-431)
- When overriding a global `:focus-visible` accent ring for an error state, override only the longhand `outline-color` (and `border-color`) with the danger token instead of restating the full outline shorthand — width/style inherit from the… (fonte: P3-431)
- When the spec asks to suppress a styling token in favor of another, assert the old token's ABSENCE in the test (e.g. `!/var\(--accent\)/` over the matched rule), not just the new one's presence — presence-only checks let a global rule chan… (fonte: P3-431)
- When a card/component must render in both a populated grid and a new empty-state world, do extract it into a single shared JSX variable used by both branches so its state and actions never get orphaned by the empty collapse (fonte: P3-434)
- When adding an empty state to a pane, do extend the existing `.browser-empty-*`/`.artifacts-empty-*` selector groups and size the container to the parent's layout mode (`.screen` flex column → in-flow `flex: 1`, not the browser pane's abso… (fonte: P3-434)
- When rendering a pane title or accessible label, do use the shared `t()` dictionary key in every branch — including fallback (`!daemonApi`) branches and iframe `title` attributes — and keep inline icons out of the pane `h1` (fonte: P3-434)
- When a battery check counts patterns in source, count structural elements (e.g. the <select> tag) after slicing the file to the component region between stable className anchors, because raw token/key counts over the whole file get inflate… (fonte: P3-429)
- When replacing an existing UI pattern with a new one, pin both directions in the tests — new markup present AND the old direct-child pattern absent — so a silent partial revert of the refactor cannot pass the battery. (fonte: P3-429)
- When a control is identified only by an invisible aria-label, wrap it in a visible label that reuses the screen's existing furniture grammar (quiet-caps, muted, one type step down) and mirror the sibling field's focus treatment (border esc… (fonte: P3-429)
- When painting selected/active state on a rail or tab list from a shared selector with a default fallback (`topSlot` defaulting to `chat`), do filter the set against the controls actually enabled in the current mode — at the gate the fallba… (fonte: P3-430)
- When introducing a mode (gate, degraded, read-only) that disables controls, do encode its locked set once (`GATE_LOCKED_SLOTS`) and feed both the `disabled` flag and the active-state selector (`gateActiveSlots`) from it — a shared selector… (fonte: P3-430)
- When interpolating values into AppleScript `do script`, remember the string executes twice — as an AppleScript literal, then by the user's shell — so escaping only `"` leaves `;`, `$()`, backticks, `&&` and newlines live; do pass the comma… (fonte: P3-430)
- When an error block names a root cause (e.g. agent/daemon down), offer in that same block the action that fixes the cause — a generic retry whose prerequisite is the down dependency itself (re-minting the QR needs the daemon) is a dead end (fonte: P3-443)
- When threading shell bridges (IPC callbacks like app:reconnectDaemon) into components, accept them as optional props and fall back to the previous generic action when absent (plain browser) — never render a bridge-dependent control uncondi… (fonte: P3-443)
- When adding a fix action to an error state, reuse the existing shared component (ReconnectButton) with the established primary styling instead of duplicating a button — keeps one primary-action dialect across first-boot surfaces (fonte: P3-443)
- When adding chrome/buttons in panes or headers, reuse the shared SVG components from icons.tsx instead of typing unicode glyphs (←, ↻, ⤢) — text glyphs render with font metrics that drift across themes/zoom and create a second visual diale… (fonte: P3-437)
- When a test/boot runner has a watchdog plus a printed verdict, treat the verdict as the exit-code gate: race teardown calls like electronApp.close() against a hard ceiling (they may never settle after SIGKILL on a dead CDP pipe) and preser… (fonte: P3-437)
- When a tool rewrites a file at every agent spawn (e.g. writeSandboxConfig emitting opencode.json), gitignore it immediately — it is generated per-agent sandbox state, never a repo file, and one spawn's config will otherwise get committed o… (fonte: P3-437)
- When wrapping content in new layout containers on a screen with a sticky brand header, do keep the sticky element as a direct child of the scroll container (outside the new composition) — `position: sticky` only pins within its containing… (fonte: P3-441)
- When introducing wrapper divs around children that inherited layout from a parent (e.g. `.screen` gap), do re-declare those inherited properties on the wrappers for the mobile breakpoint — a new container silently drops what its children u… (fonte: P3-441)
- When adding a responsive composition to a screen the shell also switches at a breakpoint, do reuse the shell's exact threshold (1024px / isDesktop) for the new media query — two sources of truth for "desktop" make CSS and shell disagree ab… (fonte: P3-441)
