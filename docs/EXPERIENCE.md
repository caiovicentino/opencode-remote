# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When a module is imported by a file that runs `main()` on import, keep transform/mirror modules pure (no node:fs, node:http, node:child_process, fetch or I/O) and enforce it with a source-reading test; wire post-boot installs through the s… (fonte: P2-300)
- When a classifier gains new verdict states (e.g. zip-temp/unc-share in installloc), grep and extend every consumer that branches on that verdict in the SAME change — P2-299 shipped two states no decision table read, leaving the Windows upd… (fonte: P2-301)
- When gating a download/install action on location verdicts, fail OPEN: only states with positive physical proof the binary can't be replaced may block, while unknown/absent/non-object verdicts allow the action — blocking on doubt freezes m… (fonte: P2-301)
- When appending a rule to an ordered decision chain, add it AFTER the existing rules, keep the test-harness/session rule FIRST, rewrite the full order in the header as a contract, return a new static snake_case skip reason without renaming… (fonte: P2-301)
- When adding a new counter or termination policy to the relay, wire it into ALL observability surfaces at creation time — the /healthz body AND the /metrics endpoint in both Prometheus and JSON formats, reusing the naming grammar of the sib… (fonte: P2-302)
- When publishing a Prometheus series, emit the `# TYPE <name> counter` line immediately before the value line and render zero as an explicit `0` (never conditionally omit the series), so the operator can distinguish a healthy relay with zer… (fonte: P2-302)
- When exposing an existing counter on a new surface, read the SAME in-memory counter at scrape time (no new counter, route, request or timer), never publish room ids/addresses/IPs/ports/tokens in values (write that boundary as a comment at… (fonte: P2-302)
- When a platform-specific smoke script must judge outcomes the CI host can't run, do keep the verdict in a pure, no-I/O module with static reasons and fail-closed on missing tooling, so every failure reason (install-failed, layout-missing,… (fonte: P2-304)
- When adding a guard step to a release workflow, do lock it in with a parity test that reads the real YAML and asserts position relative to the publish step, single occurrence, explicit `shell: bash` and its own `timeout-minutes`, so future… (fonte: P2-304)
- When the daemon publishes a new capability state pair to /api/health or the settings mirror, do file the Machine-state UI row as the immediate follow-up task (declared continuation in README) — this is the third time a backend field waited… (fonte: P2-305)
- When adding a row to machinestate.ts, do follow the sibling pattern exactly: append the key at the END of MACHINE_ROW_ORDER and LABEL_KEYS (row order is a pinned, user-visible contract), map verdicts through a closed exported table like TT… (fonte: P2-305)
- When a test or evidence hatch needs the set of valid capability states, do import the exported table (TTS_STATES/VOICE_STATES/BROWSE_STATES) instead of duplicating the literals — and mind the field-name collision: ttsState is spoken replie… (fonte: P2-305)
- When adding an environment-driven decision to the daemon's relay dial, split it into a pure verdict module (receives caller-normalized env, returns a closed tunnel|direct verdict with a rule-order contract pinned by table tests) plus an I/… (fonte: P2-303)
- When adding an optional transport path (proxy CONNECT) to an existing dial, keep the no-proxy path byte-identical, fail closed to the default on unparseable URL, embedded credential, non-http(s) scheme or non-textual env value (never guess… (fonte: P2-303)
- When the desktop shell already resolves a decision a spawned sidecar needs, inject it via a dedicated env var at spawn time (OCR_RELAY_PROXY) instead of re-resolving in the child, and honor an explicit https:// proxy scheme by TLS-wrapping… (fonte: P2-303)
- When comparing newly saved state against what is in effect, remember the last applied verdict in the main process so re-saving the same choice is an idempotent keep, and make the sidecar-facing field dominate the verdict since a restart su… (fonte: P2-307)
- When adding a periodic runtime check to a long-lived server (cert reload, revalidation, watching files), ride the already-existing sweep/interval instead of introducing a new timer, route or dependency, and pin it with a structural test ov… (fonte: P2-306)
- When hot-swapping TLS material at runtime, apply it with `setSecureContext` inside its own try/catch and refuse fail-closed — keep the in-service pair when the fresh one is illegible, expired, not-yet-valid beyond the documented clock tole… (fonte: P2-306)
- When re-reading mutable files to decide a swap, compare cheap file stats (size/mtime) first and read only on change, capture stats BEFORE the read so a straddled write heals on the next tick, advance the on-disk impression whatever the ver… (fonte: P2-306)
- When a client fail-closes on a property of a published artifact (e.g. the installer sha512 that the update feed declares), make the publishing CI measure that same property from the real bytes and confront declaration vs measurement before… (fonte: P2-308)
- When confronting declared feed metadata against measurements, keep the logic in a pure module (no node:fs, no network) that receives declared entries plus measured values and returns a closed problem list (declared entry without file, dige… (fonte: P2-308)
- When a smoke script mutates host state (hdiutil attach, spawning Electron), do fail closed on missing prerequisites BEFORE the first mutation (check the Playwright driver and platform up front so a missing one can never produce a vacuous p… (fonte: P2-309)
- When probing a symlink that may be broken or mispointed (e.g., the DMG's Applications shortcut), do use lstatSync + readlinkSync instead of existsSync — existsSync returns false for broken links, which would misreport a present-but-wrong l… (fonte: P2-309)
- When validating TLS material from a PEM file with Node's X509Certificate, do not assume the whole file was read — the constructor only parses the FIRST block, so extract every `-----BEGIN CERTIFICATE-----` block yourself (matchAll in file… (fonte: P2-310)
- When a verdict diagnoses user-facing breakage but the system still works (or half-works), keep it explain-only: publish it additively (healthz field, one static log line, warn level for the bad cases) and never wire it into admission/refus… (fonte: P2-310)
- When adding a classification of security material, put the decision logic in a module that imports nothing (no node:fs/tls/crypto, no network), receive pre-extracted data from the caller, map every degenerate input (empty, non-textual, hal… (fonte: P2-310)
- When handling embedded credentials in a URL-like address (proxy user:pass), carry the secret only as an opaque, already-encoded value (the ready header, e.g. base64 `Basic …`) and expose only its PRESENCE (`none`/`basic`) in verdicts and h… (fonte: P2-311)
- When parsing user:password out of an authority string, split at the LAST "@" and split user from password BEFORE percent-decoding (a percent sequence must never create a delimiter), failing closed to NO secret — dialing unauthenticated — o… (fonte: P2-311)
- When new logic must classify errors without changing their policy, keep the error kind/backoff untouched and attach a static, secret-free hint keyed to an unambiguous marker of the response (e.g. `(status 407)` matched by regex, so a Node… (fonte: P2-311)
- When a metric input may be unmeasurable (missing, NaN, negative, infinite), omit the series entirely instead of publishing an invented zero — a fabricated 0 makes threshold-based alerting call a degrading process healthy (fonte: P2-313)
- When you need event-loop/scheduling lag without new timers, hook the already-existing periodic sweep: record max(elapsed − intendedInterval) per window and read-and-reset that max inside the /metrics scrape so each window covers exactly on… (fonte: P2-313)
- When a feature needs to map a raw OS/API state (permissions, downloads, guards) to a user-facing verdict, keep the mapping in a pure module (no electron, no node builtins, no I/O) and have the unit test read the source file to assert that… (fonte: P2-312)
- When reporting permission or system state to the renderer, read it at request time in the IPC handler, never cache it at boot — the user can flip the permission while the app is open and the next ask must see it; reuse existing test hatche… (fonte: P2-312)
- When adding a limit to one path whose sibling path already has a documented limit pattern (upload), mirror it byte-for-byte — same default, ceiling, fail-closed boot refusal and refusal-message grammar — and pin the equality with a test th… (fonte: P2-314)
- When a resource map pruned by age also gains an entries ceiling, run the age sweep BEFORE admission (prune → verdict → insert), never only after insert — otherwise aged-out entries count as live forever and the endpoint wedges at the limit… (fonte: P2-314)
- When an admission verdict guards a resource registration, evaluate it BEFORE creating any identifier so refusals leave zero state behind, return only static refusal copy (no path, file name or measured size — the client renders it as-is),… (fonte: P2-314)
- When a new diagnostic field rides an existing persisted file, sanitize it at the load boundary by rebuilding each record field by field — drop only the malformed record, delete the field when no valid record survives, and keep loading file… (fonte: P2-316)
- When a stored record must never carry free text (prompt, agent reply, error message, path), make it constructible only from typed facts and rebuild every field through a validating normalizer, so hostile/unknown fields are dropped by const… (fonte: P2-316)
- When instrumenting a sweep that has several terminal paths (fire failure, run failure, completion, stuck run, skipped day), emit exactly one record per trigger at every exit and take the sessionId from that run's own local data — a routine… (fonte: P2-316)
- When a CI job exists to prove platform-specific behavior, derive its trigger scope from the files the battery actually exercises (superset of the suite's own module list), never from a single app's directory — a narrower trigger lets path… (fonte: P2-317)
- When widening a workflow job's trigger, add a sibling scope classifier plus a NEW job output and OR it into only that job's condition (classify docs/media-only changesets false via an inert-extension list) — overloading an existing output… (fonte: P2-317)
- When a workflow condition encodes a correctness guard, pin it with a unit test that reads the real workflow YAML asserting the output is declared, the condition cites all required outputs, and sibling jobs' conditions stay untouched — so f… (fonte: P2-317)
- When killing a spawned child process in Node on Windows, do not rely on signals for graceful shutdown (child.kill("SIGTERM") there is an immediate unclean termination, so drain logic wired to signal handlers never runs) — open the spawn IP… (fonte: P2-315)
- When a module's purity is a load-bearing invariant (no electron, no node:fs, no I/O), do not trust convention — add a test that reads the real source file and asserts it contains no such imports, and make every input combination (channel p… (fonte: P2-315)
- When exposing an IPC handler that reports an OS permission (camera, mic), read the system state inside the handler at request time, never at boot — the user can flip the permission in system settings while the app is open, and a cached sna… (fonte: P2-319)
- When a web component gains a shell-only capability (verdict phrases, settings targets), inject it as an optional bridge prop (e.g. `getCamAccess?: () => Promise<Verdict | null>`) and fall back to the existing i18n dictionary phrase when th… (fonte: P2-319)
- When a feature depends on a hard-to-reproduce OS state like "permission denied", wire the pre-existing env hatch (`OCR_DESKTOP_CAMERA_BLOCK=1`) into the new handler instead of inventing a new one, and route any "open system settings" actio… (fonte: P2-319)
- When rendering records that cross a serialization boundary (route payload → UI), re-validate every field against its shape and a closed set (finite parsed timestamp, integer ≥ 0, known outcome string) and discard a malformed record alone s… (fonte: P2-318)
- When a component derives display labels from data (relative time, readable duration, translated outcome), extract a pure module that takes `now` and the translate fn as parameters instead of reading clocks/window/i18n globals — no React, n… (fonte: P2-318)
- When capping a history list for display, sort by the parsed instant first and cap after — capping in raw wire order silently keeps the oldest rows instead of the newest (fonte: P2-318)
- When committing a generated artifact to the repo (e.g. deploy/relay/alerts.yml), pin it with a unit test that reads the committed file and asserts byte-for-byte equality with the generator's output, and have the generator emit a "GENERATED… (fonte: P2-320)
- When a pure module makes claims about a runtime surface it doesn't own (alert rules citing metric series, config keys citing env vars), add a fail-closed test that reads the real emission site's source and proves every cited name exists th… (fonte: P2-320)
- When asserting a module's purity by grepping its source for banned tokens (node:fs, setTimeout, fetch), strip comments first — a doc header that documents the hygiene boundary names those tokens verbatim and makes an unstripped check false… (fonte: P2-320)
- When adding or touching a GitHub Actions job, always declare a job-level `timeout-minutes` coherent with the work — step-level timeouts never cover hangs between/outside steps (dependency install, image pull, disk mount) and GitHub's undec… (fonte: P2-322)
- When supervising a spawned child only through exit handlers, also probe its health endpoint after the first healthy boot — a child that wedges alive (port bound, event loop pinned) never exits, so exit-based supervision never fires and the… (fonte: P2-321)
- When adding a watcher/recovery loop around stop/respawn machinery, guard with an in-flight flag so the recovery's own stop and fresh spawn do not disarm the watcher mid-recovery, and never fire recovery while another stop/respawn transitio… (fonte: P2-321)
- When writing a decision classifier (exit, stop, wedge verdicts), keep it a pure module — no electron, no node builtins, no timers, no I/O — with a closed verdict set, fail-closed defaults (malformed/missing input → the least destructive ve… (fonte: P2-321)
- When writing UI code that runs inside the Electron desktop shell (apps/web), never call `window.prompt`, `window.confirm` or `window.alert` — Chromium in Electron does not implement `prompt` (the action silently no-ops with no error) and `… (fonte: P2-323)
- When adding a dialog or confirmation flow, split it into a pure descriptor module (no React, no fetch, no `window`, fail-closed on unknown intent/invalid input) that is table-testable, and pair it with a test that reads the real source fil… (fonte: P2-323)
- When building a modal dialog, treat focus as part of the contract: focus moves to the text field (or confirm button) on open, returns to the element that opened it on close, Esc cancels, Enter confirms, and the 150–300ms animation must be… (fonte: P2-323)
- When client state that gates reconnection (lastSeen/awaitingPong) can be touched by frames crossing an untrusted relay room, do update it only after the sealed envelope opens (single markAlive() on the authenticated path) and demote clear… (fonte: RT-341)
- When hardening decision logic inside a large client, do split it into a pure gate module (no DOM/WebSocket/crypto/timers, fixed-order classification, tolerant readers that never throw) with thin wiring, then pin the wiring with source-read… (fonte: RT-341)
- When extending the task-id grammar with a new namespace (RT-\d+), do update every copy of the id regex in the same change — backlogview.parseTaskLine, inline daemon API validators, pilotforensic.dataTask — and pin tests accepting both name… (fonte: RT-341)
