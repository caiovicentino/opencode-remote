# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When a shell reuses an external (adopted) daemon it did not spawn, do not apply the owned-child respawn budget/give-up semantics — only your own children can exhaust a budget; for adopted ones probe health forever with capped backoff and s… (fonte: P1-053)
- When arming a self-scheduling async watchdog, make start/stop idempotent, stop it before intentional stops or fresh spawns, and re-check the armed flag after every await — an in-flight probe must never schedule a timer or mutate state for… (fonte: P1-053)
- When recovery from a daemon outage must not require re-pairing, never rewrite the 0600 state file or allowlist during reconnect, keep the degraded state's uri null so no QR overlay can open from it, and re-run auto-pair once when the banne… (fonte: P1-053)
- When you see a CSS var used with a hardcoded fallback (e.g. `var(--panel, #101014)`), treat it as a used-but-undefined token: define it in `:root` for every theme and delete the fallbacks, because theme-specific fallbacks silently break th… (fonte: P2-055)
- When replacing emoji-as-icons with a real icon set, centralize everything in one icons module with a shared base SVG wrapper (same viewBox, 2px stroke, `currentColor`, `aria-hidden`) and a typed component per icon, and remove the old emoji… (fonte: P2-055)
- When subscribing a status/event callback after an async `connect()` resolves, sync the current state explicitly right after subscribing (`setStatus(client.status)`), since the last event may have fired before the handler existed and the in… (fonte: P2-055)
- When adding automated `git commit`/`git push` in pilot code, do retry the push with a sleep between attempts (extract a helper with injectable exec/sleep like `commitAndPushFindings`) because concurrent researcher/scribe/explorer pushes mo… (fonte: P3-052)
- When unit-testing shell-out helpers, do inject `exec`/`sleep`/`exists` as an interface so push-failure/exit-code semantics are testable with fakes, and add a real-git smoke test against a bare remote whose commit message contains an apostr… (fonte: P3-052)
- When adding a long-running inline agent pass to the scheduler loop, do feed `touchHeartbeat()` from the agent's stdout callback and persist its once-per-day state guard before the run starts, so the self-watchdog doesn't kill the pilot mid… (fonte: P3-052)
- When adding state preservation across attempts (e.g., keeping a task branch between runs), audit every pre-existing reset/fallback path that touches that state — a reset to origin/main that was safe on a fresh workspace destroys preserved… (fonte: P1-060)
- When parsing multiple trailing metadata tags from a backlog line, strip them right-to-left in a loop anchored at end-of-line so tags work in any order and a tag mentioned mid-spec stays part of the spec text (fonte: P1-060)
- When reviewing long-horizon work, review the incremental diff since a round checkpoint instead of the truncated whole-branch diff, and re-validate any sha/id loaded from a file outside the repo against a strict charset (e.g. /^[0-9a-f]{7,4… (fonte: P1-060)
- When adding an escalation/arbitration layer (stronger reviewer) that can reject a build, union its verified kept findings with the earlier rounds' verified kept findings (dedup) before feeding the builder — an arbiter must add evidence, ne… (fonte: P1-059)
- When spawning a non-streaming CLI (e.g. `claude -p` emits only the final answer), do not depend on stdout activity for liveness — feed self-watchdog heartbeats on a timer, treat the run as valid only if exit 0 + non-empty output + expected… (fonte: P1-059)
- When capping a directory listing into an LLM prompt, sort by mtime descending BEFORE slicing — `readdirSync` order is arbitrary and a raw cap silently drops the newest carryover evidence (fonte: P1-059)
- When a server hosts sessions over multiple transports (e.g., relay + direct loopback WS), do scope connection-loss teardown to the failed transport's sockets (match `session.socket === ws`) instead of clearing the whole session map — clear… (fonte: P1-061)
- When an expiry/stale sweep keys on `lastSeen`, do refresh that timestamp on every liveness signal including pings — otherwise broadcast() skips an open, heartbeating socket and the client stays "paired" but blind, never triggering a reconn… (fonte: P1-061)
- When adding a new dial/failover path to a client, do write an integration test that drives the real `connect()` with the credential provider stubbed, not just unit-pinned pure helpers — silent-socket bugs (e.g., missing send-on-open → 3s t… (fonte: P1-061)
- When a viewer component must render both as a fixed full-screen overlay and embedded inside a flex pane, add a `variant` prop that switches only the positioning styles instead of forking the component, and gate the two render sites with co… (fonte: P2-062)
- When UI must switch layout at a viewport threshold, initialize state from `window.innerWidth` and keep it live with `matchMedia(min-width).addEventListener("change", ...)` — a one-shot check or ad-hoc resize handler goes stale when the win… (fonte: P2-062)
- When wrapping an existing max-width reading column into a flex row, explicitly override the parent constraint (`max-width: none`, including scoped variants like `.desk-main .screen`) and set `min-width: 0`/`min-height: 0` on the flex child… (fonte: P2-062)
- When an agent ingests untrusted external content (fetched web pages, pushed text) on the host, do not run it with shell or edit permissions — run it with a bash/edit/external_directory:deny sandbox, make it emit proposals as plain text bet… (fonte: P1-057)
- When a runner commits or pushes on behalf of an agent, do not push blindly — gate the push on git diff --name-only origin/main...HEAD matching exactly one allowlisted path, re-read the guard from the real branch diff on every retry, and ne… (fonte: P1-057)
- When serving an authenticated dashboard, do not embed the apiToken in the rendered HTML — have the browser exchange the Bearer token once for a short-lived HttpOnly SameSite=Strict session cookie, keep sessions memory-only so a daemon rest… (fonte: P1-057)
- When recording a success marker after an async/queued operation (`gh pr merge --auto` returns success while the squash is still queued), capture the pre-op HEAD first and only record the post-op state if HEAD actually moved past it AND car… (fonte: P2-058)
- When a best-effort write underpins a safety guarantee (quarantine, allowlist, lock), treat its failure as a degraded guarantee: escalate via the human-notify channel in addition to logging, so the missing protection is visible instead of s… (fonte: P2-058)
- When gating an action on state files (verified merges, quarantine), resolve the target by walking first-parent history to the newest valid non-banned entry (skipping bookkeeping commits, fail-closed when none) and re-validate the concrete… (fonte: P2-058)
- When uma navegação de UI for controlada por booleanos independentes (settings/filesView/artifactsView...), unifique-os num único reducer com view stack e derive o estado ativo via helpers puros (topSlot/activeSlots/isPaneOpen) — estados im… (fonte: P1-046)
- When adicionar atalhos de teclado num shell Electron, conecte os accelerators do Menu no main via IPC com action ids string (`ocr:menu-action`) e só registre o fallback keydown no renderer quando o bridge de preload estiver ausente — dentr… (fonte: P1-046)
- When abrir um pane puder destruir estado vivo (chat aberto, página do browser, input digitado), mantenha a superfície montada — coluna de chat dedicada + pane com `display:none` para o browser — em vez de renderização condicional; remontar… (fonte: P1-046)
- When parsing verdict/status markers out of free-text agent output, never use a substring test like `/VERDICT:\s*APPROVE/i` — markers quoted in prose or examples mask the real one; collect all matches, let the LAST one decide (`parseVerdict… (fonte: P2-038)
- When a verifier classifies LLM findings as hallucinated, respect category boundaries: a code observation has no stdout, so verify it deterministically instead of dropping — cited `file:line` must exist in the workspace and every quoted sym… (fonte: P2-038)
- When writing regexes for file citations, add a negative lookahead after the optional extension (`(?!\w)`) so `.tsx` is not read as `.ts` (this silent truncation dropped 7 valid findings), and resolve bare-name citations like `CommandPalett… (fonte: P2-038)
- When building CLI tools that shell out to optional binaries (e.g. converters), resolve candidates from both PATH and known install locations (like /Applications/LibreOffice.app/Contents/MacOS/soffice) and keep the selection/validation logi… (fonte: P2-065)
- When a task touches UI, cite the 1440x900 (and 390px) screenshot paths in the EVIDENCE block alongside typecheck/test output — the deterministic gatekeeper rejects UI tasks whose evidence lacks shot paths, even with green builds. (fonte: P2-065)
- When a UI has mutually exclusive panes, replace the boolean-flag patchwork with a single reducer-owned view stack: parallel booleans (session/settings/filesView/…) produce double-active buttons, stale panes, and chat-destroying navigation… (fonte: P2-065)
- When bumping Electron (even patch-level), verify the packaged artifact's real runtime version (e.g. CFBundleVersion in Electron Framework.framework/Info.plist after `dist --dir`) before running the validation battery, so gates exercise wha… (fonte: P2-066)
- When an update/e2e probe logs errors, classify them as test-fixture artifacts vs regressions by checking the decision line and downstream behavior: a self-pointing feed URL yields a log-only `ditto: Couldn't read PKZip signature` while the… (fonte: P2-066)
- When auditing breaking changes from a runtime major, grep the codebase for each affected API and record per-change impact against actual usage (none — renderer uses navigator.clipboard, only openAtLogin is passed), and split bundle-size de… (fonte: P2-066)
- When you find `var(--token, <literal>)` fallbacks in themed UI, remove them and define the token in both theme blocks — a fallback silently masks an undefined token and pins the wrong color in the other theme instead of surfacing the bug (fonte: P1-047)
- When restyling components you don't own via a global stylesheet, replace `!important` + `nth-child` positional selectors by adding real semantic class hooks to the markup — positional patchwork breaks silently whenever the component's DOM… (fonte: P1-047)
- When theme logic follows the OS through `prefers-color-scheme`, re-applying the theme must remove the previous `matchMedia` change listener before registering a new one, or toggling themes leaks listeners and fires duplicate updates (fonte: P1-047)
- When a value extracted from agent-adjacent logs (stdout bridges like `--print-logs`) feeds shell or AppleScript interpolation, do treat it as hostile input and validate structurally — strict charset allowlist plus anchoring to the expected… (fonte: P2-048)
- When JSON-parsing a request body inside a long-running daemon route, do wrap the parse in try/catch and return 400 — one malformed POST must never become an unhandled rejection that can take the whole process down. (fonte: P2-048)
- When hardening a sink that consumes untrusted values, do validate or drop the same value at every consumer, including success responses echoed back to the renderer — returning log-derived fields unvetted keeps the broken trust boundary ali… (fonte: P2-048)
- When adding follow-tail autoscroll to a streaming chat, gate the `scrollIntoView` effect on an "at bottom" flag kept in a ref (read inside the effect to dodge stale closures) mirrored by state for the ↓ jump pill, use a px threshold (e.g.… (fonte: P2-049)
- When writing e2e/desktop-flow assertions that grep JSON-stringified IPC output, match the actual user-visible copy for every locale (e.g. `/Invalid pairing code|Código de pareamento inválido/`) instead of a generic non-empty-quoted-string… (fonte: P2-049)
- When converting inline overlay divs into accessible modals, capture `document.activeElement` as the trigger before focusing the dialog, restore it in the effect cleanup, wire Esc via a document keydown listener, trap Tab by cycling first/l… (fonte: P2-049)
- When running long synchronous work (exec, CPU-bound loops) in a process that shares a watchdog/heartbeat timer, touch the heartbeat on BOTH sides of the blocking call or make it async — a blocking run within its own allowed timeout can sta… (fonte: P1-044)
- When a module exposes an injectable event sink (emitEvent/onEvent pattern) for testability, route ALL new events through it, never the raw global emit — otherwise the eval battery silently loses the ability to pin the new behavior. (fonte: P1-044)
- When new control flow lives inside an existing loop (sliding windows, modulo scheduling, multiple rollback branches), extract the loop into a function with injectable probe/clock/sleep/event deps and pin the wiring in tests — pinning only… (fonte: P1-044)
- When detecting "did the state change" (e.g., self-reload after deploy), capture the pre-mutation value (`prev`) before any reset/checkout and compare against it afterward — a `git diff <sha>..HEAD` computed after resetting to that sha comp… (fonte: P1-034)
- When comparing git ids, normalize both sides to full ids (post-reset `git rev-parse HEAD`) before string comparison and validate them with SHA_RE — abbreviated vs full 40-hex ids false-positive against each other, and empty/malformed ids m… (fonte: P1-034)
- When fixing a silent no-op condition, extract the decision into a pure exported function, unit-test the exact failure case ("HEAD moved but diff empty") and pin the broken pattern with a source-level grep test so it cannot regress (fonte: P1-034)
- When a staleness-based watchdog can kill the process during long awaits of aux agents, feed the heartbeat with a periodic timer for the duration of the await instead of pausing/disabling the watchdog — pausing blinds it to real event-loop… (fonte: P1-035)
- When wiring liveness signals around spawned children, never feed the watchdog from stdout callbacks: a healthy-but-silent agent emits no chunks and starves the check; arm an independent setInterval at spawn and stop it on every termination… (fonte: P1-035)
- When adding timers that write shared process state (heartbeat files, locks), return an idempotent stop function from the arming helper and make the touch callback + interval injectable so unit tests are hermetic and never touch the real st… (fonte: P1-035)
- When core decision logic lives inline inside a large orchestrator that spawns LLM agents (untestable by design), extract it verbatim into an exported pure-ish function and rewire the call site — a byte-for-byte testability refactor unlocks… (fonte: P1-036)
- When testing git-dependent logic, don't mock git: build scratch repos (mkdtemp + bare origin + clone), run the real function, and assert on concrete SHAs — that the preserved commit still resolves (`cat-file -e <sha>^{commit}`) and that di… (fonte: P1-036)
- When recreating/resetting state from scratch (e.g., force-recreating a branch), also clear derived artifacts tied to the old state (checkpoint files, caches) in the same path, or stale metadata like a saved diff range can resurrect already… (fonte: P1-036)
- When a generated artifact (refill lines, lessons, findings) is landed via commit+push inside a slot worktree, persist it to a store outside every worktree (e.g. `~/.opencode-remote/pilot/`) the moment the push fails — a failed `appendCommi… (fonte: P1-037)
- When building a reland/retry loop for pending data, pre-filter ids against `origin/main` before pushing and map "everything already landed" to a terminal state that clears the store — only transient `failed` may keep the file for retry; a… (fonte: P1-037)
- When adding durable state, use the failureLessons pattern — pure helpers + fs wrappers with an injectable path and atomic tmp+rename writes, corrupt reads return `null` not throw — and test survival with a real bare-remote clone plus fake… (fonte: P1-037)
- When caching command execution results across pipeline phases in a multi-slot setup, key the cache by (command, cwd) with a separator that cannot appear in either field — a command-only key silently aliases results between workspace clones (fonte: P2-040)
- When a shared execution cache spans phases (preflight, evidence re-run, gate steps), create the map in the orchestrator (round loop) and pass it down as a parameter; components that instantiate their own cache can never share entries (fonte: P2-040)
- When caching results of deterministic commands, cache failures too and recreate the cache at every boundary where the code may have changed (new builder round) — caching only successes re-rolls deterministic failures, and caching across ro… (fonte: P2-040)
- When adding a recovery path (rollback, restart, failover), do not end it with a blind sleep — poll the health endpoint in a bounded, constant-documented window and surface the verdict (structured log, feed event, UI alert, supervisor escal… (fonte: P2-041)
- When a function mixes logic with side effects (probing, clock, event emission, notifications), do extract a hooks object with injectable overrides and production defaults so tests run hermetically without real fetches or timers (fonte: P2-041)
- When deriving alert state from an event feed, do scan the full history for the newest relevant verdict (a healthy verdict or later clean completion clears older alerts) instead of a truncated tail, and pin the wiring with a source-level un… (fonte: P2-041)
- When adding a context block (lessons, failure records) to a new agent prompt, grep docs for role enumerations ("builder e strategist"-style claims) and update all of them (AGENTS.md, README, templates) in the same change — stale role lists… (fonte: P2-042)
- When a prompt function gains new context, use optional defaulted params (e.g., `lessons: string[] = []`, empty block default) so no-match callsites keep the prompt clean, and add tests asserting both presence (injected) and absence (empty)… (fonte: P2-042)
- When several prompts need the same distilled context, reuse the existing selectors and formatters (pickRelevantLessons, failureLessonsBlock) instead of re-implementing keyword matching — divergent selection logic makes different roles act… (fonte: P2-042)
- When skipping a slow idempotent step (npm ci, build, migration) because "input unchanged", decide from a persisted record of the last *successful* run (hash file), never from an intra-run before/after comparison — only the persisted marker… (fonte: P1-021)
- When an error/rollback path also installs dependencies, re-record the success marker only if that install verifiably succeeded, keeping the invariant "saved hash == what's on disk"; leave state stale on failure and let a repair ladder (fas… (fonte: P1-021)
- When command flags encode security requirements (--ignore-scripts, ELECTRON_CACHE), export the command as one named literal and pin it by exact string in the eval battery — duplicating the command inline at each call site is how supply-cha… (fonte: P1-021)
- When querying a SQLite database owned by a live process (e.g. opencode.db during reconciliation), open it with `-readonly` (or `file:…?mode=ro`) even for pure SELECTs, because a plain connection can still write WAL/journal files against th… (fonte: P2-028)
- When reconciling a stored total against a source that grows over time (resumed sessions, retried attempts), recompute it REPLACE-style from the full deduplicated id list persisted beside the total, never ADD deltas — and on a transient DB… (fonte: P2-028)
- When capturing ids from agent stdout that will later be interpolated into SQL, anchor the capture regex with a lookbehind so glued prose (`mytask_ses_…`) never matches, enforce a strict charset guard before inlining, send the query via std… (fonte: P2-028)
- When retrying a pilot task after a failed/crashed attempt, don't `git checkout -b pilot/<ID>` blindly — the branch may be preserved by design from the previous attempt, so use `git checkout -B` (or check `git rev-parse --verify` and reuse)… (fonte: P3-010)
- When writing a Node CLI script that needs unit tests, export the pure logic (`listProblems`, `resolveBundleDir`) and guard `main()` behind `import.meta.url === pathToFileURL(process.argv[1]).href` so the test can import the module without… (fonte: P3-010)
- When validating electron-builder output, remember layouts vary per platform — mac `.app` puts resources in `Contents/Resources` with the binary in `Contents/MacOS/*`, win/linux `-unpacked` use `resources/` at root with `*.exe` / mode-bit e… (fonte: P3-010)
- When a WebSocket server shares its http server (ws upgrade path), do not call server.closeAllConnections() during shutdown — send close(1001) per ws client first, use server.close() to refuse new admissions, wait ~250ms so close frames rea… (fonte: P2-023)
- When implementing shutdown/drain logic, extract it to a pure module with injectable sinks (log, exit, setTimeout/clearTimeout) so unit tests can simulate hung drains, forced exits and double signals with fake timers without killing the tes… (fonte: P2-023)
- When starting a task branch, first check whether `pilot/<ID>` already exists from a prior attempt and reuse/checkout it — unconditional `git checkout -b pilot/<ID>` fails with status 128 "branch already exists" and crashes the pipeline (fonte: P2-023)
- When persisting state whose truncation silently resets counters (circuit breakers, attempt trackers), do write to `<file>.tmp` then renameSync over the destination — rename is atomic within the same filesystem — and on any failure unlink t… (fonte: P2-024)
- When adding fs-touching helpers that need unit tests, do take the fs surface as an injectable parameter (interface with default real impl, P3-013 pattern) and assert the tmp+rename+cleanup contract against an in-memory mock, plus one real… (fonte: P2-024)
- When starting work on task `<ID>` whose `pilot/<ID>` branch survived a failed attempt, do reuse the existing branch (`git checkout pilot/<ID>`) instead of `git checkout -b pilot/<ID>`, which exits 128 "branch already exists" and crashed th… (fonte: P2-024)
- When adding Electron-coupled behavior (tray items, shell opens, dialogs), extract the decision logic into pure helpers in tray.ts-style modules (no electron imports) with fs/shell passed in as injectable sinks, so unit tests cover every pa… (fonte: P3-016)
- When a tray/menu action must be best-effort, catch all fs/shell failures inside the helper and resolve `false` instead of rejecting (always mkdir with `{recursive:true}` so an existing or deleted dir never throws); the caller logs the fail… (fonte: P3-016)
- When (re)creating a task branch on a retry attempt, reuse the existing branch idempotently (`git checkout -B pilot/<ID>` or a `rev-parse --verify` guard) because branch preservation after failed attempts makes plain `git checkout -b` fail… (fonte: P3-016)
- When keying any per-IP limit, allowlist, or bucket on `req.socket.remoteAddress`, do normalize first — unmask IPv4-mapped `::ffff:a.b.c.d` (dotted or hex tail) to plain IPv4 and aggregate real IPv6 by its /64 prefix (first 4 hextets, lower… (fonte: P2-026)
- When a normalized key feeds both admission and release paths, do normalize exactly once at the single ingestion point and stash the key (e.g. on the socket) so every consumer acts on the identical bucket — normalizing at each call site ris… (fonte: P2-026)
- When writing parsers for untrusted address strings in an admission path, do return a safe fallback (lowercased raw input, never throw) on unparseable values so the control degrades to previous behavior instead of crashing the accept loop (fonte: P2-026)
- When spawning child processes in the packaged Electron desktop app, never rely on `stdio: "inherit"` or forwarded `process.stdout` — pipe stdout AND stderr and tee every chunk to a rotating file under `userData/logs/`, because the packaged… (fonte: P3-018)
- When writing desktop log/IO helper modules, follow the desktop-log.ts contract: inject the fs subset (`LogFs`) and never import `electron` so unit tests run pure against a temp dir; wrap every fs op and route failures to a guarded `onFailu… (fonte: P3-018)
- When a module exposes a process-wide default via init + accessor (like `initSidecarLog`/`teeSidecarChunk`), install it at the top of main.ts before any `spawnChild` and make the accessor a silent no-op before init — late installation loses… (fonte: P3-018)
- When adding a CLI subcommand that mutates shared state (circuit-breaker counters, state.json), do make the no-flag form report-only and reject bare destructive flags (`--clear` without an id is an error, never clear-all), table-testing eve… (fonte: P1-030)
- When repair code shells out using git refnames or task ids (gh probes, `git branch -D`), do validate each refname against the strict `pilot/<TASK_ID>` shape before any shell use, treat an external-probe failure as "skip", and set ok:false… (fonte: P1-030)
- When making a file repair idempotent under a tolerant loader (loadState masks corruption with fresh defaults), do compare the normalized result against the raw file text, not the parsed/reloaded object, so a corrupt file is actually rewrit… (fonte: P1-030)
- When a finding cites multiple files and quotes symbols, validate symbols against the union of all cited files' contents plus the diff, never per-citation — per-citation `every()` checks drop valid cross-file findings (fonte: P1-065)
- When a verifier handles findings of absence ("X has no coverage of Y"), the quoted symbol by definition won't fully appear in the cited file, so require resolution via the citation union rather than failing the check (fonte: P1-065)
- When an "all symbols must match" rule would drop findings mixing real and fake references, add a lenient fallback tier (≥1 quoted span of ≥6 chars matches the union) with a minimum-length guard so short spans never trigger false keeps (fonte: P1-065)
- When changing a feature's state-machine semantics (e.g., update flow becomes check→download→consent→install), grep every user-visible string that mirrors the old flow (tray labels, dialogs) and reword it in the same PR — stale copy like "r… (fonte: P1-050)
- When an HTTP route serves potentially large files on a process that shares its event loop with other duties (e.g., daemon's metrics server also carrying relay WS + /api/*), stream with `await stat()` for content-length plus `createReadStre… (fonte: P1-050)
- When a route must stay unauthenticated because a client can't attach tokens (e.g., electron-updater), contain the blast radius instead of loosening global auth: dedicated folder, per-segment charset validation, extension allowlist, and a r… (fonte: P1-050)
- When a global breaker (pause queue, halt deploys, throttle) fires on a sliding window of events, aggregate failures by the responsible entity id before counting — raw event counts let one stubborn entity (task retrying through maxAttemptsP… (fonte: P2-063)
- When adding an attribution field to persisted state samples, make the aggregation tolerate id-less entries (pipeline crashes, legacy pre-migration samples) via a synthetic per-event key (e.g., `?${at}`) so unattributed failures still count… (fonte: P2-063)
- When changing a trigger's semantics from a rate to a distinct count, re-audit invariants tied to the old metric — the "partial window never trips" guard existed because small-sample rates are noisy; a distinct-entity threshold is meaningfu… (fonte: P2-063)
- When serving paged responses through a frame-limited relay, size each page in exact UTF-8 bytes of the serialized body (Buffer.byteLength — CJK/emoji cost 3-4 bytes per char, so message counts lie), shrink by halving the limit, and degrade… (fonte: P1-064)
- When adding new behavior to an existing endpoint, gate it on new query params and keep the no-param response shape byte-identical (a plain array stays a plain array) — export, handoff and internal syncs depend on the legacy contract, so th… (fonte: P1-064)
- When testing size caps or paging contracts, assert against the body the daemon actually serves (capture the sealed op body or the audit envelope's byte count), never a pre-built synthetic array — a test on fixture data passes while the rea… (fonte: P1-064)
- When reaping WebSocket peers that vanished without a close frame, do terminate via socket.terminate() so the normal close handlers release rooms and per-IP slots — never duplicate cleanup logic in the reaper, or releases will drift from th… (fonte: P2-067)
- When adding time-based policy to the relay, do extract the decision as a pure helper with no ws/node imports (padrão ipcap.ts/ratelimit.ts) that receives `now` and returns victims — and never sweep entries whose lifecycle isn't stamped yet… (fonte: P2-067)
- When testing ws liveness/reaping end-to-end, do remember ws clients auto-pong by default — open the doomed peer with `{ autoPong: false }`, run the relay with a 1s RELAY_PING_INTERVAL_S via env for speed, and assert the reaped socket's clo… (fonte: P2-067)
- When detecting "new message arrived" in a list that gets refetched or has older pages prepended, compare first-element identity plus growth from a non-empty previous anchor (prev.len > 0 && length grew && bubbles[0] unchanged) — length che… (fonte: P3-053)
- When adding UI-derived state like unread counters, implement it as a pure reducer in a lib file (no DOM/Electron imports) so unit tests drive every transition, and confine the side effect (bridge push) to a single function that swallows fa… (fonte: P3-053)
- When main process mirrors renderer-pushed state for OS effects (dock badge), store the last pushed value as the verification surface instead of reading back from the OS, normalize hostile IPC payloads (non-finite/negative → 0), and clear t… (fonte: P3-053)
- When injecting prompt context into opencode sessions, remember `SessionPromptData.system` is per-turn — do inject on every message POST (idempotent via a unique dedupe marker) and audit every creation path (proxy tunnel, /api, scheduled ro… (fonte: P1-068)
- When a guarantee relies on an in-memory registry (Set cleared on restart), do not write docs claiming "every session" — document the restart caveat explicitly or reviewers will flag the README as overstating behavior (fonte: P1-068)
- When an acceptance criterion requires live/manual E2E evidence (e.g., a shot in `shots/builder/`), do actually run the live eval and land the file before merging — green typecheck/unit does not satisfy the AC and the reviewer will reject t… (fonte: P1-068)
- When gating side effects on event role/source (auto-open panes, notifications), do fail closed with an explicit `=== "assistant"` check — never `?? "assistant"`, which fails open and lets user-echoed text (parts can arrive with evicted/mis… (fonte: P1-072)
- When reading session identity from opencode SDK events, do derive it from the part payload (`part.sessionID`, guaranteed by installed types like `TextPart`) and fall back to top-level `properties.sessionID` — top-level-only reads silently… (fonte: P1-072)
- When adding logic to `apps/daemon/src/index.ts` (runs `main()` on import, so inline stream-wiring has zero unit coverage), do extract pure helpers into their own module (pattern: `preview.ts`, `localws.ts`) and pin semantics there with tes… (fonte: P1-072)
- When simulating a first boot in a session-keyed harness (OCR_DESKTOP_SESSION), derive a never-used session name from the date (digits-only, path-safe) so the keeper spawns fresh and hermeticEnv mints a virgin userData — and if `open` repor… (fonte: P1-071)
- When capturing an app's untouched first screen, make the launch command itself take the screenshot (open with a shot argument) — a standalone shot before the keeper/process is up fails. (fonte: P1-071)
- When adding a judgment rule to the constitution, update docs/CONSTITUTION.md and the `CONSTITUTION` prompt constant in lockstep, pin both in unit tests, and prefix the commit body with `constitution-change:` — touching only the docs file i… (fonte: P1-071)
- When a production origin (PWA/mobile endpoint) is at stake, never back it with a dev server — serve the built `dist` with a dependency-free static server under a supervisor (launchd + KeepAlive) so it survives reboots and shell death (fonte: P2-075)
- When a failure is only visible on one client (phone sees white screen because desktop loads the bundle from disk), add a `/healthz` probe plus a dashboard event/chip so the downtime surfaces within minutes instead of hours (fonte: P2-075)
- When adding a new service to the deploy pipeline that may not exist on every host, make its restart best-effort (`allowFail: true`) and emit a deploy event instead of failing — KeepAlive plus per-request static reads mean a skipped kicksta… (fonte: P2-075)
- When splitting failure handling into infra vs merit paths, bias the classifier's false-positive direction toward infra (the retry is free: no attempt burned, no block can land) and pin merit negatives (gatekeeper rejection, empty diff, rev… (fonte: P1-074)
- When a failure detail must be classified downstream, emit a unique tagged marker at the failure site (e.g. `[infra] builder timed out without output`) instead of inferring from log tails — and only for the unambiguous case: a timeout with… (fonte: P1-074)
- When adding an optional counter to PilotState, backfill it in loadState in both branches (same-day and date-rollover) so legacy state.json files never yield undefined/NaN — reuse the existing `merges` field pattern. (fonte: P1-074)
- When adding another read of the daemon state file (or any 0600 credential file), resolve the path through the existing shared helper (stateFilePath()/stateFile()) instead of re-deriving `join(homedir(), ...)`, so test-only env overrides (O… (fonte: P1-070)
- When a boot-time mode decision replaces a legacy UI flow (local auto-connect vs QR ceremony), stamp the mode into every degraded/intermediate state (daemon-down, reconnecting) and gate legacy fetches behind it, so a failed poll can never r… (fonte: P1-070)
- When an async bridge value (pairing state) feeds a mount-time auto-connect, mirror it into a sync ref updated in the same `.then()` and re-run the connect attempt when a state with the expected mode lands, because the effect's first run ra… (fonte: P1-070)
- When building LLM prompts for a provider with prefix caching, do assemble a byte-identical stable prefix first (role, rules, constitution, output contract using generic `<TASK-ID>` placeholders instead of interpolated ids) and the variable… (fonte: P1-077)
- When extending an existing totals-only token/cost store into a multi-field breakdown, do make the row-level query the core and reimplement the old totals function as a thin wrapper, accept both rows and legacy numeric injectors at the fold… (fonte: P1-077)
- When adding a new optional field to persisted state or a derived metric, do normalize/backfill it in BOTH `loadState` and `normalizePilotState` (legacy files must become `{}`, never crash) and guard derived ratios with a zero-denominator c… (fonte: P1-077)
- When fixing horizontal overflow in a flex layout, do apply `min-width: 0` (and `max-width: 100%`) on EVERY flex item along the row-axis chain down to the bubble — flex items default to `min-width: auto`, so a single wide child (long code l… (fonte: P1-080)
- When making pre/code blocks scroll horizontally, do pair `overflow-x: auto` with `max-width: 100%` + `min-width: 0` on the block itself — `overflow-x: auto` alone lets the block grow to its content width, so the page scrolls instead of the… (fonte: P1-080)
- When asserting layout geometry in desktop-flow tests, do freeze CSS animations/transitions (inject a `* { animation: none !important; transition: none !important }` style) before reading `getBoundingClientRect` — entrance animations like a… (fonte: P1-080)
- When rendering actionable UI (approval cards, confirm dialogs) from a WS event buffer, always reconcile against the server's current state (its pending list endpoint) as source of truth and use events only to trigger debounced re-fetches —… (fonte: P1-082)
- When the same logical request can emit multiple events, dedupe by its stable ID in a Map with last-occurrence-wins, normalizing legacy field variants (id vs permissionID) before keying, or N duplicates render N identical cards (fonte: P1-082)
- When a POST can hit a resource resolved concurrently (AutoMode, other device), map the 404 to a friendly "already resolved" inline note that also cleans local state instead of surfacing the raw error (fonte: P1-082)
- When writing orphan/cleanup killers that match processes by argv markers, make the kill decision symmetric — require an env second factor (e.g. test marker keys or HOME under `<tmpdir>/ocr-*`) for EVERY marker, never argv substring alone,… (fonte: P1-081)
- When parsing darwin `ps -wwE` output, argv and env share one line, so any argv token shaped `KEY=VALUE` (or a temp path in the arguments) gets absorbed as an env entry — read the plain command first and parse only the env suffix after it,… (fonte: P1-081)
- When booting test servers, bind port 0 and resolve the port from `AddressInfo.port` instead of fixed ports (zombies from previous runs hold them), and only trust a readiness probe while the child is still alive — a signal death (`signalCod… (fonte: P1-081)
- When a component is mounted persistently across entity switches (ChatView is deliberately not keyed by sessionId), do not assume its useState resets — restore state keyed by the active id via useState(() => get(sessionId)) plus a [sessionI… (fonte: P1-088)
- When an async callback (voice transcription, video note) mutates state that survives navigation, do pin the origin id at action start and write through the per-id store, touching the visible input only when origin === current id — never ca… (fonte: P1-088)
- When a state is easy for future edits to corrupt (user input living near stream/event effects), do declare a single-writer invariant in a comment at the declaration and route every write through wrappers so rg 'setInput\(' lists only the d… (fonte: P1-088)
- When two independent sources populate the same UI list (history fetch replaces, live event stream appends), do all merges keyed by a stable per-item id (messageID) via a pure idempotent merge function instead of comparing the last item's t… (fonte: P1-089)
- When a component holds cross-session transient state (streaming tail refs/text) above a `[sessionId]` switch effect, do clear it inside that effect on every session change, or an idle-finalize racing the switch appends the previous session… (fonte: P1-089)
- When a hermetic test relaunches an app session dir, do keep the session id short (macOS AF_UNIX bind paths truncate at 104 chars — a truncated keeper.sock fails EADDRINUSE forever on relaunch) and wait for the keeper PROCESS to exit after… (fonte: P1-089)
- When backfilling a slot scheduler from multiple call sites (main loop + worker `finally`), keep the pick-and-start path fully synchronous (sync exec, no `await`) so event-loop atomicity guarantees each task is picked exactly once — an asyn… (fonte: P1-099)
- When removing a global serialization lock for parallelism, first confirm every resource it protected has become hermetic/instance-scoped (ephemeral ports, unique session ids, per-workspace state) — the lock was a symptom; delete it only wh… (fonte: P1-099)
- When running logic inside a `finally` (eager-fill, cleanup, bookkeeping), wrap it best-effort: try/catch with a warn log and skip on failure (e.g. queue read failure) so the hook never throws out of `finally` and the main loop's next cycle… (fonte: P1-099)
- When emitting one notification per real write from fs.watch, debounce each changed path until it settles and dedupe by a size:mtime fingerprint — chunked writes, edit-in-place edits and atomic renames all fire multiple/coalesced events — a… (fonte: P2-090)
- When validating paths derived from filesystem events, resolve them against the watched root, reject anything that escapes it, and lstat (never stat) the target so a symlink planted in the tree is not followed or announced as a real file (fonte: P2-090)
- When adding auto-open behavior to a pane, keep auto state separate from the manual pick (render `manual ?? auto`), track dismissed files so a closed pane is never re-opened, reset pending auto state on session switch, and declare the consu… (fonte: P2-090)
- When an effect must re-fire for the same logical value (e.g. re-opening the same artifact from a picker), do pass a fresh object identity (clone `{ ...meta }`) into the state setter — effect deps compare by reference, so reusing the old ob… (fonte: P2-091)
- When routing a selection from one surface (global Artifacts list) into another view's behavior, do funnel it through the destination's existing state path (its manual pick) instead of adding a parallel mechanism, so established invariants… (fonte: P2-091)
- When calling Electron `<webview>.loadURL()` inside an effect, do guard it in try/catch with a `src`-attribute fallback — after a remount (mobile⇄desk flip) the fresh webview isn't dom-ready yet and loadURL throws; the src attribute carries… (fonte: P2-091)
- When styling Electron `<webview>` elements, keep the host as a flex container: the shadow root lays its guest iframe with `flex: 1 1 auto` and no intrinsic height, so forcing `display: block` on the host collapses the guest to its 150px de… (fonte: P2-092)
- When a container can change size without a remount (maximize toggle, split resize, hidden⇄shown flips), attach a ResizeObserver to the frame and re-assert explicit px width/height on the guest view, skipping updates when the box is under 1… (fonte: P2-092)
- When asserting "content fills the pane" in flow tests, measure both the element bounding box and the guest viewport via executeJavaScript innerWidth/innerHeight, and re-measure after a width change — a correct element box can coexist with… (fonte: P2-092)
- When a mode flag suppresses UI affordances (e.g. AutoMode hiding approval cards), ensure every server-side failure path in that mode emits a client-visible event (not just a `log warn`) so the operator gets a manual fallback instead of a s… (fonte: P1-093)
- When event-sourced UI state uses "last occurrence wins" dedupe, wire new failure events so a later success event clears the failure flag and other-session events are ignored entirely — otherwise recovery and cross-session isolation break s… (fonte: P1-093)
- When adding retries to daemon-side best-effort calls, bound them tightly (2 attempts, ~500ms) and prove it in E2E with an always-rejecting fake backend plus an exact POST-count assertion — this catches unbounded retry loops without slowing… (fonte: P1-093)
- When classifying failure kinds from text that embeds logs or reviewer findings, do classify from a structured field set at the failure's producer site instead of substring-scanning detail text — a merit finding citing "ECONNREFUSED" must n… (fonte: P1-094)
- When reasoning that a classifier's false positive is harmless because "the retry is free", do check whether it bypasses termination mechanisms (attempt budgets, circuit breakers, fever windows) — spared accounting turns a benign FP into in… (fonte: P1-094)
- When a text marker exists only for observability (e.g. `[infra]` in detail), do keep it for log greppability but never let control flow depend on it — propagate an explicit typed flag through intermediate result objects (RunResult → Pipeli… (fonte: P1-094)
- When a periodic job's trigger combines a fixed wall-clock hour with a contested condition (e.g. all slots idle), do verify reachability with real timestamps (`git log`) before trusting the code — if pipelines routinely span that hour, repl… (fonte: P1-095)
- When a scheduled pass can be silently missed, do record an observable "skipped" event (deduped per day in state + emitted to the dashboard) and require run evidence (commit prefix in git log or event line in events.jsonl) before accepting… (fonte: P1-095)
- When extending a state schema that rolls over daily, do add new fields to the preserved `shared` block of the loader with tolerant parsing (garbage → null, non-finite → undefined), and early-return in one-shot (`--once`) mode before spawni… (fonte: P1-095)
- When a function pairs an idempotency "already recorded → no-op" short-circuit with a cleanup/invalidation branch over the same state key, order the freshness check BEFORE the short-circuit — the record can outlive the condition it recorded… (fonte: P2-100)
- When a side effect must accompany a state checkpoint (event append + "recorded" marker), emit the side effect FIRST and checkpoint only after it succeeds — checkpoint-then-emit turns a transient append failure into a permanently dead signa… (fonte: P2-100)
- When the spec pins a numeric contract (e.g. a 10s fetch timeout), export the constant and assert its value in tests — `signal instanceof AbortSignal` passes for any duration, leaving the spec'd number unverified. (fonte: P2-100)
- When a per-session value (id, path, timestamp) would land in a system prompt shared by all sessions, split stable from variable: keep the `system` block byte-identical across sessions so the provider prefix-cache survives, and ride the per… (fonte: P1-096)
- When injecting one-shot per-session data into message bodies, pair an in-memory "already told" Set with marker-based idempotence inside the helper, and fail-open on malformed bodies (no `parts` array, no text part) so a later turn retries… (fonte: P1-096)
- When pinning prompt-builder output in tests, assert the invariant itself (two sessions produce `system === system`, no per-session id/dir anywhere in the block) rather than concrete per-session values, or the test keeps blessing the bug it… (fonte: P1-096)
- When serving unbounded user files into RAM (especially base64 over a tunnel, +33% bloat), check `st.size` via lstat before `readFileSync` and refuse with a distinct failure reason (413) instead of a generic null/404 — and hide UI actions l… (fonte: P2-097)
- When sandboxing an iframe that renders a blob you created in the same page, use `sandbox="allow-same-origin"` — a bare `sandbox` attribute strips same-origin and breaks the browser's built-in PDF viewer, while still blocking scripts/forms/… (fonte: P2-097)
- When a service worker caches fetch responses, only `res.status === 200` may enter the cache (wrap the async `caches.put` in `event.waitUntil`), and version the cache name on every SW change — a constant cache name plus caching errors/redir… (fonte: P2-097)
- When an idempotent installer regenerates a launchd plist, do recover every previously-set env var from the existing plist when its env override is absent — recovering only the primary URL (RELAY_URL) while silently dropping secondary ones… (fonte: P2-098)
- When adding a fallback source to feed/URL resolution, do gate it to the packaged default only — an explicitly configured override (e.g. OCR_UPDATE_FEED) must surface feed-unreachable instead of silently producing an outbound request to a t… (fonte: P2-098)
- When packaging a DMG with electron-builder on dmg-builder >= 1.2.5, do omit the relative `path: "*.app"` glob in dmg contents (FileNotFoundError at dmg time) and set `name: "<App Name>.app"` so the built .app is sourced via its absolute ap… (fonte: P2-098)
- When giving an agent bash/edit permissions in a scratch workspace, never symlink the production node_modules into it — cpSync the full tree (as done for dist dirs) so a shared read-write dependency tree can't turn a tampered bin/postinstal… (fonte: P3-101)
- When interpolating values into execSync/shell commands, route every one through the existing quoting helper (shq) and only interpolate values that pass a strict format check (e.g., /^[0-9a-f]{40}$/ for shas) — raw `${...}` template strings… (fonte: P3-101)
- When a long-lived daemon must pick up new code, don't rely on deploy-time self-reload alone (processes spawned before the fix keep the dead path in memory) — pin boot HEAD at startup and at a fully idle moment (no slots running, no deploy… (fonte: P3-101)
- When building LLM agent prompts that should hit the provider prefix cache, assemble them stable-first — role/rules/contract byte-identical, all per-run variables (task text, ids, workspace paths, session env, lessons) after the last stable… (fonte: P1-078)
- When deferring a scheduled pick through a wait window (stagger/backoff), emit a distinct "staged" phase instead of announcing "started", and after the wait re-check only gates that can genuinely flip (frozen/audit) — never re-count budgets… (fonte: P1-078)
- When parallel workers share an account-level provider cache, route same-shape work to the worker that last ran that shape within the cache TTL and stagger simultaneous starts (~20s) so the first cache-write completes; record per-worker cac… (fonte: P1-078)
- When referencing a new CSS custom property in a stylesheet, grep the token file for its definition first — an undefined `var()` fails silently at computed-value time (declaration dropped, no build/test error), so hover affordances like bor… (fonte: P3-083)
- When adding reduced-motion support, remember the CSS media query does not cover JS scrolls: gate every `scrollIntoView({behavior:"smooth"})` behind `matchMedia("(prefers-reduced-motion: reduce)")` and add `scroll-behavior: auto !important`… (fonte: P3-083)
- When extracting literals into design tokens, sweep the whole file for pre-existing inline duplicates of the same category (font stacks, durations, grays) and replace them in the same diff — converting only the rules you touch leaves one-of… (fonte: P3-083)
- When computing calendar-day boundaries (Today/Yesterday buckets), do step via date fields (`setDate(getDate() - 1)` or `new Date(y, m, d)`) instead of subtracting 86_400_000 ms, because DST-change days have 23/25 hours and fixed-offset mat… (fonte: P3-084)
- When classifying timestamps into recency buckets, do route unknown/garbage values (0, NaN, negative) into the oldest bucket — never into a recent one — since missing data can never be proven fresh and must not fake recency (fonte: P3-084)
- When building UI that e2e gates must verify across locales, do attach stable data-* hooks (e.g. data-group="today") to generated elements instead of asserting on translated label text (fonte: P3-084)
- When ending/freezing a streaming state on a generic event envelope (e.g. `message.part.updated`), gate the transition on the payload discriminator (`part.type === "text"`) — tool/file/step parts stream mid-turn and must not act as if the a… (fonte: P3-085)
- When custom follow-tail autoscroll owns scrolling while a live bubble grows, set `overflow-anchor: none` on the scroll container — Chromium's native scroll anchoring micro-adjusts scrollTop and fights the "at bottom" heuristic (fonte: P3-085)
- When accumulating streaming-derived state (reasoning, live text) in a session-scoped view, keep it in refs cleared on every sessionId switch and fold events through a pure reducer that drops foreign sessionIDs — a late event otherwise blee… (fonte: P3-085)
- When extracting testable logic from a React component (geometry, labels, formatting), do move the pure functions into a lib file so unit tests hold the contract without a renderer, and register the new test script in the `test:unit` chain… (fonte: P3-086)
- When implementing an auto-grow textarea, do read line-height/padding from `getComputedStyle` instead of hardcoding and floor the max-lines cap (`Math.floor(lh*maxLines + padY)`) so fractional computed line-heights can't leave the box 0.2px… (fonte: P3-086)
- When replacing a native `<select>` with a custom inline dropdown, do rebuild the full a11y/discipline layer it loses — `aria-expanded`/`aria-haspopup`/`role="listbox"`/`aria-selected`, stable `data-*` hooks so the desktop-flow gate can tar… (fonte: P3-086)
- When keeping a React component mounted to play a CSS exit animation (exit-phase hook or `.out` class hold), add `pointer-events: none` to the exiting/invisible variant — a fading scrim or sliding panel still intercepts clicks aimed at the… (fonte: P3-087)
- When an exit animation needs content to render during the hold, keep a "last value" ref (`if (value) ref.current = value`) so the unmounting tree shows the previous item instead of null, and short-circuit the phase timer to "closed" under… (fonte: P3-087)
- When a test beat probes computed `animation-name` in a shared e2e file, remove freeze styles injected by earlier beats (`#p1-080-noanim`-style `animation: none !important`) before reading computed styles, and emulate reduced-motion through… (fonte: P3-087)
- When two apps resolve the same provider/model catalog (pilot `contextWindowFor` vs daemon `buildWindowMap`), pin a test asserting they agree on EVERY key shape the session reports (bare id, `provider/model`, entry `id` alias, unknown) — on… (fonte: P1-079)
- When a gauge/metric effect refetches on a recurring event (agent idle, `idleCount` in deps), wipe the displayed sample only on identity change (sessionId) and keep the previous value until the fresh one lands — otherwise the panel unmounts… (fonte: P1-079)
- When a checkpoint recycles a builder session, drop the dead session id from resume state in the same transition (never advertise "fresh start" and "resume via -s" together) and fail open — unmeasurable pressure or unusable recap keeps the… (fonte: P1-079)
- When a reviewer's REQUEST_CHANGES has all findings mechanically dropped as unverifiable, treat the evidence as unproven-either-way and fail closed (escalate or keep the rejection) — never let "zero verified findings" degenerate into an eff… (fonte: P1-073)
- When a predicate is gated by round number, split orthogonal signals into separate conditions before the round check — a single `round !== 1 return false` silently disables signals (like all-unverifiable findings) that are dangerous in any… (fonte: P1-073)
- When fail-closing without an arbiter, append an actionable instruction to the rejection feedback (restate the concern citing verifiable path:line evidence from the diff) so fail-closed doesn't become an unresolvable builder loop (fonte: P1-073)
- When a daily-guarded flow (nightly pass, cron-like maintenance) must run once per day, do stamp the "last run" state guard only AFTER the work completes — pre-stamping turns any mid-run process exit/reload into a silently lost day (fonte: P1-075)
- When prompting an agent to distill lessons/summaries of a code change, do feed it only the artifact (the diff) — reviewer findings and gate tails in the prompt bias the output toward harness/process lessons instead of product-code ones (fonte: P1-075)
- When deduping lines by semantic similarity, do gate the threshold (e.g. Jaccard >= 0.6) behind a minimum token count and fall back to exact-key matching for short lines — similarity over small token sets is noisy (fonte: P1-075)
- When success depends on a deferred remote action (e.g. `gh pr merge --auto` only queues the squash until checks pass), do not report success at arming time — poll the external system until the end state is confirmed (state MERGED AND head… (fonte: P1-076)
- When a deterministic edit is retried against concurrent or deferred landings, model the edit as tri-state applied/noop/missing and converge "desired state already present" (noop) as success — a retry after a queued merge finally lands must… (fonte: P1-076)
- When retries rewind/rebase a shared branch before reapplying edits, re-derive everything that depends on file content (task ids, insertions, counts) inside the apply callback from the freshly synced copy — ids or pass results computed befo… (fonte: P1-076)
- When comparing agent-pasted command output against a re-run (evidence verification), capture stderr on success too (spawnSync + separate pipe buffers, not execSync): vite/tsc/esbuild print warnings on stderr, and dropping them makes honest… (fonte: P1-101)
- When deriving phase metrics from open/close events, put informational sub-events (retries, mid-phase failures) in an aux set that never opens a tracked phase, or they clobber the real opener and silently break the open→close pairing (fonte: P1-101)
- When adding flaky-retry to a cached gate step, evict the red cache entry, run exactly one more time, classify fail→pass as flaky deterministically (no LLM), and never retry pasted-output divergence — only command-execution failures (fonte: P1-101)
- When parsing structured verdict output from LLM reviewers, parse finding bullets ONLY under the marker that semantically carries them (REQUEST_CHANGES) and treat bullets after APPROVE as rationale — LLMs routinely append justification afte… (fonte: P1-102)
- When extracting quoted spans with a length floor, split on the delimiter and pair sequentially (odd indices) instead of putting the floor inside the regex quantifier — a short quoted span between two long ones mis-pairs the regex and swall… (fonte: P1-102)
- When mechanically verifying citations, check verbatim evidence (quote present in the diff) BEFORE positional evidence (path:line resolution) so real findings with imperfect line cites survive, but exclude path-shaped quotes from the verbat… (fonte: P1-102)
- When a thrown exception in a pipeline worker never produced a merit verdict, record it as infra evidence only (infraFails + doctor wake) and never touch the per-task attempt counter or circuit breaker — a crash loop must burn no attempts a… (fonte: P1-104)
- When a long-lived process self-reloads after deploy, never `process.exit` with slots mid-pipeline: hold new scheduler picks, poll until the running-slot counter drains to 0 (pipelines end by themselves via task timeout), and feed the watch… (fonte: P1-104)
- When a fix removes a dangerous call site (tripCircuitBreaker in a catch, immediate exit on reload), add source-level wiring-pin tests (readFileSync + includes on the call site) so a refactor cannot silently re-enable the old path (fonte: P1-104)
- When a machine-parsed review/verdict contract depends on structured severity tags rather than the verdict marker alone, make untagged/ambiguous input fail closed as the most severe class (e.g. `isBlockingFinding` treats anything without `[… (fonte: P1-103)
- When replacing an escalation or rejection trigger (e.g. round-1 divergence → repeated findings between rounds), preserve pre-existing fail-closed paths: all-unverifiable findings must still escalate in any round and a nit-only REQUEST_CHAN… (fonte: P1-103)
- When matching "same concern" across LLM outputs from different rounds, key on stable structural signals first (cited file paths, lowercased, order-insensitive) and only fall back to normalized text (strip severity tags, bullet markers, cas… (fonte: P1-103)
- When a pipeline step runs `git clean` between agent dispatches (e.g. landMetaCommit in metapush.ts:239), re-write untracked generated configs (workspace opencode.json via writeSandboxConfig) before the next headless agent runs — otherwise… (fonte: P2-105)
- When an automated review consumes file evidence, mandate stable per-step file names, capture the closest truthful state under that exact name even when the step fails, and only trust files that pass existsSync — an honest empty state is ev… (fonte: P2-105)
- When generating BACKLOG.md task lines programmatically, keep the trailing `(area: ...)` tag as the last thing on the line — the scheduler's AREA_RE (backlog.ts:28) anchors at line end and mis-schedules otherwise (fonte: P2-105)
