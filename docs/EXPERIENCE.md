# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When UI copy promises an automatic behavior ("connects by itself"), do render that attempt live (status dot + phase copy + retry) instead of an idle manual form — a screen contradicting its own intro is a product bug, not a styling one (fonte: P3-332)
- When an automatic mode (local auto-connect) can hide the only manual path, do force the mode off for explicit user intent (`localMode={false}` for "add machine" — entering a remote code IS the manual ceremony) so automation never swallows… (fonte: P3-332)
- When a retry button backs an automatic attempt, do re-arm the automation itself (`tryAutoPair()`), not just reset the phase — retry that only flips UI state leaves the promised auto-flow dead on screens with no manual fallback (fonte: P3-332)
- When centering content inside a header implemented as a flex row (e.g. a shared `space-between` `.screen > header`), do drop the flex (`display: block`) together with `text-align: center` — flex items ignore `text-align`, so adding only th… (fonte: P3-330)
- When overriding a shared base class for one screen, do scope the override to the variant selector (`.screen.degraded > header`, not `.screen > header`) and assert no sibling component's className matches it — sibling screens reusing the ba… (fonte: P3-330)
- When a fix lands purely in CSS with no runtime behavior to exercise, do add a textual unit-test assertion on the rule (expected properties present, selector still scoped) — it is the cheapest guard against a later refactor silently droppin… (fonte: P3-330)
- When a wait or error state depends on an external process (local daemon/agent, network), do name that dependency in the UI copy — an indefinite spinner whose cause is invisible reads as broken, not as "waiting" (fonte: P3-329)
- When an automatic flow can get stuck, do put the labeled manual escape on the stuck screen itself — an alternative hidden behind an unlabeled link one screen earlier is unreachable for a first-time user (fonte: P3-329)
- When a fix adds new user-facing copy, do add a unit assertion that the keys resolve in every supported locale (result ≠ raw key) — a missed translation renders the raw key string on the exact screen being fixed (fonte: P3-329)
- When gating UI on async-loaded state (e.g. `localMode={pairingState?.mode === "local"}`), the flag evaluates false while the state is still loading and flashes the wrong flow — make a positive verdict sticky for the session (a `nextShellLo… (fonte: P3-331)
- When an automatic mode (auto-connect/local pairing) coexists with a manual ceremony (paste/scan code), explicit manual intent must force the automatic mode off (`localMode={false}` / `localMode && !pairManual`) and every wait/error state n… (fonte: P3-331)
- When re-arming auto-connect from a poll-driven effect after an error, use a time-based backoff (e.g. 15s, never a hard cap, never per-poll) to avoid hammering a half-up daemon, and make Retry reconnect any stored pairing verbatim before re… (fonte: P3-331)
- When a verdict computed in the desktop main process must reach the UI, extend the existing payload additively (optional field in preload.ts + mirrored tolerant type in the web-side interface, no new channel/IPC) — otherwise the data dies i… (fonte: P2-324)
- When mapping an external status object to user-facing copy, write a pure fail-closed function that validates types, accepts only the documented closed set of states, returns null (silence) for absent/malformed/unknown/non-textual inputs, a… (fonte: P2-324)
- When several notices share one UI band, encode precedence in a single explicit expression (e.g. `exitNotice ?? wedgeNotice`) with the stronger story winning, and lock in module hygiene with source-level unit tests asserting purity (no Reac… (fonte: P2-324)
- When a fail-closed guard asserts the PRESENCE of an expected artifact type (`--expect dmg,zip`), wire that expectation only into the job that actually builds the artifact (release.yml packaging jobs) and run the same collector without it i… (fonte: P2-325)
- When classifying a packaging output dir by file suffix, skip the unpacked payloads during the walk (`.app` bundles, `*-unpacked/` dirs) — they contain the app's own `.exe` binary, which a suffix classifier would otherwise measure against t… (fonte: P2-325)
- When setting a size budget, derive the ceiling from a real measured maximum plus documented slack (~40%, written in the comment beside the table entry) and make missing/zero/negative/non-numeric measurements distinct failures — a broken st… (fonte: P2-325)
- When checking if a file is executable on win32, never trust `statSync(path).mode & 0o111` — libuv never sets exec bits in st_mode on Windows — do detect by case-insensitive `.exe` suffix and reserve exec-bit checks for non-win32 platforms (fonte: P3-343)
- When adding best-effort diagnostics (screenshots, dumps) to CI boot smokes, do make them strictly fail-open: env-gated (e.g. `OCR_PACKAGED_BOOT_SHOT`), try/catch with a one-line log, uploaded with `if: always()` + `if-no-files-found: ignor… (fonte: P3-343)
- When a rule depends on the host platform, do extract it into a pure function taking `platform` as an explicit parameter (like `isExecutableEntry(name, mode, platform)`) instead of reading `process.platform` inline, so unit tests can exerci… (fonte: P3-343)
- When auto-resolving git merge conflicts programmatically, keep the resolver a pure function (marker text in, plan out, all I/O in the caller) and fail closed on anything unprovable: malformed/nested markers, marker-free files (binary or de… (fonte: P3-341)
- When a repair pushes a new PR head mid-merge, re-probe gated on `headRefOid === expectedSha` before arming the merge — a snapshot missing the sha field stays `pending` (honest infra timeout, free retry), so a green CI verdict inherited fro… (fonte: P3-341)
- When feeding git-reported paths into shell commands, read them NUL-delimited (`git diff -z --name-only`) and escape with single-quote shell quoting — JSON.stringify is NOT shell quoting and leaves `$()`/backticks active inside double quote… (fonte: P3-341)
- When rejecting or revoking a client on a multiplexed socket (the single relay WebSocket shared by all paired clients), never close the shared transport — drop that pub's frames or close only its logical/local session, or one zombie client… (fonte: P3-344)
- When identifying client keys in logs or audit records, don't slice the base64/DER prefix — the leading SPKI bytes are a constant ASN.1 header identical for all P-256 keys; hash the decoded DER bytes (sha-256, first 16 hex chars) for a fing… (fonte: P3-344)
- When a rejected/untrusted client can auto-reconnect in a loop, throttle per-identity warn+audit emissions via a bounded in-memory map (pure decision function with injected clock) while a metrics counter ticks every occurrence, so the loop… (fonte: P3-344)
- When a fixed-size (resize: none) input holds unbreakable strings like URIs, add `overflow-wrap: anywhere` to the rule — with no resize grip or row growth a long token otherwise overflows the box (fonte: P3-369)
- When an input/textarea receives machine-generated content (URIs, tokens, codes), set `spellCheck={false}`, `autoCapitalize="off"`, `autoCorrect="off"` and `autoComplete="off"` — keyboard autocorrect silently mangles pasted values (fonte: P3-369)
- When tempted to add a `window.*` debug hook for flow autopsies, expose nothing: any page script can read it (seq counters, pending request paths), so diagnose via the daemon log and remove leftover hooks from the client (fonte: P3-369)
- When a CI test exercises a restart/reconnect race that loses a frame roughly once in N runs, mirror the local fail-flaky-once policy in the workflow — `set -euo pipefail` + `cmd || { echo "why retrying"; cmd }` under a `timeout-minutes` ca… (fonte: P3-372)
- When adding per-second elapsed/attempt feedback to a status line that carries `role="status"`, wrap the ticking segment in `aria-hidden` (and use tabular numerals against per-second width jitter) so screen readers are not re-announced ever… (fonte: P3-372)
- When a component prop encodes a platform-wide policy (e.g. `preferPaste` = desktop paste-first) instead of a per-feature choice, do add a structural test asserting EVERY call site of that component passes the prop — one omitted call site s… (fonte: P3-366)
- When a toast/overlay must be dismissible across parent branch-switch remounts, do keep the dismissal in parent state zeroing the same timestamp that seeds the trigger — a component-local flag is re-seeded on remount and the dismissed UI re… (fonte: P3-366)
- When asserting a CSS layout fix in source (not in a browser), test the property COMBINATION — e.g. `text-align: center` AND the absence of `display: flex`/`justify-content` — because flex items ignore text-align, so a single-property asser… (fonte: P3-374)
- When a control must sit relative to others (step indicator under wordmark, skip inside the action row), pin the markup ORDER with `indexOf` comparisons between stable class-name markers, not just presence checks — order is what layout bugs… (fonte: P3-374)
- When extracting a CSS rule for assertions, slice from the selector index to the next `}` and check the slice is found (`indexOf > 0`/`> -1`) before asserting properties, so a missing rule fails the test instead of passing vacuously (fonte: P3-374)
- When asserting DOM state through the desktop-flow `ipc` harness, return probes as `join('|')` lists (booleans + `|`) instead of `JSON.stringify` — the harness JSON-wraps the eval result, so in-page JSON comes back with `\"` escapes that no… (fonte: P3-367)
- When a second component needs to read/write a preference already persisted by a shared lib (theme, lang, font), do import its helpers (`readTheme`/`applyTheme`/`THEME_KEY` from `lib/theme`) instead of re-implementing a local localStorage r… (fonte: P3-368)
- When user-facing copy enumerates capabilities ("Idioma e tema ficam nesta máquina"), do verify each enumerated item has a working control in that same view before merging — or scope the copy down to what ships; promise/feature mismatch is… (fonte: P3-368)
- When exposing daemon-backed views pre-connection, do wire them to a no-op request/transport and humanize their error paths, or the newly reachable panes leak raw "not connected" throws instead of degrading gracefully (fonte: P3-365)
- When copy claims a feature is locked, do derive that copy from the same state that controls reachability (e.g. a `panesReachable` prop), because a lock glyph beside a working rail button is the self-contradicting-screen bug class (fonte: P3-365)
- When an arbiter/judge LLM approves a round in which every adversarial finding was mechanically discarded as unverifiable, require proof of contact with the code — at least one cited `path:line` or quoted snippet that survives `verifyFindin… (fonte: P3-355)
- When a renderer lib touches localStorage, do keep it pure — inject the storage as a parameter, wrap every read/write in try/catch, and sanitize (trim, strip NULs, length-cap) on both read and write — so a poisoned or unavailable store retu… (fonte: P3-360)
- When a textarea and action rows live inside a CSS grid panel targeted at phone widths, do set `min-width: 0` on the grid children — grid items default to min-content sizing and the hint/save rows clip past the panel border instead of shrin… (fonte: P3-360)
- When a Node script that spawned an Electron app (or any child process tree) prints its verdict but hangs in CI, do not rely on `process.exitCode` or a parent-only SIGKILL — capture the child pid right after launch and end with a determinis… (fonte: P3-348)
- When a watchdog timer guards a smoke script's deadline, keep it armed through the post-verdict path (never clearTimeout at the success point) and `unref()` it so it is never itself the handle holding the loop — a hang after the verdict is… (fonte: P3-348)
- When deriving an exit code dynamically from a verdict, coerce non-integer/undefined/NaN codes to 1 and run best-effort cleanup (kill) before a single final `exit` call, so a throwing kill or an early throw never becomes an accidental succe… (fonte: P3-348)
- When a periodic maintenance job is gated on organic idleness (N hours with no slots busy) on a fleet that runs near saturation, do reserve the window instead — hold new work so slots drain into the job, cap the wait (after which it runs wi… (fonte: P3-356)
- When a loop tick overwrites a rolling anchor (windowSince) from a per-tick decision, do capture the previous tick's value before the update if later consumers on the same cycle (a skip record written after the window closed) need to report… (fonte: P3-356)
- When a client API answers HTTP 200 with the real outcome in the body (e.g. `{delivered}`), do check the body verdict and set the client timeout above the server's measured worst-case (a full opencode turn >120s) — an unconditional 200 plus… (fonte: P3-357)
- When a delivery attempt ends with an UNKNOWN outcome (timeout/abort while the server may still deliver), do drop it instead of queueing a replay — park only definitive failures (HTTP status, socket, `delivered=false`) so retries never dupl… (fonte: P3-357)
- When a file-backed JSONL queue is read-rewritten from concurrent async paths (a park landing mid-flush), do serialize every mutation behind one promise-chain lock or the flush's final rewrite erases the fresh entry and overlapping flushes… (fonte: P3-357)
- When an e2e probe must assert an error state is absent, give the error element a dedicated class (e.g. `.artifacts-error`) to query as a stable hook, and strip escaped quotes from the IPC stdout before regex-matching (fonte: P3-327)
- When parameterizing a git base branch, thread the config through every `origin/<ref>` consumer in the same change — queue reads, workspace sync, PR `--base`, deploy history walks, doctor resets — and keep the local branch canonical (`main`… (fonte: P3-358)
- When a shared commit/push helper gains a new option (e.g. `seedSkeleton`, `baseBranch`), grep and update ALL its callers in the same task — aux agents that duplicate the call pattern (researcher, refills, blockAndPush) are the ones silentl… (fonte: P3-358)
- When a dispatcher guard can only be "tested" by grepping source text, extract the pre-conditions into a pure decision function (like `deploySkipReason`) so the battery proves the refusal behaviorally instead of asserting on the source (fonte: P3-358)
- When a push feeds both a passive indicator (badge) and a one-shot interruption (native notification), do sanitize stricter for the interruption — a malformed count that floors for the badge (fractional, negative, non-numeric) must fail clo… (fonte: P2-326)
- When decision logic lives inside an Electron main-process handler, do extract it as a pure module (no electron, no node:fs, no timers, no I/O) returning a closed-set verdict, and prove purity and wiring order with source-reading tests that… (fonte: P2-326)
- When a feature must never show a native artifact on the operator's screen during hermetic test sessions, do consult the session rule FIRST before constructing the object (write the order contract into the module header) and have e2e assert… (fonte: P2-326)
- When adding a confirmation/status string that renders in a card alongside an existing hint, re-read all copy visible in that post-action state together and give each line a distinct job (promise vs. consequence vs. action) — paraphrased du… (fonte: P3-388)
- When changing the meaning of an i18n key, update every locale in `dict` in the same commit with semantically equivalent copy, and note the pairing rationale (e.g. "hint carries the promise, confirmation carries the meaning") in a comment s… (fonte: P3-388)
- When touching the set of panes reachable at the pairing gate (GATE_SHELL_PANES / the gate rail in App.tsx), update the PaneMap.tsx rows in the same change — the map is a hand-maintained mirror of gate behavior and silently under-reports wh… (fonte: P3-389)
- When changing PaneMap's row list, update the structural assertions pinned in scripts/unit.test.ts — both the rowKeys/descKeys arrays and the literal occurrence counts (`locked: true` === 1, `locked: !reachable` === N) — the battery fails o… (fonte: P3-389)
- When adding a user-facing UI string, add the i18n key to BOTH the en and pt-BR dicts in apps/web/src/lib/i18n.ts in the same commit, and mirror the feature description in README.md plus docs/PRODUCT.md's gate section (fonte: P3-389)
- When bounding a replay-dedupe cache (nonce seen-set), do refuse the newcomer with an `overflow` verdict instead of evicting the oldest, and only run cache admission after cheaper checks pass — an attacker's flood must never buy room for a… (fonte: RT-390)
- When refusing a handshake or session frame, do place the return BEFORE any state mutation (`sessions.set`, allowlist saves) and give each refusal its own distinct verdict — a rejected replay must never clobber a live session's replay guard… (fonte: RT-390)
- When hardening a replayable handshake, do seal the freshness proof (authenticated timestamp) inside the existing token rather than adding clear wire fields, treat a missing/non-finite stamp as its own fail-closed refusal instead of a toler… (fonte: RT-390)
- When several views style the same chrome role (pane headers, wordmarks, cards), do extract one shared class on the token scale in index.css instead of inline `style={{ fontSize: ... }}` — copies of the same literal drift apart (1rem vs the… (fonte: P3-384)
- When a new shared class replaces per-component styling, do absorb the earlier one-off fixes into it (P3-327's ellipsis, flex/min-width/margin layout props) and delete the now-dead local rules (`.browser-header h1`, `.mission header h1`) in… (fonte: P3-384)
- When you establish a CSS discipline like this, do pin it with a structural check in scripts/unit.test.ts (regex each view's `<h1>` tags asserting the shared class is present and `style=` absent, plus a token assertion that the class reads… (fonte: P3-384)
- When vertically centering content in a scrollable column (flex `justify-content: center`), do use overflow-safe centering (`justify-content: safe center` + `overflow-y: auto` on the container plus `scroll-padding-top`) — plain center makes… (fonte: P3-387)
- When adding a layout override to fix one screen, do first verify the selector targets a class unique to that component (not a shared base class like `.screen`) so the fix can't silently change sibling screens, and say so in the comment (fonte: P3-387)
- When fixing a pure-CSS visual invariant with no runtime test harness, do pin it with a source-reading unit test that asserts the rule exists with its key properties, and reference the task ID in the adjacent CSS comment so future refactors… (fonte: P3-387)
- When a new prominent state block (escalation card, error banner) renders in a column that already has a call-to-action, do inventory which CTAs coexist in that state and fold or demote the standalone one into the new block so users never s… (fonte: P3-385)
- When demoting a button to a quieter link, do reuse the same component with a className override instead of writing a fresh <a>/<button>, so its feedback contract (spinner, trying state, result toast) survives the restyle for free (fonte: P3-385)
- When an existing i18n label starts rendering on a new surface or state, do add that key to the journey-copy locale-resolve array in scripts/unit.test.ts in the same commit so raw-key fallback fails the battery in both locales (fonte: P3-385)
- When styling an interactive control nested inside a card, do pick its background relative to the immediate container (surface token on the white card) instead of the page-background token, and back it with a firmer resting border — a field… (fonte: P3-386)
- When fixing a visual bug caused by a subtle token choice, do pin the CSS contract with a source-reading test (regex the rule, assert the required tokens AND the absence of the offending one) so a later refactor cannot silently resurrect th… (fonte: P3-386)
- When a pane header label duplicates a label already owned by another surface (rail, menu, settings), do reuse that surface's exact dict key (e.g. `navBrowser` for both rail and pane title) instead of adding a parallel key — same-meaning ke… (fonte: P3-382)
- When localizing error copy fed by two sources (your own throws vs messages from the daemon/another process), do pattern-match known internal throws to dedicated dict keys and route everything else through one generic localized sentence wit… (fonte: P3-382)
- When a new state (escalation, offline, error) introduces its own action block, do suppress the pre-existing standalone action and fold it in as a demoted element preserving the full feedback contract (spinner, trying state, result toast) —… (fonte: P3-382)
- When a control is rendered `disabled` by a gate or phase (especially a primary CTA), do pair the attribute with visible disabled chrome (opacity gray-out + `cursor: not-allowed`) and a `title` tooltip explaining why, because native `disabl… (fonte: P3-380)
- When adding explanatory hints to gate-gated controls, do reuse the hint key the sibling controls on the same surface already carry (e.g. the rail's `gateSessionsHint`) instead of inventing new copy, so every blocked element explains the sa… (fonte: P3-380)
- When fixing a state-visibility bug on an interactive element, do pin the contract in the flow test with computed-style assertions (`disabled`, `title` present, `opacity`, `cursor`) rather than DOM-only checks, so a later refactor cannot si… (fonte: P3-380)
- When a brand glyph or display mark is a bare text node styled by a CSS class, always set an explicit display font-size with `line-height: 1` on that class — color/margin-only styling silently inherits body size and the mark collapses to ~1… (fonte: P3-383)
- When a vertically centered flex screen (e.g. `.degraded` gate column) can grow taller than the viewport, make the screen itself the scroll container and use `justify-content: safe center` with `scroll-padding-top` — plain `center` overflow… (fonte: P3-383)
- When caught raw error strings or hardcoded UI copy can reach a localized surface, classify known internal throws into their own dictionary keys (regex match) and ride unknown messages through a localized `{msg}` placeholder, and reuse the… (fonte: P3-383)
- When routing user-visible errors through the shared `humanizeError`, do treat it as a partial translator — it returns its input unchanged when no regex matches (errors.ts:43, and it knows "opencode responded 502" but not the "HTTP 502" for… (fonte: P3-381)
- When one error state renders per-surface with a ternary like `phone ? t("missionLoadFailed") : error`, do treat the raw branch as the bug and unify every surface on the same localized resolver in the same commit — surface-divergent renderi… (fonte: P3-381)
- When a view throws its own sentinel strings for display ("daemon unreachable", `HTTP ${status}`), do pattern-match those exact sentinels to dedicated dict keys before consulting the shared humanizer — its regexes only cover protocol-level… (fonte: P3-381)
- When mounting an Electron `<webview>` with no page (new-tab/empty first paint), do trigger the first navigation via the `src` attribute, not `loadURL()` — the guest has no dom-ready yet and `loadURL()` throws (same for `reload()` on an emp… (fonte: P3-379)
- When a pane would auto-navigate on first paint, do ship a designed empty state with no default URL and lock the invariant with a source-reading test asserting the old default literal (and its host port) is gone — a hardcoded default src si… (fonte: P3-379)
- When gating an overlay state on webview navigation events, do ignore the guest's idle `about:blank` in `did-navigate` handlers and only un-hide on real navigations, or the idle page both un-hides the empty state and dirties the address bar (fonte: P3-379)
- When a pane's primary data load fails because a local dependency is down (dead daemon), do split it into its own guided state (a `loadFailed` flag rendering a card: what broke, the auto-reload promise, pointer to the recovery surface, manu… (fonte: P3-377)
- When adding a manual retry to an auto-polling view, do eagerly re-fetch in parallel every source the pane renders (list, summary, per-selection detail), because a retry that just waits for the next poll tick is indistinguishable from doing… (fonte: P3-377)
- When adding error/guided-state copy keys, do pin in tests that each key resolves in BOTH locales (`translate(lang, key) !== key` and non-empty) so a missing dict entry can't ship as a raw-key fallback in a pt-BR app (fonte: P3-377)
- When a flex row with `flex-wrap: wrap` still collapses a title to an ellipsis instead of breaking to a second row, do give the title a natural `flex-basis: auto` — a child carrying `flex: 1` (basis 0) "fits" on any line and gets squeezed t… (fonte: P3-376)
- When a pane header overflows at its default width, do shorten labels that duplicate an adjacent control's qualifier ("Dashboard ao vivo" beside an "Ao vivo" pill) to the bare noun before reaching for smaller fonts — redundant wording is of… (fonte: P3-376)
- When compacting pane header controls, do reuse the established pane-chrome button scale (.browser-header's padding/line-height/font-size) instead of one-off sizing, and lock every independent piece of the fix (CSS wrap, compact scale, i18n… (fonte: P3-376)
- When an error represents an EXPECTED state (no client yet, first boot, machine switch), throw a dedicated sentinel Error class and branch on identity via an `isX()` helper — never match message prose, so rewording can't silently revert cal… (fonte: P3-375)
- When reserving danger-tone UI for real failures, also cover the previously-successful-load case: keep stale results on screen with a quiet hint riding along instead of wiping them, going red, or going silently stale (fonte: P3-375)
- When removing message-coupled branching, pin the fix with source-level test assertions that fail if the prose throw (`throw new Error("not connected")`) or prose-regex matching reappears anywhere (fonte: P3-375)
- When a packaged GUI/Electron app resolves external binaries, never trust `process.env.PATH` alone — a Finder/Explorer launch inherits a minimal PATH (`/usr/bin:/bin:/usr/sbin:/sbin`), so also probe the well-known runtime-manager install lo… (fonte: P3-395)
- When a pure module must stay free of fs/path/os imports but needs filesystem-derived data, do not add builtins — have the caller enumerate fault-tolerantly (missing dir, EACCES, hostile listing, cap overflow → that root contributes nothing… (fonte: P3-395)
- When extending an ordered candidate/precedence list, keep dedup favoring the earliest source (PATH wins over known locations) and make every new input degrade to exactly the previous list on failure, proving both with table tests per direc… (fonte: P3-395)
- When one i18n key is rendered by a shared component on two surfaces (desktop shell and phone), split it into per-surface keys selected by a pure helper from the shell verdict App already computes (`desktopBridge() !== null`) instead of wri… (fonte: P3-394)
- When removing banned copy from a dictionary, add a unit assertion that reads the real source file (i18n.ts) proving the forbidden literals are gone, and add every new key to the journey-copy locale-resolution array so raw-key fallback or a… (fonte: P3-394)
- When UI copy references an action, verify the referenced affordance exists on the same screen/block (like the adjacent "Open diagnostics" button) before writing the string — copy that sends the user elsewhere (a terminal, another machine)… (fonte: P3-394)
- When you must evidence a UI state caused by an absent machine dependency (e.g. binary not installed) on hosts that DO have it, do add a documented env hatch (like OCR_OPENCODE_MISSING=1) that forces only the classifier's verdict while the… (fonte: P3-392)
- When a layperson-facing degraded state has a fix outside the app, do surface it as in-app actions — per-platform install command to clipboard, official docs via the existing extlink gate, and a re-check that always ends in a terminal state… (fonte: P3-392)
- When adding filesystem enumeration to a daemon boot path, do keep it a pure module with injected readdir and a fail-tolerant contract: missing/unreadable/hostile entries contribute nothing, the result is capped and deterministic, and any f… (fonte: P3-392)
- Quando adicionar copy visível em qualquer superfície nativa do shell (diálogo, menu, bandeja) em apps/desktop, entre com o vocabulário no shellLabels (shelllang.ts) nas duas línguas com interpolação de versão via função — literais crus no… (fonte: P3-393)
- Quando renderizar texto de terceiros (notas de release do feed) num diálogo nativo, do sanitize num módulo puro (sem electron/node:fs) que remove marcação, caracteres de controle, caminhos e endereços, corta em tetos documentados e falha v… (fonte: P3-393)
- Quando um dado que já existe num ponto do fluxo (FeedInfo) só é usado depois (diálogo de consentimento), do thread pelos tipos existentes (DownloadedState → sink askInstall) capturando no único ponto em que o objeto está em mãos — sem cana… (fonte: P3-393)
- When adding window-level gesture listeners (drop, paste, hotkeys) inside a leaf component like ChatView, remember they die with its mount — give the app shell a single owner whose active surface comes from mount truth (gate while unpaired,… (fonte: P3-398)
- When a global gesture needs an accept/refuse/route decision, put the verdict in a pure no-React/no-DOM module over a closed input set (surface enum, bridge flag, count) with fail-closed rules for unknown/missing/non-numeric input and a doc… (fonte: P3-398)
- When a dropped payload must land in a conversation that does not exist yet, hold the files in App state with a fresh array identity per drop and let the newly mounted view adopt them once through the existing attachFile path (the paneArtif… (fonte: P3-398)
- When serving a verdict derived from a cached upstream probe (model readiness, doc-conversion readiness...), do re-observe lazily at the point of use behind a min-interval knob with a single-flight guard — no new route/port/periodic timer —… (fonte: P3-396)
- When adding a new read of an existing upstream endpoint, do route it through one shared helper that forwards the same auth credential and carries an abort timeout — a credential-less fetch misreads an authed server as "absent" and a timeou… (fonte: P3-396)
- When one verdict needs different copy per surface (desktop shell vs phone), do add dedicated i18n keys per surface chosen by a pure helper from the shell verdict the App already computes, keep every re-check action landing in a terminal st… (fonte: P3-396)
- When a cached readiness verdict is only fed by unrelated fetches (context-gauge refresh, passthrough) and freezes stale, re-observe lazily at the point of use: only while the verdict is not in the ready/happy state, at most once per a docu… (fonte: P3-397)
- When several call sites read the same upstream endpoint, funnel them through one shared fetch helper that forwards the same auth credential and applies the shared probe timeout — an uncredentialed copy misreads a 401 as "no model" and an u… (fonte: P3-397)
- When folding raw text into a single-line log entry, include CR (`\u000D`) in the stripped control-character range even if `\n` is the only char you fold — a surviving carriage return forges extra lines in the composed log (fonte: P3-397)
- When a native notification is decided by a renderer-pushed counter, do put the verdict in a pure module (no electron, no timers, no I/O) that fails closed on malformed input, and keep only the last known-good value as previous state in mai… (fonte: P3-399)
- When the shell also runs hermetic e2e sessions, do consult the test-session flag BEFORE constructing any Notification and lock that order with a source-reading assertion, because a log-only verdict must never leak a test toast onto the ope… (fonte: P3-399)
- When a new push signal resembles an existing one (pending asks vs unread badge), do ride its own preload channel with a best-effort try/catch in the web lib and log only the verdict kind + reason — never counts, commands, paths or titles —… (fonte: P3-399)
- When adding a module under apps/daemon/src, do keep it free of node:fs, network and timers — daemon index.ts runs main() on import, so one transitive import with top-level side effects boots a daemon inside unit tests (fonte: P3-400)
- When a route materializes data from an upstream origin, do inject the origin behind an interface whose every method returns null on any failure — the orchestrator degrades to a partial answer marked truncated, the route never throws on an… (fonte: P3-400)
- When scanning unbounded collections inside one request (sessions × messages), do enforce hard caps — most-recent count, per-conversation message cap, total wall-clock budget with early exit — mark every partial answer truncated:true, skip… (fonte: P3-400)
- When calling speechSynthesis.speak() from a PWA outside a user gesture (iOS standalone), do treat the first utterance as silently dropped — no onend/onerror ever fires — so resolve "not started" with a short watchdog checking synth.speakin… (fonte: P3-403)
- When adding a daemon route that spawns a host subprocess (whisper, edge-tts), do admit the request through a per-session sliding-window limiter BEFORE staging any work and bound the limiter's key map (prune fully-expired entries past a max… (fonte: P3-403)
- When gating a UI feature on a new capability, do plumb it as an additive optional field in the handshake caps (acceptPayload), feed it a live host verdict (binary probed at pairing time, never a constant) and require BOTH host verdict and… (fonte: P3-403)
