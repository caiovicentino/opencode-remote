# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When a brand glyph or display mark is a bare text node styled by a CSS class, always set an explicit display font-size with `line-height: 1` on that class — color/margin-only styling silently inherits body size and the mark collapses to ~1… (fonte: P3-383)
- When caught raw error strings or hardcoded UI copy can reach a localized surface, classify known internal throws into their own dictionary keys (regex match) and ride unknown messages through a localized `{msg}` placeholder, and reuse the… (fonte: P3-383)
- When routing user-visible errors through the shared `humanizeError`, do treat it as a partial translator — it returns its input unchanged when no regex matches (errors.ts:43, and it knows "opencode responded 502" but not the "HTTP 502" for… (fonte: P3-381)
- When one error state renders per-surface with a ternary like `phone ? t("missionLoadFailed") : error`, do treat the raw branch as the bug and unify every surface on the same localized resolver in the same commit — surface-divergent renderi… (fonte: P3-381)
- When a view throws its own sentinel strings for display ("daemon unreachable", `HTTP ${status}`), do pattern-match those exact sentinels to dedicated dict keys before consulting the shared humanizer — its regexes only cover protocol-level… (fonte: P3-381)
- When mounting an Electron `<webview>` with no page (new-tab/empty first paint), do trigger the first navigation via the `src` attribute, not `loadURL()` — the guest has no dom-ready yet and `loadURL()` throws (same for `reload()` on an emp… (fonte: P3-379)
- When a pane would auto-navigate on first paint, do ship a designed empty state with no default URL and lock the invariant with a source-reading test asserting the old default literal (and its host port) is gone — a hardcoded default src si… (fonte: P3-379)
- When gating an overlay state on webview navigation events, do ignore the guest's idle `about:blank` in `did-navigate` handlers and only un-hide on real navigations, or the idle page both un-hides the empty state and dirties the address bar (fonte: P3-379)
- When a pane's primary data load fails because a local dependency is down (dead daemon), do split it into its own guided state (a `loadFailed` flag rendering a card: what broke, the auto-reload promise, pointer to the recovery surface, manu… (fonte: P3-377)
- When adding a manual retry to an auto-polling view, do eagerly re-fetch in parallel every source the pane renders (list, summary, per-selection detail), because a retry that just waits for the next poll tick is indistinguishable from doing… (fonte: P3-377)
- When adding error/guided-state copy keys, do pin in tests that each key resolves in BOTH locales (`translate(lang, key) !== key` and non-empty) so a missing dict entry can't ship as a raw-key fallback in a pt-BR app (fonte: P3-377)
- When a flex row with `flex-wrap: wrap` still collapses a title to an ellipsis instead of breaking to a second row, do give the title a natural `flex-basis: auto` — a child carrying `flex: 1` (basis 0) "fits" on any line and gets squeezed t… (fonte: P3-376)
- When a pane header overflows at its default width, do shorten labels that duplicate an adjacent control's qualifier ("Dashboard ao vivo" beside an "Ao vivo" pill) to the bare noun before reaching for smaller fonts — redundant wording is of… (fonte: P3-376)
- When compacting pane header controls, do reuse the established pane-chrome button scale (.browser-header's padding/line-height/font-size) instead of one-off sizing, and lock every independent piece of the fix (CSS wrap, compact scale, i18n… (fonte: P3-376)
- When an error represents an EXPECTED state (no client yet, first boot, machine switch), throw a dedicated sentinel Error class and branch on identity via an `isX()` helper — never match message prose, so rewording can't silently revert cal… (fonte: P3-375)
- When reserving danger-tone UI for real failures, also cover the previously-successful-load case: keep stale results on screen with a quiet hint riding along instead of wiping them, going red, or going silently stale (fonte: P3-375)
- When removing message-coupled branching, pin the fix with source-level test assertions that fail if the prose throw (`throw new Error("not connected")`) or prose-regex matching reappears anywhere (fonte: P3-375)
- When a packaged GUI/Electron app resolves external binaries, never trust `process.env.PATH` alone — a Finder/Explorer launch inherits a minimal PATH (`/usr/bin:/bin:/usr/sbin:/sbin`), so also probe the well-known runtime-manager install lo… (fonte: P3-395)
- When a pure module must stay free of fs/path/os imports but needs filesystem-derived data, do not add builtins — have the caller enumerate fault-tolerantly (missing dir, EACCES, hostile listing, cap overflow → that root contributes nothing… (fonte: P3-395)
- When extending an ordered candidate/precedence list, keep dedup favoring the earliest source (PATH wins over known locations) and make every new input degrade to exactly the previous list on failure, proving both with table tests per direc… (fonte: P3-395)
- When one i18n key is rendered by a shared component on two surfaces (desktop shell and phone), split it into per-surface keys selected by a pure helper from the shell verdict App already computes (`desktopBridge() !== null`) instead of wri… (fonte: P3-394)
- When removing banned copy from a dictionary, add a unit assertion that reads the real source file (i18n.ts) proving the forbidden literals are gone, and add every new key to the journey-copy locale-resolution array so raw-key fallback or a… (fonte: P3-394)
- When UI copy references an action, verify the referenced affordance exists on the same screen/block (like the adjacent "Open diagnostics" button) before writing the string — copy that sends the user elsewhere (a terminal, another machine)… (fonte: P3-394)
- When you must evidence a UI state caused by an absent machine dependency (e.g. binary not installed) on hosts that DO have it, do add a documented env hatch (like OCR_OPENCODE_MISSING=1) that forces only the classifier's verdict while the… (fonte: P3-392)
- When adding filesystem enumeration to a daemon boot path, do keep it a pure module with injected readdir and a fail-tolerant contract: missing/unreadable/hostile entries contribute nothing, the result is capped and deterministic, and any f… (fonte: P3-392)
- Quando adicionar copy visível em qualquer superfície nativa do shell (diálogo, menu, bandeja) em apps/desktop, entre com o vocabulário no shellLabels (shelllang.ts) nas duas línguas com interpolação de versão via função — literais crus no… (fonte: P3-393)
- Quando renderizar texto de terceiros (notas de release do feed) num diálogo nativo, do sanitize num módulo puro (sem electron/node:fs) que remove marcação, caracteres de controle, caminhos e endereços, corta em tetos documentados e falha v… (fonte: P3-393)
- Quando um dado que já existe num ponto do fluxo (FeedInfo) só é usado depois (diálogo de consentimento), do thread pelos tipos existentes (DownloadedState → sink askInstall) capturando no único ponto em que o objeto está em mãos — sem cana… (fonte: P3-393)
- When a global gesture needs an accept/refuse/route decision, put the verdict in a pure no-React/no-DOM module over a closed input set (surface enum, bridge flag, count) with fail-closed rules for unknown/missing/non-numeric input and a doc… (fonte: P3-398)
- When a dropped payload must land in a conversation that does not exist yet, hold the files in App state with a fresh array identity per drop and let the newly mounted view adopt them once through the existing attachFile path (the paneArtif… (fonte: P3-398)
- When serving a verdict derived from a cached upstream probe (model readiness, doc-conversion readiness...), do re-observe lazily at the point of use behind a min-interval knob with a single-flight guard — no new route/port/periodic timer —… (fonte: P3-396)
- When adding a new read of an existing upstream endpoint, do route it through one shared helper that forwards the same auth credential and carries an abort timeout — a credential-less fetch misreads an authed server as "absent" and a timeou… (fonte: P3-396)
- When one verdict needs different copy per surface (desktop shell vs phone), do add dedicated i18n keys per surface chosen by a pure helper from the shell verdict the App already computes, keep every re-check action landing in a terminal st… (fonte: P3-396)
- When several call sites read the same upstream endpoint, funnel them through one shared fetch helper that forwards the same auth credential and applies the shared probe timeout — an uncredentialed copy misreads a 401 as "no model" and an u… (fonte: P3-397)
- When folding raw text into a single-line log entry, include CR (`\u000D`) in the stripped control-character range even if `\n` is the only char you fold — a surviving carriage return forges extra lines in the composed log (fonte: P3-397)
- When a native notification is decided by a renderer-pushed counter, do put the verdict in a pure module (no electron, no timers, no I/O) that fails closed on malformed input, and keep only the last known-good value as previous state in mai… (fonte: P3-399)
- When the shell also runs hermetic e2e sessions, do consult the test-session flag BEFORE constructing any Notification and lock that order with a source-reading assertion, because a log-only verdict must never leak a test toast onto the ope… (fonte: P3-399)
- When a new push signal resembles an existing one (pending asks vs unread badge), do ride its own preload channel with a best-effort try/catch in the web lib and log only the verdict kind + reason — never counts, commands, paths or titles —… (fonte: P3-399)
- When adding a module under apps/daemon/src, do keep it free of node:fs, network and timers — daemon index.ts runs main() on import, so one transitive import with top-level side effects boots a daemon inside unit tests (fonte: P3-400)
- When a route materializes data from an upstream origin, do inject the origin behind an interface whose every method returns null on any failure — the orchestrator degrades to a partial answer marked truncated, the route never throws on an… (fonte: P3-400)
- When scanning unbounded collections inside one request (sessions × messages), do enforce hard caps — most-recent count, per-conversation message cap, total wall-clock budget with early exit — mark every partial answer truncated:true, skip… (fonte: P3-400)
- When calling speechSynthesis.speak() from a PWA outside a user gesture (iOS standalone), do treat the first utterance as silently dropped — no onend/onerror ever fires — so resolve "not started" with a short watchdog checking synth.speakin… (fonte: P3-403)
- When adding a daemon route that spawns a host subprocess (whisper, edge-tts), do admit the request through a per-session sliding-window limiter BEFORE staging any work and bound the limiter's key map (prune fully-expired entries past a max… (fonte: P3-403)
- When gating a UI feature on a new capability, do plumb it as an additive optional field in the handshake caps (acceptPayload), feed it a live host verdict (binary probed at pairing time, never a constant) and require BOTH host verdict and… (fonte: P3-403)
- When adding a feature that fetches sensitive host data (screen frames, camera, mic), do capture in the desktop shell — the TCC-responsible Electron context — and keep the daemon a pure request→frame matcher holding data in memory with a TT… (fonte: P3-404)
- When a relay route accepts fulfillment of a sensitive request (e.g. screen frame upload), do require a single-use pending requestId minted by the explicit request route and consume it on receipt (409 for unknown/replayed ids) — otherwise a… (fonte: P3-404)
- When the desktop app runs a hermetic E2E session (OCR_DESKTOP_SESSION), do return a fixed synthetic frame (and short-circuit desktopCapturer) before any real screen access, and validate capture source ids against a strict regex (`/^(?:scre… (fonte: P3-404)
- When a React effect holds a getUserMedia stream, check the cancelled flag after every await and stop all tracks the moment the consuming <video> is missing or the panel shows an error — a stream left running behind an overlay keeps the cam… (fonte: P3-402)
- When feature-detecting non-standard MediaTrack capabilities like torch, test key presence ("torch" in capabilities), never truthiness — torch:false means supported-but-off — and remember Chromium only applies it inside the advanced constra… (fonte: P3-402)
- When a capture flow promises "nothing leaves the device until send" (privacy copy in the UI), keep capture I/O-free (canvas→blob in memory), have the parent confirm consumption via a Promise<boolean> so staged object URLs are only revoked… (fonte: P3-402)
- When writing a Blocked/failure reason from raw infra output, do pass the last failure detail through a pure sanitizer (control chars → spaces, URLs and 33+ char non-space runs redacted, whitespace collapsed, capped at a documented ceiling)… (fonte: P3-405)
- When an aggregate CI check fails, do name the red sibling checks already carried in the same statusCheckRollup (name=CONCLUSION format, excluding the aggregate itself) instead of emitting a bare "aggregate failed" message — the evidence is… (fonte: P3-405)
- When unit-testing secret-redaction code, do build token-like fixtures at runtime by concatenation (e.g. prefix + "x".repeat(40)) so no literal secret in the test source trips the no-secrets invariant scan (fonte: P3-405)
- When an Electron menu item displays an accelerator that is already registered system-wide via globalShortcut, mark it registerAccelerator:false (display-only) or the OS shortcut and the local menu accelerator both fire the same action (fonte: P3-406)
- When a React parent must trigger a one-shot focus/action in a child that remounts (tabs, session switches), use absolute bump counters that the child consumes via a callback the parent uses to reset the tick to 0 — booleans or unconsumed t… (fonte: P3-406)
- When a secret-scrubbing rule already exists for one output (sidecar log), do extract it into one shared pure function reused by every other surface — copied regexes drift and the un-updated copy becomes the leak — and pass machine-derived… (fonte: P3-407)
- When saving a user-facing file from Electron, do write atomically (temp file in the target folder + rename, 0600 mode) and return a status-only result ({ok, canceled}) — never the path — treating a dismissed save dialog as its own terminal… (fonte: P3-407)
- When a decision can hold a shared machine resource (sleep blocker, lock, shutdown veto), do fail-safe to RELEASE on any invalid input and order the hermetic-test-session check FIRST, pinned with a source-reading assertion over the real mai… (fonte: P3-409)
- When a renderer-inferred signal drives OS-level behavior in main, do derive it in a pure module, ship only a validated primitive over a dedicated one-way channel (renderer→preload→main), and re-validate bounds in main before consulting the… (fonte: P3-409)
- When persisting a default-on user preference, do write atomically (sibling tmp 0600 + rename, following quitstore) and degrade every read failure to the documented default unless an explicit false is stored, keeping ENOENT silent and the f… (fonte: P3-409)
- When a vertically centered flex column can outgrow its viewport (full-screen gates, wizards, hero cards), do not rely on plain justify-content: center — centered overflow clips content above the scroll origin so the wordmark renders sheare… (fonte: P3-423)
- When pinning a header with position: sticky over scrolling cards, give it an opaque page-color background, an explicit z-index, and move the screen's top inset/safe-area padding onto the header itself so the scrollport starts flush at the… (fonte: P3-423)
- When a scroll container has a sticky header, set scroll-padding-top (header height plus safe-area inset) on the scrollport — programmatic focus() scrolls, like an empty-submit refocusing the paste/code box, otherwise land the focused eleme… (fonte: P3-423)
