# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When a screen needs its main action, reuse the shared accent primary (button.primary) instead of inventing a one-off paint like inverted fg/bg, so one user journey never shows two competing primary-action dialects; keep warn tones on the s… (fonte: P3-449)
- When two cards in the same visible journey repeat the same promise, do state it once in the card where the user acts and let the other card say only what happens next (fonte: P3-436)
- When fixing copy in `apps/web/src/lib/i18n.ts`, do mirror the edit across both the en and pt dictionary entries in the same commit — the parallel dictionaries keep the removed duplication alive in the unedited locale (fonte: P3-436)
- When an earlier task pinned a component's TSX shape against a visual regression, do also pin the CSS side — extract the class's rule block and assert it carries no card-affordance tokens (border/background/radius/box-shadow), because a lat… (fonte: P3-438)
- When a CSS class must stay scoped to one variant (note vs full card), do assert its occurrence count and source order in the component file (exactly one use, appearing before the card element) so the class can never leak back onto the vari… (fonte: P3-438)
- When a caption/note variant shares a component with a card variant, do treat "zero chrome on captions, affordances only in the card branch" as a durable invariant worth its own battery check — the explorer shot shows a one-line note wearin… (fonte: P3-438)
- When building a passthrough URL from an attacker-controllable path with `new URL(path, base)`, validate the raw string before any parser touches it (must start with `/`; reject `//`, any backslash, control chars/whitespace the WHATWG parse… (fonte: P3-452)
- When tightening an existing passthrough with an anchored allowlist, first inventory every script that already probes the route (live soak, smoke, chunk regression, e2e) and admit their exact literals — invariants/smoke/chunk all send `GET… (fonte: P3-452)
- When a client feature starts calling a new opencode route through the daemon tunnel, add an anchored entry to `UPSTREAM_ROUTES` in apps/daemon/src/pathguard.ts in the same change — the allowlist is hand-maintained and unlisted routes fail… (fonte: P3-452)
- When adding an empty state to a pane, reuse the composed house pattern (centered icon + `-empty-title` + `-empty-hint` stack, muted tokens) instead of new chrome, and keep the pane's auxiliary action card rendered inside the empty world so… (fonte: P3-444)
- When mounting an existing card inside a new empty-state container that sets `color: var(--muted)`, add a scoped override restoring full text color and container-appropriate margins — grid-context margins and inherited muted color silently… (fonte: P3-444)
- When a caps section label and the card title directly beneath it repeat the same action phrase, do rename the label to name the direction/category and keep the action verb only in the card title, so identical copy never stacks 40px apart i… (fonte: P3-435)
- When docs quote literal UI copy (README journey walkthroughs), do update those quotes in the same commit as the i18n change, or the documentation drifts from the shipped interface (fonte: P3-435)
- When building an upstream URL from a request field a peer can seal (e.g. `req.path` into `new URL(path, base)`), validate the shape first — the URL constructor silently discards the base whenever the value carries a scheme or authority, so… (fonte: P3-442)
- When rejecting attacker-controllable input, log and audit only a structured rejection reason and return 400 — never echo the raw path/query (log-injection) and never attribute it to auth failure, which would train clients to retry as auth (fonte: P3-442)
- When a gated/disabled primary CTA reads as broken in a static screenshot, put the explanation in a visible caption directly under the control (tooltips vanish in shots and distant footer copy gets ignored) and dim only the label over a fir… (fonte: P3-442)
- When demoting a CTA's visual rank for hierarchy on one screen, inventory every surface that renders the same class combo first — the demotion silently applied to the standalone manual ceremony where the paste form IS the main action, readi… (fonte: P3-433)
- When superseding a pinned style decision, flip its battery assertion to outcome terms — assert the one-off override block is ABSENT and the className combo remains — because token-level matches (exact `var(--bg)`, `var(--border-strong)` in… (fonte: P3-433)
- When interpolating values into AppleScript `do script` (or any host that hands text to a user's shell), quote BOTH layers by construction — POSIX single-quote the shell command (`'…'` with inner `'` as `'\''`) and pass that command to osas… (fonte: RT-439)
- When validating an id or path that is interpolated into a fetch URL or command, enforce its full shape with a strict regex (e.g. `^ses_[A-Za-z0-9]{4,64}$`) rather than a prefix check like `startsWith("ses")` — prefix checks let traversal/m… (fonte: RT-439)
- When hardening injection-prone handlers, extract validators/quoting/argv assembly into a pure module (no I/O, clock, logging) so they are unit-testable in isolation, and never log the rejected input value — logging attacker-controlled stri… (fonte: RT-439)
- When styling error feedback for recoverable user mistakes (a paste error, an empty submit), reserve full danger treatment (danger border + tinted banner) for true incidents and reuse the house `.degraded-upstream` tone instead — neutral `v… (fonte: P3-432)
- When a task's acceptance is purely visual (colors, borders, backgrounds), DOM tests can't observe it — pin the rule blocks by reading `index.css` text (slice from `.selector {` to the next `}`) and asserting the CSS tokens, while keeping s… (fonte: P3-432)
- When demoting an action button from danger styling to the neutral bordered language, add explicit hover feedback (`color-mix(in srgb, var(--fg) 6%, transparent)`) — a transparent-background neutral button otherwise reads as static text and… (fonte: P3-432)
- When a form field needs an error visual state, set `aria-invalid` from the same state variable that renders the inline error message and style it with `[aria-invalid="true"]` (plus `:focus`/`:focus-visible` variants) — one attribute serves… (fonte: P3-431)
- When overriding a global `:focus-visible` accent ring for an error state, override only the longhand `outline-color` (and `border-color`) with the danger token instead of restating the full outline shorthand — width/style inherit from the… (fonte: P3-431)
- When the spec asks to suppress a styling token in favor of another, assert the old token's ABSENCE in the test (e.g. `!/var\(--accent\)/` over the matched rule), not just the new one's presence — presence-only checks let a global rule chan… (fonte: P3-431)
- When a card/component must render in both a populated grid and a new empty-state world, do extract it into a single shared JSX variable used by both branches so its state and actions never get orphaned by the empty collapse (fonte: P3-434)
- When adding an empty state to a pane, do extend the existing `.browser-empty-*`/`.artifacts-empty-*` selector groups and size the container to the parent's layout mode (`.screen` flex column → in-flow `flex: 1`, not the browser pane's abso… (fonte: P3-434)
- When rendering a pane title or accessible label, do use the shared `t()` dictionary key in every branch — including fallback (`!daemonApi`) branches and iframe `title` attributes — and keep inline icons out of the pane `h1` (fonte: P3-434)
- When a battery check counts patterns in source, count structural elements (e.g. the <select> tag) after slicing the file to the component region between stable className anchors, because raw token/key counts over the whole file get inflate… (fonte: P3-429)
- When replacing an existing UI pattern with a new one, pin both directions in the tests — new markup present AND the old direct-child pattern absent — so a silent partial revert of the refactor cannot pass the battery. (fonte: P3-429)
- When a control is identified only by an invisible aria-label, wrap it in a visible label that reuses the screen's existing furniture grammar (quiet-caps, muted, one type step down) and mirror the sibling field's focus treatment (border esc… (fonte: P3-429)
- When interpolating values into AppleScript `do script`, remember the string executes twice — as an AppleScript literal, then by the user's shell — so escaping only `"` leaves `;`, `$()`, backticks, `&&` and newlines live; do pass the comma… (fonte: P3-430)
- When an error block names a root cause (e.g. agent/daemon down), offer in that same block the action that fixes the cause — a generic retry whose prerequisite is the down dependency itself (re-minting the QR needs the daemon) is a dead end (fonte: P3-443)
- When threading shell bridges (IPC callbacks like app:reconnectDaemon) into components, accept them as optional props and fall back to the previous generic action when absent (plain browser) — never render a bridge-dependent control uncondi… (fonte: P3-443)
- When adding a fix action to an error state, reuse the existing shared component (ReconnectButton) with the established primary styling instead of duplicating a button — keeps one primary-action dialect across first-boot surfaces (fonte: P3-443)
- When adding chrome/buttons in panes or headers, reuse the shared SVG components from icons.tsx instead of typing unicode glyphs (←, ↻, ⤢) — text glyphs render with font metrics that drift across themes/zoom and create a second visual diale… (fonte: P3-437)
- When a tool rewrites a file at every agent spawn (e.g. writeSandboxConfig emitting opencode.json), gitignore it immediately — it is generated per-agent sandbox state, never a repo file, and one spawn's config will otherwise get committed o… (fonte: P3-437)
- When wrapping content in new layout containers on a screen with a sticky brand header, do keep the sticky element as a direct child of the scroll container (outside the new composition) — `position: sticky` only pins within its containing… (fonte: P3-441)
- When introducing wrapper divs around children that inherited layout from a parent (e.g. `.screen` gap), do re-declare those inherited properties on the wrappers for the mobile breakpoint — a new container silently drops what its children u… (fonte: P3-441)
- When adding a responsive composition to a screen the shell also switches at a breakpoint, do reuse the shell's exact threshold (1024px / isDesktop) for the new media query — two sources of truth for "desktop" make CSS and shell disagree ab… (fonte: P3-441)
- When interpolating agent-generated multiline text into a single-line structured record (markdown task line, config entry), do flatten it through a pure normalizer first (newlines/control chars→spaces, strip the metacharacters the record's… (fonte: P2-336)
- When classifying free text into a fixed vocabulary (like an area tag for scheduling), do use a keyword table with documented precedence (row order, critical surfaces first) plus a stable fallback, word-bounded and case-insensitive so a key… (fonte: P2-336)
- When adding a diagnostic module that unit tests must import, keep it pure — no network, no node:fs, no timers, no imports at all — because apps/daemon/src/index.ts runs main() on import and any dependency boots a daemon; pass the expected… (fonte: P2-335)
- When two apps need the same URL-derivation rule (ws/wss → http/https healthz), duplicate it deliberately in each app and pin parity with unit tests over the same host table instead of cross-app imports, which drag electron-adjacent code in… (fonte: P2-335)
- When adding a new versioned/self-describing field to a shared payload (e.g. `protocol` in relay /healthz from P2-331), grep and update every consumer that validates that payload (connection tests, probes) in the same change — an old valida… (fonte: P2-332)
- When classifying a peer against a wire-protocol constant, map an absent or non-positive-integer version field to a calm lenient state ("outdated, keeps working"), never to failure — only an explicit positive-integer mismatch is a hard verd… (fonte: P2-332)
- When code compares against a shared protocol constant (RELAY_WIRE_PROTOCOL), pin it with a source-reading test assertion that fails on numeric literals, proving the compared value comes from the imported constant — copy-pasted literals sil… (fonte: P2-332)
- When a nightly/automated agent reviews a built app (explorer, fable, red-team shots), rebuild the gitignored bundles from the current checkout before every run and fail closed if the build fails — gitignored dist survives `git clean -qfd`… (fonte: P3-440)
- When a long blocking exec (spawnSync, minutes) shares a Node process with a watchdog/KeepAlive timer, feed the heartbeat immediately before AND after the exec — the timer interval cannot fire mid-exec but its queued callback runs the insta… (fonte: P3-440)
- When scripting e2e interactions against a React controlled input whose error nudge only clears via onChange, never rely on fill("") into an already-empty field — React's value tracker dedupes the no-op set so no onChange fires and the nudg… (fonte: P3-440)
- When an inline error verdict can be produced by more than one source (local validation state and a parent/App-level prop), drive the field's invalid styling (aria-invalid, danger border, focus ring) from the same predicate the message bloc… (fonte: P3-428)
- When a field can wear an invalid state, make its focus-visible ring switch to the danger color together with the border (one shared condition driving both) — a green accent focus ring directly above a red rejection reads as "accepted" at t… (fonte: P3-428)
- When classifying an error prop for styling, compare it against the same localized t("key") literal the verdict block uses rather than a separate mechanism, so the field flag and the message can never diverge if the copy or key changes (fonte: P3-428)
- When a UI entry (scan QR, pair-phone, intro) depends on a resource the same screen just reported as down, suppress it with a conditional on the settled health signal the error path already uses (reusing the same kind verdict, never a secon… (fonte: P3-427)
- When a surface can be genuinely absent (not just demoted), assert with a null/boolean probe like `!!document.querySelector(sel) === false` or section-count equality instead of class-rank checks, and move the removed element's live-state co… (fonte: P3-427)
- When recovery is only reachable behind a quiet back control, render the recovery action (ReconnectButton with the shell restart bridge) inside the same verdict card as the message naming the cause, repeating the error's exact wording one c… (fonte: P3-427)
- When sibling cards on one screen could each promise the same behavior (storing the typed text), give the promise one owner — the queue card owns the storage promise and the mission card only says where the mission comes from — and record t… (fonte: P3-426)
- When fixing an i18n empty-state string, add unit checks asserting the new concrete phrase in every locale (en "first-message box" / pt "caixa de primeira mensagem") plus the survival of the prior fix's content (after-pairing deferral, GitH… (fonte: P3-426)
- When re-serializing unauthenticated wire input, type- and bounds-check EVERY field that flows into JSON.stringify before the rebuild — its recursion is unbounded, so attacker-controlled metadata like `from`/`seq` is as dangerous as `payloa… (fonte: RT-455)
- When a malformed message must never take down a long-lived server, wrap the entire per-message routing path in try/catch that closes only the offending socket (1011) and releases its rooms/slots via the normal close path — no shape gate ca… (fonte: RT-455)
- When tightening a wire protocol without breaking peers, keep the gate pure (no imports/I/O) and byte-identical for legitimate frames (absent stays omitted, null stays null, existing fallbacks unchanged), placed exactly where the old check… (fonte: RT-455)
- When a client records a permanent incompatibility with a remote peer (wire-version mismatch), do pace its reconnect through the SAME existing max(jittered, floor) rule via a pure state→ms helper that floors only the exact known string and… (fonte: P2-339)
- When adding a new floor to a retry scheduler that already has multiple floors, do extend the floor-source enum additively and encode precedence in one place (peer-requested backoff, e.g. relay close, outranks any client-derived floor), bot… (fonte: P2-339)
- When a long backoff exists to stop hammering a remote service, do make it anticipatable only by an explicit throttled human action ("reconnect now") and never by code paths, while any floor the peer itself requested stays never anticipatab… (fonte: P2-339)
- When consuming an additive field from another process's payload (e.g. daemon /api/health into the desktop shell), sanitize it through a pure function to a closed set that degrades absent/null/out-of-set/non-string to null — fail-closed — s… (fonte: P2-338)
- When adding a state to an existing verdict chain (linkVerdict), keep live evidence outranking stale diagnosis — connected beats a recorded mismatch because the self-healing source clears it — and make the neutral states preserve byte-for-b… (fonte: P2-338)
- When a constant must be shared between apps that cannot import each other (desktop must not drag daemon sources into its build), duplicate it deliberately and pin parity with a source-reading test in scripts/unit.test.ts that fails the mom… (fonte: P2-338)
- When two screens promise the same behavior (e.g. auto-retry) on adjacent states, extract the feedback into one shared component/contract instead of parallel copies — otherwise the promise renders two dialects one screen apart (fonte: P3-454)
- When a seconds ticker resets on a prop change (e.g. attempt counter bump), reset the started-at ref AND bump a state sitting in the interval effect's deps — zeroing only the elapsed state leaves the already-cleared interval and the ticker… (fonte: P3-454)
- When an escape action exits a wizard mid-onboarding (e.g. "Open diagnostics" from the escalation block), have it stamp the wizard-complete flag first so the side door is an explicit terminal choice and the flow never re-prompts or dead-ends (fonte: P3-454)
- When adding an inline escape action to a modal/dialog notice that only works on some surfaces, render it only when the caller hands the handler as an optional prop — paneless surfaces (classic unpaired, add-machine ceremony) and the phone… (fonte: P2-337)
- When wiring a one-shot focus/scroll request across components (App → pane child), use an absolute bump counter state consumed by a callback that resets the state to 0, keep the tick in a per-consumer ref guard, and keep the request pending… (fonte: P2-337)
- When imperatively scrolling to a section for a test harness or user focus, select it by a copy-independent `data-*` attribute (never by i18n copy) and pass `behavior: matchMedia("(prefers-reduced-motion: reduce)").matches ? "auto" : "smoot… (fonte: P2-337)
- When surfacing a verdict the parent already holds (e.g. `pairingState.relayLink`) on a new surface, thread it as an optional prop defaulted to null and render the line only when the prop is present — surfaces without the data (phone, pure… (fonte: P3-453)
- When a second surface shows the same live connection verdict as an existing screen, reuse that screen's i18n keys, the verdict's own static message and its warn class, and expose a copy-independent `data-*` state attribute for the harness… (fonte: P3-453)
- When a status line can be unknown or absent (e.g. daemon predates the field), keep unknown/missing neutral with no warn styling — absence of data is never an accusation; only known bad states paint warnings (fonte: P3-453)
- When free-form prose can land inside a parser-consumed section of shared markdown (e.g. `## Ready` in BACKLOG.md), do keep writer-side validation unified with the parser on the same exported predicate (isValidTaskLine) and flag orphan bloc… (fonte: P2-341)
- When a periodic pass emits an alert tied to a metric, do dedupe by comparing the value between passes (equal count stays silent) and reset the memo when it returns to baseline so cleanup followed by regrowth alerts again — in-memory memo i… (fonte: P2-341)
- When adding a scanner or diagnostic helper, do keep it pure (markdown string in, report object out — no node:fs/process) and cover it with tests reading the repo's real files (actual BACKLOG.md yielding count>0, real index.ts source provin… (fonte: P2-341)
- When a build-tool flag's runtime behavior depends on the tool's generated scripts (e.g. electron-builder's `nsis.deleteAppDataOnUninstall` and its NSIS templates), do read the installed dependency's real templates/source under node_modules… (fonte: P3-456)
- When a test reads external dependency sources as compile-time truth, do also pin the dependency's version (its package.json) so a lockfile drift fails loudly and forces human re-verification instead of silently validating stale behavior (fonte: P3-456)
- When configuring destructive cleanup in NSIS installers, do guard the wipe on the upgrade signal the template exposes (`--updated` / `${isUpdated}`) because installing over an old version runs the previous uninstaller silently — unguarded… (fonte: P3-456)
- When adding a user-triggered action that mirrors an existing automatic behavior (a click duplicating the wake path), reuse the exact same internal helper and keep the legacy call path byte-for-byte by only adding a return value to it, lett… (fonte: P2-340)
- When exposing a main-process network answer to the renderer over IPC, funnel it through a pure sanitizer that maps only the documented enum pairs to a closed set and fails closed to a single "unavailable" value on non-200 status, null/malf… (fonte: P2-340)
- When a UI control awaits an async backend call, guarantee a terminal outcome: one static i18n phrase per closed-set verdict (both locales, path/URL/port-free) with a rejected promise or unknown value degrading to the failure phrase, so the… (fonte: P2-340)
- When gating a fleet-critical path like update offers by percentage, make every missing, non-numeric, fractional or out-of-range input fail open to today's behavior (holding a machine by doubt freezes the fleet) and keep exactly one explici… (fonte: P2-342)
- When assigning installs to rollout cohorts, derive the bucket from hash(installationId + offeredVersion) % 100 — a random UUID persisted once in userData with mode 0600, never hardware/keys/pairing-derived, regenerated silently when illegi… (fonte: P2-342)
- When wiring a pure verdict module into main.ts, resolve its side-effecting inputs lazily (cache the id read so only machines with a pending update ever touch the file) and short-circuit hermetic harness sessions before any I/O (the pure ru… (fonte: P2-342)
- When two surfaces can be mounted at the same time and expose the same kind of action, give each its own copy-independent data attribute (overlay data-pair-relay-redial vs Settings data-relay-redial) so e2e selectors never collide across pa… (fonte: P2-343)
- When several components must render identical closed-set verdict phrases, export the phrase map from one module and import it in the other instead of duplicating keys — a single source prevents phrase drift. (fonte: P2-343)
- When an async bridge-backed action can reject or return an unexpected value, degrade it to a terminal static failure phrase (catch to unavailable, map fallback to the failure key) and render the action only when the caller hands the handle… (fonte: P2-343)
