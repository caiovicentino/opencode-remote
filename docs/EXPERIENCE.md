# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When UI copy promises an automatic behavior ("connects by itself"), do render that attempt live (status dot + phase copy + retry) instead of an idle manual form — a screen contradicting its own intro is a product bug, not a styling one (fonte: P3-332)
- When an automatic mode (local auto-connect) can hide the only manual path, do force the mode off for explicit user intent (`localMode={false}` for "add machine" — entering a remote code IS the manual ceremony) so automation never swallows… (fonte: P3-332)
- When a retry button backs an automatic attempt, do re-arm the automation itself (`tryAutoPair()`), not just reset the phase — retry that only flips UI state leaves the promised auto-flow dead on screens with no manual fallback (fonte: P3-332)
- When centering content inside a header implemented as a flex row (e.g. a shared `space-between` `.screen > header`), do drop the flex (`display: block`) together with `text-align: center` — flex items ignore `text-align`, so adding only th… (fonte: P3-330)
- When overriding a shared base class for one screen, do scope the override to the variant selector (`.screen.degraded > header`, not `.screen > header`) and assert no sibling component's className matches it — sibling screens reusing the ba… (fonte: P3-330)
- When a fix lands purely in CSS with no runtime behavior to exercise, do add a textual unit-test assertion on the rule (expected properties present, selector still scoped) — it is the cheapest guard against a later refactor silently droppin… (fonte: P3-330)
- When a wait or error state depends on an external process (local daemon/agent, network), do name that dependency in the UI copy — an indefinite spinner whose cause is invisible reads as broken, not as "waiting" (fonte: P3-329)
- When an automatic flow can get stuck, do put the labeled manual escape on the stuck screen itself — an alternative hidden behind an unlabeled link one screen earlier is unreachable for a first-time user (fonte: P3-329)
- When a fix adds new user-facing copy, do add a unit assertion that the keys resolve in every supported locale (result ≠ raw key) — a missed translation renders the raw key string on the exact screen being fixed (fonte: P3-329)
- When gating UI on async-loaded state (e.g. `localMode={pairingState?.mode === "local"}`), the flag evaluates false while the state is still loading and flashes the wrong flow — make a positive verdict sticky for the session (a `nextShellLo… (fonte: P3-331)
- When an automatic mode (auto-connect/local pairing) coexists with a manual ceremony (paste/scan code), explicit manual intent must force the automatic mode off (`localMode={false}` / `localMode && !pairManual`) and every wait/error state n… (fonte: P3-331)
- When re-arming auto-connect from a poll-driven effect after an error, use a time-based backoff (e.g. 15s, never a hard cap, never per-poll) to avoid hammering a half-up daemon, and make Retry reconnect any stored pairing verbatim before re… (fonte: P3-331)
- When a verdict computed in the desktop main process must reach the UI, extend the existing payload additively (optional field in preload.ts + mirrored tolerant type in the web-side interface, no new channel/IPC) — otherwise the data dies i… (fonte: P2-324)
- When mapping an external status object to user-facing copy, write a pure fail-closed function that validates types, accepts only the documented closed set of states, returns null (silence) for absent/malformed/unknown/non-textual inputs, a… (fonte: P2-324)
- When several notices share one UI band, encode precedence in a single explicit expression (e.g. `exitNotice ?? wedgeNotice`) with the stronger story winning, and lock in module hygiene with source-level unit tests asserting purity (no Reac… (fonte: P2-324)
- When a fail-closed guard asserts the PRESENCE of an expected artifact type (`--expect dmg,zip`), wire that expectation only into the job that actually builds the artifact (release.yml packaging jobs) and run the same collector without it i… (fonte: P2-325)
- When classifying a packaging output dir by file suffix, skip the unpacked payloads during the walk (`.app` bundles, `*-unpacked/` dirs) — they contain the app's own `.exe` binary, which a suffix classifier would otherwise measure against t… (fonte: P2-325)
- When setting a size budget, derive the ceiling from a real measured maximum plus documented slack (~40%, written in the comment beside the table entry) and make missing/zero/negative/non-numeric measurements distinct failures — a broken st… (fonte: P2-325)
- When checking if a file is executable on win32, never trust `statSync(path).mode & 0o111` — libuv never sets exec bits in st_mode on Windows — do detect by case-insensitive `.exe` suffix and reserve exec-bit checks for non-win32 platforms (fonte: P3-343)
- When adding best-effort diagnostics (screenshots, dumps) to CI boot smokes, do make them strictly fail-open: env-gated (e.g. `OCR_PACKAGED_BOOT_SHOT`), try/catch with a one-line log, uploaded with `if: always()` + `if-no-files-found: ignor… (fonte: P3-343)
- When a rule depends on the host platform, do extract it into a pure function taking `platform` as an explicit parameter (like `isExecutableEntry(name, mode, platform)`) instead of reading `process.platform` inline, so unit tests can exerci… (fonte: P3-343)
- When auto-resolving git merge conflicts programmatically, keep the resolver a pure function (marker text in, plan out, all I/O in the caller) and fail closed on anything unprovable: malformed/nested markers, marker-free files (binary or de… (fonte: P3-341)
- When a repair pushes a new PR head mid-merge, re-probe gated on `headRefOid === expectedSha` before arming the merge — a snapshot missing the sha field stays `pending` (honest infra timeout, free retry), so a green CI verdict inherited fro… (fonte: P3-341)
- When feeding git-reported paths into shell commands, read them NUL-delimited (`git diff -z --name-only`) and escape with single-quote shell quoting — JSON.stringify is NOT shell quoting and leaves `$()`/backticks active inside double quote… (fonte: P3-341)
- When rejecting or revoking a client on a multiplexed socket (the single relay WebSocket shared by all paired clients), never close the shared transport — drop that pub's frames or close only its logical/local session, or one zombie client… (fonte: P3-344)
- When identifying client keys in logs or audit records, don't slice the base64/DER prefix — the leading SPKI bytes are a constant ASN.1 header identical for all P-256 keys; hash the decoded DER bytes (sha-256, first 16 hex chars) for a fing… (fonte: P3-344)
- When a rejected/untrusted client can auto-reconnect in a loop, throttle per-identity warn+audit emissions via a bounded in-memory map (pure decision function with injected clock) while a metrics counter ticks every occurrence, so the loop… (fonte: P3-344)
- When a fixed-size (resize: none) input holds unbreakable strings like URIs, add `overflow-wrap: anywhere` to the rule — with no resize grip or row growth a long token otherwise overflows the box (fonte: P3-369)
- When an input/textarea receives machine-generated content (URIs, tokens, codes), set `spellCheck={false}`, `autoCapitalize="off"`, `autoCorrect="off"` and `autoComplete="off"` — keyboard autocorrect silently mangles pasted values (fonte: P3-369)
- When tempted to add a `window.*` debug hook for flow autopsies, expose nothing: any page script can read it (seq counters, pending request paths), so diagnose via the daemon log and remove leftover hooks from the client (fonte: P3-369)
- When a CI test exercises a restart/reconnect race that loses a frame roughly once in N runs, mirror the local fail-flaky-once policy in the workflow — `set -euo pipefail` + `cmd || { echo "why retrying"; cmd }` under a `timeout-minutes` ca… (fonte: P3-372)
- When adding per-second elapsed/attempt feedback to a status line that carries `role="status"`, wrap the ticking segment in `aria-hidden` (and use tabular numerals against per-second width jitter) so screen readers are not re-announced ever… (fonte: P3-372)
- When a component prop encodes a platform-wide policy (e.g. `preferPaste` = desktop paste-first) instead of a per-feature choice, do add a structural test asserting EVERY call site of that component passes the prop — one omitted call site s… (fonte: P3-366)
- When a toast/overlay must be dismissible across parent branch-switch remounts, do keep the dismissal in parent state zeroing the same timestamp that seeds the trigger — a component-local flag is re-seeded on remount and the dismissed UI re… (fonte: P3-366)
- When asserting a CSS layout fix in source (not in a browser), test the property COMBINATION — e.g. `text-align: center` AND the absence of `display: flex`/`justify-content` — because flex items ignore text-align, so a single-property asser… (fonte: P3-374)
- When a control must sit relative to others (step indicator under wordmark, skip inside the action row), pin the markup ORDER with `indexOf` comparisons between stable class-name markers, not just presence checks — order is what layout bugs… (fonte: P3-374)
- When extracting a CSS rule for assertions, slice from the selector index to the next `}` and check the slice is found (`indexOf > 0`/`> -1`) before asserting properties, so a missing rule fails the test instead of passing vacuously (fonte: P3-374)
- When asserting DOM state through the desktop-flow `ipc` harness, return probes as `join('|')` lists (booleans + `|`) instead of `JSON.stringify` — the harness JSON-wraps the eval result, so in-page JSON comes back with `\"` escapes that no… (fonte: P3-367)
- When a second component needs to read/write a preference already persisted by a shared lib (theme, lang, font), do import its helpers (`readTheme`/`applyTheme`/`THEME_KEY` from `lib/theme`) instead of re-implementing a local localStorage r… (fonte: P3-368)
- When user-facing copy enumerates capabilities ("Idioma e tema ficam nesta máquina"), do verify each enumerated item has a working control in that same view before merging — or scope the copy down to what ships; promise/feature mismatch is… (fonte: P3-368)
- When exposing daemon-backed views pre-connection, do wire them to a no-op request/transport and humanize their error paths, or the newly reachable panes leak raw "not connected" throws instead of degrading gracefully (fonte: P3-365)
- When copy claims a feature is locked, do derive that copy from the same state that controls reachability (e.g. a `panesReachable` prop), because a lock glyph beside a working rail button is the self-contradicting-screen bug class (fonte: P3-365)
- When an arbiter/judge LLM approves a round in which every adversarial finding was mechanically discarded as unverifiable, require proof of contact with the code — at least one cited `path:line` or quoted snippet that survives `verifyFindin… (fonte: P3-355)
- When a renderer lib touches localStorage, do keep it pure — inject the storage as a parameter, wrap every read/write in try/catch, and sanitize (trim, strip NULs, length-cap) on both read and write — so a poisoned or unavailable store retu… (fonte: P3-360)
- When a textarea and action rows live inside a CSS grid panel targeted at phone widths, do set `min-width: 0` on the grid children — grid items default to min-content sizing and the hint/save rows clip past the panel border instead of shrin… (fonte: P3-360)
- When a Node script that spawned an Electron app (or any child process tree) prints its verdict but hangs in CI, do not rely on `process.exitCode` or a parent-only SIGKILL — capture the child pid right after launch and end with a determinis… (fonte: P3-348)
- When a watchdog timer guards a smoke script's deadline, keep it armed through the post-verdict path (never clearTimeout at the success point) and `unref()` it so it is never itself the handle holding the loop — a hang after the verdict is… (fonte: P3-348)
- When deriving an exit code dynamically from a verdict, coerce non-integer/undefined/NaN codes to 1 and run best-effort cleanup (kill) before a single final `exit` call, so a throwing kill or an early throw never becomes an accidental succe… (fonte: P3-348)
- When a periodic maintenance job is gated on organic idleness (N hours with no slots busy) on a fleet that runs near saturation, do reserve the window instead — hold new work so slots drain into the job, cap the wait (after which it runs wi… (fonte: P3-356)
- When a loop tick overwrites a rolling anchor (windowSince) from a per-tick decision, do capture the previous tick's value before the update if later consumers on the same cycle (a skip record written after the window closed) need to report… (fonte: P3-356)
- When a client API answers HTTP 200 with the real outcome in the body (e.g. `{delivered}`), do check the body verdict and set the client timeout above the server's measured worst-case (a full opencode turn >120s) — an unconditional 200 plus… (fonte: P3-357)
- When a delivery attempt ends with an UNKNOWN outcome (timeout/abort while the server may still deliver), do drop it instead of queueing a replay — park only definitive failures (HTTP status, socket, `delivered=false`) so retries never dupl… (fonte: P3-357)
- When a file-backed JSONL queue is read-rewritten from concurrent async paths (a park landing mid-flush), do serialize every mutation behind one promise-chain lock or the flush's final rewrite erases the fresh entry and overlapping flushes… (fonte: P3-357)
- When an e2e probe must assert an error state is absent, give the error element a dedicated class (e.g. `.artifacts-error`) to query as a stable hook, and strip escaped quotes from the IPC stdout before regex-matching (fonte: P3-327)
- When parameterizing a git base branch, thread the config through every `origin/<ref>` consumer in the same change — queue reads, workspace sync, PR `--base`, deploy history walks, doctor resets — and keep the local branch canonical (`main`… (fonte: P3-358)
- When a shared commit/push helper gains a new option (e.g. `seedSkeleton`, `baseBranch`), grep and update ALL its callers in the same task — aux agents that duplicate the call pattern (researcher, refills, blockAndPush) are the ones silentl… (fonte: P3-358)
- When a dispatcher guard can only be "tested" by grepping source text, extract the pre-conditions into a pure decision function (like `deploySkipReason`) so the battery proves the refusal behaviorally instead of asserting on the source (fonte: P3-358)
- When a push feeds both a passive indicator (badge) and a one-shot interruption (native notification), do sanitize stricter for the interruption — a malformed count that floors for the badge (fractional, negative, non-numeric) must fail clo… (fonte: P2-326)
- When decision logic lives inside an Electron main-process handler, do extract it as a pure module (no electron, no node:fs, no timers, no I/O) returning a closed-set verdict, and prove purity and wiring order with source-reading tests that… (fonte: P2-326)
- When a feature must never show a native artifact on the operator's screen during hermetic test sessions, do consult the session rule FIRST before constructing the object (write the order contract into the module header) and have e2e assert… (fonte: P2-326)
- When adding a confirmation/status string that renders in a card alongside an existing hint, re-read all copy visible in that post-action state together and give each line a distinct job (promise vs. consequence vs. action) — paraphrased du… (fonte: P3-388)
- When changing the meaning of an i18n key, update every locale in `dict` in the same commit with semantically equivalent copy, and note the pairing rationale (e.g. "hint carries the promise, confirmation carries the meaning") in a comment s… (fonte: P3-388)
- When touching the set of panes reachable at the pairing gate (GATE_SHELL_PANES / the gate rail in App.tsx), update the PaneMap.tsx rows in the same change — the map is a hand-maintained mirror of gate behavior and silently under-reports wh… (fonte: P3-389)
- When changing PaneMap's row list, update the structural assertions pinned in scripts/unit.test.ts — both the rowKeys/descKeys arrays and the literal occurrence counts (`locked: true` === 1, `locked: !reachable` === N) — the battery fails o… (fonte: P3-389)
- When adding a user-facing UI string, add the i18n key to BOTH the en and pt-BR dicts in apps/web/src/lib/i18n.ts in the same commit, and mirror the feature description in README.md plus docs/PRODUCT.md's gate section (fonte: P3-389)
