# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When a shell reuses an external (adopted) daemon it did not spawn, do not apply the owned-child respawn budget/give-up semantics — only your own children can exhaust a budget; for adopted ones probe health forever with capped backoff and s… (fonte: P1-053)
- When arming a self-scheduling async watchdog, make start/stop idempotent, stop it before intentional stops or fresh spawns, and re-check the armed flag after every await — an in-flight probe must never schedule a timer or mutate state for… (fonte: P1-053)
- When recovery from a daemon outage must not require re-pairing, never rewrite the 0600 state file or allowlist during reconnect, keep the degraded state's uri null so no QR overlay can open from it, and re-run auto-pair once when the banne… (fonte: P1-053)
- When you see a CSS var used with a hardcoded fallback (e.g. `var(--panel, #101014)`), treat it as a used-but-undefined token: define it in `:root` for every theme and delete the fallbacks, because theme-specific fallbacks silently break th… (fonte: P2-055)
- When replacing emoji-as-icons with a real icon set, centralize everything in one icons module with a shared base SVG wrapper (same viewBox, 2px stroke, `currentColor`, `aria-hidden`) and a typed component per icon, and remove the old emoji… (fonte: P2-055)
- When subscribing a status/event callback after an async `connect()` resolves, sync the current state explicitly right after subscribing (`setStatus(client.status)`), since the last event may have fired before the handler existed and the in… (fonte: P2-055)
- When adding automated `git commit`/`git push` in pilot code, do retry the push with a sleep between attempts (extract a helper with injectable exec/sleep like `commitAndPushFindings`) because concurrent researcher/scribe/explorer pushes mo… (fonte: P3-052)
- When unit-testing shell-out helpers, do inject `exec`/`sleep`/`exists` as an interface so push-failure/exit-code semantics are testable with fakes, and add a real-git smoke test against a bare remote whose commit message contains an apostr… (fonte: P3-052)
- When adding a long-running inline agent pass to the scheduler loop, do feed `touchHeartbeat()` from the agent's stdout callback and persist its once-per-day state guard before the run starts, so the self-watchdog doesn't kill the pilot mid… (fonte: P3-052)
- When adding state preservation across attempts (e.g., keeping a task branch between runs), audit every pre-existing reset/fallback path that touches that state — a reset to origin/main that was safe on a fresh workspace destroys preserved… (fonte: P1-060)
- When parsing multiple trailing metadata tags from a backlog line, strip them right-to-left in a loop anchored at end-of-line so tags work in any order and a tag mentioned mid-spec stays part of the spec text (fonte: P1-060)
- When reviewing long-horizon work, review the incremental diff since a round checkpoint instead of the truncated whole-branch diff, and re-validate any sha/id loaded from a file outside the repo against a strict charset (e.g. /^[0-9a-f]{7,4… (fonte: P1-060)
- When adding an escalation/arbitration layer (stronger reviewer) that can reject a build, union its verified kept findings with the earlier rounds' verified kept findings (dedup) before feeding the builder — an arbiter must add evidence, ne… (fonte: P1-059)
- When spawning a non-streaming CLI (e.g. `claude -p` emits only the final answer), do not depend on stdout activity for liveness — feed self-watchdog heartbeats on a timer, treat the run as valid only if exit 0 + non-empty output + expected… (fonte: P1-059)
- When capping a directory listing into an LLM prompt, sort by mtime descending BEFORE slicing — `readdirSync` order is arbitrary and a raw cap silently drops the newest carryover evidence (fonte: P1-059)
- When a server hosts sessions over multiple transports (e.g., relay + direct loopback WS), do scope connection-loss teardown to the failed transport's sockets (match `session.socket === ws`) instead of clearing the whole session map — clear… (fonte: P1-061)
- When an expiry/stale sweep keys on `lastSeen`, do refresh that timestamp on every liveness signal including pings — otherwise broadcast() skips an open, heartbeating socket and the client stays "paired" but blind, never triggering a reconn… (fonte: P1-061)
- When adding a new dial/failover path to a client, do write an integration test that drives the real `connect()` with the credential provider stubbed, not just unit-pinned pure helpers — silent-socket bugs (e.g., missing send-on-open → 3s t… (fonte: P1-061)
- When a viewer component must render both as a fixed full-screen overlay and embedded inside a flex pane, add a `variant` prop that switches only the positioning styles instead of forking the component, and gate the two render sites with co… (fonte: P2-062)
- When UI must switch layout at a viewport threshold, initialize state from `window.innerWidth` and keep it live with `matchMedia(min-width).addEventListener("change", ...)` — a one-shot check or ad-hoc resize handler goes stale when the win… (fonte: P2-062)
- When wrapping an existing max-width reading column into a flex row, explicitly override the parent constraint (`max-width: none`, including scoped variants like `.desk-main .screen`) and set `min-width: 0`/`min-height: 0` on the flex child… (fonte: P2-062)
- When an agent ingests untrusted external content (fetched web pages, pushed text) on the host, do not run it with shell or edit permissions — run it with a bash/edit/external_directory:deny sandbox, make it emit proposals as plain text bet… (fonte: P1-057)
- When a runner commits or pushes on behalf of an agent, do not push blindly — gate the push on git diff --name-only origin/main...HEAD matching exactly one allowlisted path, re-read the guard from the real branch diff on every retry, and ne… (fonte: P1-057)
- When serving an authenticated dashboard, do not embed the apiToken in the rendered HTML — have the browser exchange the Bearer token once for a short-lived HttpOnly SameSite=Strict session cookie, keep sessions memory-only so a daemon rest… (fonte: P1-057)
- When recording a success marker after an async/queued operation (`gh pr merge --auto` returns success while the squash is still queued), capture the pre-op HEAD first and only record the post-op state if HEAD actually moved past it AND car… (fonte: P2-058)
- When a best-effort write underpins a safety guarantee (quarantine, allowlist, lock), treat its failure as a degraded guarantee: escalate via the human-notify channel in addition to logging, so the missing protection is visible instead of s… (fonte: P2-058)
- When gating an action on state files (verified merges, quarantine), resolve the target by walking first-parent history to the newest valid non-banned entry (skipping bookkeeping commits, fail-closed when none) and re-validate the concrete… (fonte: P2-058)
- When uma navegação de UI for controlada por booleanos independentes (settings/filesView/artifactsView...), unifique-os num único reducer com view stack e derive o estado ativo via helpers puros (topSlot/activeSlots/isPaneOpen) — estados im… (fonte: P1-046)
- When adicionar atalhos de teclado num shell Electron, conecte os accelerators do Menu no main via IPC com action ids string (`ocr:menu-action`) e só registre o fallback keydown no renderer quando o bridge de preload estiver ausente — dentr… (fonte: P1-046)
- When abrir um pane puder destruir estado vivo (chat aberto, página do browser, input digitado), mantenha a superfície montada — coluna de chat dedicada + pane com `display:none` para o browser — em vez de renderização condicional; remontar… (fonte: P1-046)
- When parsing verdict/status markers out of free-text agent output, never use a substring test like `/VERDICT:\s*APPROVE/i` — markers quoted in prose or examples mask the real one; collect all matches, let the LAST one decide (`parseVerdict… (fonte: P2-038)
- When a verifier classifies LLM findings as hallucinated, respect category boundaries: a code observation has no stdout, so verify it deterministically instead of dropping — cited `file:line` must exist in the workspace and every quoted sym… (fonte: P2-038)
- When writing regexes for file citations, add a negative lookahead after the optional extension (`(?!\w)`) so `.tsx` is not read as `.ts` (this silent truncation dropped 7 valid findings), and resolve bare-name citations like `CommandPalett… (fonte: P2-038)
- When building CLI tools that shell out to optional binaries (e.g. converters), resolve candidates from both PATH and known install locations (like /Applications/LibreOffice.app/Contents/MacOS/soffice) and keep the selection/validation logi… (fonte: P2-065)
- When a task touches UI, cite the 1440x900 (and 390px) screenshot paths in the EVIDENCE block alongside typecheck/test output — the deterministic gatekeeper rejects UI tasks whose evidence lacks shot paths, even with green builds. (fonte: P2-065)
- When a UI has mutually exclusive panes, replace the boolean-flag patchwork with a single reducer-owned view stack: parallel booleans (session/settings/filesView/…) produce double-active buttons, stale panes, and chat-destroying navigation… (fonte: P2-065)
- When bumping Electron (even patch-level), verify the packaged artifact's real runtime version (e.g. CFBundleVersion in Electron Framework.framework/Info.plist after `dist --dir`) before running the validation battery, so gates exercise wha… (fonte: P2-066)
- When an update/e2e probe logs errors, classify them as test-fixture artifacts vs regressions by checking the decision line and downstream behavior: a self-pointing feed URL yields a log-only `ditto: Couldn't read PKZip signature` while the… (fonte: P2-066)
- When auditing breaking changes from a runtime major, grep the codebase for each affected API and record per-change impact against actual usage (none — renderer uses navigator.clipboard, only openAtLogin is passed), and split bundle-size de… (fonte: P2-066)
- When you find `var(--token, <literal>)` fallbacks in themed UI, remove them and define the token in both theme blocks — a fallback silently masks an undefined token and pins the wrong color in the other theme instead of surfacing the bug (fonte: P1-047)
- When restyling components you don't own via a global stylesheet, replace `!important` + `nth-child` positional selectors by adding real semantic class hooks to the markup — positional patchwork breaks silently whenever the component's DOM… (fonte: P1-047)
- When theme logic follows the OS through `prefers-color-scheme`, re-applying the theme must remove the previous `matchMedia` change listener before registering a new one, or toggling themes leaks listeners and fires duplicate updates (fonte: P1-047)
- When a value extracted from agent-adjacent logs (stdout bridges like `--print-logs`) feeds shell or AppleScript interpolation, do treat it as hostile input and validate structurally — strict charset allowlist plus anchoring to the expected… (fonte: P2-048)
- When JSON-parsing a request body inside a long-running daemon route, do wrap the parse in try/catch and return 400 — one malformed POST must never become an unhandled rejection that can take the whole process down. (fonte: P2-048)
- When hardening a sink that consumes untrusted values, do validate or drop the same value at every consumer, including success responses echoed back to the renderer — returning log-derived fields unvetted keeps the broken trust boundary ali… (fonte: P2-048)
- When adding follow-tail autoscroll to a streaming chat, gate the `scrollIntoView` effect on an "at bottom" flag kept in a ref (read inside the effect to dodge stale closures) mirrored by state for the ↓ jump pill, use a px threshold (e.g.… (fonte: P2-049)
- When writing e2e/desktop-flow assertions that grep JSON-stringified IPC output, match the actual user-visible copy for every locale (e.g. `/Invalid pairing code|Código de pareamento inválido/`) instead of a generic non-empty-quoted-string… (fonte: P2-049)
- When converting inline overlay divs into accessible modals, capture `document.activeElement` as the trigger before focusing the dialog, restore it in the effect cleanup, wire Esc via a document keydown listener, trap Tab by cycling first/l… (fonte: P2-049)
- When running long synchronous work (exec, CPU-bound loops) in a process that shares a watchdog/heartbeat timer, touch the heartbeat on BOTH sides of the blocking call or make it async — a blocking run within its own allowed timeout can sta… (fonte: P1-044)
- When a module exposes an injectable event sink (emitEvent/onEvent pattern) for testability, route ALL new events through it, never the raw global emit — otherwise the eval battery silently loses the ability to pin the new behavior. (fonte: P1-044)
- When new control flow lives inside an existing loop (sliding windows, modulo scheduling, multiple rollback branches), extract the loop into a function with injectable probe/clock/sleep/event deps and pin the wiring in tests — pinning only… (fonte: P1-044)
- When detecting "did the state change" (e.g., self-reload after deploy), capture the pre-mutation value (`prev`) before any reset/checkout and compare against it afterward — a `git diff <sha>..HEAD` computed after resetting to that sha comp… (fonte: P1-034)
- When comparing git ids, normalize both sides to full ids (post-reset `git rev-parse HEAD`) before string comparison and validate them with SHA_RE — abbreviated vs full 40-hex ids false-positive against each other, and empty/malformed ids m… (fonte: P1-034)
- When fixing a silent no-op condition, extract the decision into a pure exported function, unit-test the exact failure case ("HEAD moved but diff empty") and pin the broken pattern with a source-level grep test so it cannot regress (fonte: P1-034)
- When a staleness-based watchdog can kill the process during long awaits of aux agents, feed the heartbeat with a periodic timer for the duration of the await instead of pausing/disabling the watchdog — pausing blinds it to real event-loop… (fonte: P1-035)
- When wiring liveness signals around spawned children, never feed the watchdog from stdout callbacks: a healthy-but-silent agent emits no chunks and starves the check; arm an independent setInterval at spawn and stop it on every termination… (fonte: P1-035)
- When adding timers that write shared process state (heartbeat files, locks), return an idempotent stop function from the arming helper and make the touch callback + interval injectable so unit tests are hermetic and never touch the real st… (fonte: P1-035)
- When core decision logic lives inline inside a large orchestrator that spawns LLM agents (untestable by design), extract it verbatim into an exported pure-ish function and rewire the call site — a byte-for-byte testability refactor unlocks… (fonte: P1-036)
- When testing git-dependent logic, don't mock git: build scratch repos (mkdtemp + bare origin + clone), run the real function, and assert on concrete SHAs — that the preserved commit still resolves (`cat-file -e <sha>^{commit}`) and that di… (fonte: P1-036)
- When recreating/resetting state from scratch (e.g., force-recreating a branch), also clear derived artifacts tied to the old state (checkpoint files, caches) in the same path, or stale metadata like a saved diff range can resurrect already… (fonte: P1-036)
- When a generated artifact (refill lines, lessons, findings) is landed via commit+push inside a slot worktree, persist it to a store outside every worktree (e.g. `~/.opencode-remote/pilot/`) the moment the push fails — a failed `appendCommi… (fonte: P1-037)
- When building a reland/retry loop for pending data, pre-filter ids against `origin/main` before pushing and map "everything already landed" to a terminal state that clears the store — only transient `failed` may keep the file for retry; a… (fonte: P1-037)
- When adding durable state, use the failureLessons pattern — pure helpers + fs wrappers with an injectable path and atomic tmp+rename writes, corrupt reads return `null` not throw — and test survival with a real bare-remote clone plus fake… (fonte: P1-037)
- When caching command execution results across pipeline phases in a multi-slot setup, key the cache by (command, cwd) with a separator that cannot appear in either field — a command-only key silently aliases results between workspace clones (fonte: P2-040)
- When a shared execution cache spans phases (preflight, evidence re-run, gate steps), create the map in the orchestrator (round loop) and pass it down as a parameter; components that instantiate their own cache can never share entries (fonte: P2-040)
- When caching results of deterministic commands, cache failures too and recreate the cache at every boundary where the code may have changed (new builder round) — caching only successes re-rolls deterministic failures, and caching across ro… (fonte: P2-040)
- When adding a recovery path (rollback, restart, failover), do not end it with a blind sleep — poll the health endpoint in a bounded, constant-documented window and surface the verdict (structured log, feed event, UI alert, supervisor escal… (fonte: P2-041)
- When a function mixes logic with side effects (probing, clock, event emission, notifications), do extract a hooks object with injectable overrides and production defaults so tests run hermetically without real fetches or timers (fonte: P2-041)
- When deriving alert state from an event feed, do scan the full history for the newest relevant verdict (a healthy verdict or later clean completion clears older alerts) instead of a truncated tail, and pin the wiring with a source-level un… (fonte: P2-041)
- When adding a context block (lessons, failure records) to a new agent prompt, grep docs for role enumerations ("builder e strategist"-style claims) and update all of them (AGENTS.md, README, templates) in the same change — stale role lists… (fonte: P2-042)
- When a prompt function gains new context, use optional defaulted params (e.g., `lessons: string[] = []`, empty block default) so no-match callsites keep the prompt clean, and add tests asserting both presence (injected) and absence (empty)… (fonte: P2-042)
- When several prompts need the same distilled context, reuse the existing selectors and formatters (pickRelevantLessons, failureLessonsBlock) instead of re-implementing keyword matching — divergent selection logic makes different roles act… (fonte: P2-042)
- When skipping a slow idempotent step (npm ci, build, migration) because "input unchanged", decide from a persisted record of the last *successful* run (hash file), never from an intra-run before/after comparison — only the persisted marker… (fonte: P1-021)
- When an error/rollback path also installs dependencies, re-record the success marker only if that install verifiably succeeded, keeping the invariant "saved hash == what's on disk"; leave state stale on failure and let a repair ladder (fas… (fonte: P1-021)
- When command flags encode security requirements (--ignore-scripts, ELECTRON_CACHE), export the command as one named literal and pin it by exact string in the eval battery — duplicating the command inline at each call site is how supply-cha… (fonte: P1-021)
- When querying a SQLite database owned by a live process (e.g. opencode.db during reconciliation), open it with `-readonly` (or `file:…?mode=ro`) even for pure SELECTs, because a plain connection can still write WAL/journal files against th… (fonte: P2-028)
- When reconciling a stored total against a source that grows over time (resumed sessions, retried attempts), recompute it REPLACE-style from the full deduplicated id list persisted beside the total, never ADD deltas — and on a transient DB… (fonte: P2-028)
- When capturing ids from agent stdout that will later be interpolated into SQL, anchor the capture regex with a lookbehind so glued prose (`mytask_ses_…`) never matches, enforce a strict charset guard before inlining, send the query via std… (fonte: P2-028)
- When retrying a pilot task after a failed/crashed attempt, don't `git checkout -b pilot/<ID>` blindly — the branch may be preserved by design from the previous attempt, so use `git checkout -B` (or check `git rev-parse --verify` and reuse)… (fonte: P3-010)
- When writing a Node CLI script that needs unit tests, export the pure logic (`listProblems`, `resolveBundleDir`) and guard `main()` behind `import.meta.url === pathToFileURL(process.argv[1]).href` so the test can import the module without… (fonte: P3-010)
- When validating electron-builder output, remember layouts vary per platform — mac `.app` puts resources in `Contents/Resources` with the binary in `Contents/MacOS/*`, win/linux `-unpacked` use `resources/` at root with `*.exe` / mode-bit e… (fonte: P3-010)
- When a WebSocket server shares its http server (ws upgrade path), do not call server.closeAllConnections() during shutdown — send close(1001) per ws client first, use server.close() to refuse new admissions, wait ~250ms so close frames rea… (fonte: P2-023)
- When implementing shutdown/drain logic, extract it to a pure module with injectable sinks (log, exit, setTimeout/clearTimeout) so unit tests can simulate hung drains, forced exits and double signals with fake timers without killing the tes… (fonte: P2-023)
- When starting a task branch, first check whether `pilot/<ID>` already exists from a prior attempt and reuse/checkout it — unconditional `git checkout -b pilot/<ID>` fails with status 128 "branch already exists" and crashes the pipeline (fonte: P2-023)
- When persisting state whose truncation silently resets counters (circuit breakers, attempt trackers), do write to `<file>.tmp` then renameSync over the destination — rename is atomic within the same filesystem — and on any failure unlink t… (fonte: P2-024)
- When adding fs-touching helpers that need unit tests, do take the fs surface as an injectable parameter (interface with default real impl, P3-013 pattern) and assert the tmp+rename+cleanup contract against an in-memory mock, plus one real… (fonte: P2-024)
- When starting work on task `<ID>` whose `pilot/<ID>` branch survived a failed attempt, do reuse the existing branch (`git checkout pilot/<ID>`) instead of `git checkout -b pilot/<ID>`, which exits 128 "branch already exists" and crashed th… (fonte: P2-024)
- When adding Electron-coupled behavior (tray items, shell opens, dialogs), extract the decision logic into pure helpers in tray.ts-style modules (no electron imports) with fs/shell passed in as injectable sinks, so unit tests cover every pa… (fonte: P3-016)
- When a tray/menu action must be best-effort, catch all fs/shell failures inside the helper and resolve `false` instead of rejecting (always mkdir with `{recursive:true}` so an existing or deleted dir never throws); the caller logs the fail… (fonte: P3-016)
- When (re)creating a task branch on a retry attempt, reuse the existing branch idempotently (`git checkout -B pilot/<ID>` or a `rev-parse --verify` guard) because branch preservation after failed attempts makes plain `git checkout -b` fail… (fonte: P3-016)
- When keying any per-IP limit, allowlist, or bucket on `req.socket.remoteAddress`, do normalize first — unmask IPv4-mapped `::ffff:a.b.c.d` (dotted or hex tail) to plain IPv4 and aggregate real IPv6 by its /64 prefix (first 4 hextets, lower… (fonte: P2-026)
- When a normalized key feeds both admission and release paths, do normalize exactly once at the single ingestion point and stash the key (e.g. on the socket) so every consumer acts on the identical bucket — normalizing at each call site ris… (fonte: P2-026)
- When writing parsers for untrusted address strings in an admission path, do return a safe fallback (lowercased raw input, never throw) on unparseable values so the control degrades to previous behavior instead of crashing the accept loop (fonte: P2-026)
- When spawning child processes in the packaged Electron desktop app, never rely on `stdio: "inherit"` or forwarded `process.stdout` — pipe stdout AND stderr and tee every chunk to a rotating file under `userData/logs/`, because the packaged… (fonte: P3-018)
- When writing desktop log/IO helper modules, follow the desktop-log.ts contract: inject the fs subset (`LogFs`) and never import `electron` so unit tests run pure against a temp dir; wrap every fs op and route failures to a guarded `onFailu… (fonte: P3-018)
- When a module exposes a process-wide default via init + accessor (like `initSidecarLog`/`teeSidecarChunk`), install it at the top of main.ts before any `spawnChild` and make the accessor a silent no-op before init — late installation loses… (fonte: P3-018)
- When adding a CLI subcommand that mutates shared state (circuit-breaker counters, state.json), do make the no-flag form report-only and reject bare destructive flags (`--clear` without an id is an error, never clear-all), table-testing eve… (fonte: P1-030)
- When repair code shells out using git refnames or task ids (gh probes, `git branch -D`), do validate each refname against the strict `pilot/<TASK_ID>` shape before any shell use, treat an external-probe failure as "skip", and set ok:false… (fonte: P1-030)
- When making a file repair idempotent under a tolerant loader (loadState masks corruption with fresh defaults), do compare the normalized result against the raw file text, not the parsed/reloaded object, so a corrupt file is actually rewrit… (fonte: P1-030)
- When a finding cites multiple files and quotes symbols, validate symbols against the union of all cited files' contents plus the diff, never per-citation — per-citation `every()` checks drop valid cross-file findings (fonte: P1-065)
- When a verifier handles findings of absence ("X has no coverage of Y"), the quoted symbol by definition won't fully appear in the cited file, so require resolution via the citation union rather than failing the check (fonte: P1-065)
- When an "all symbols must match" rule would drop findings mixing real and fake references, add a lenient fallback tier (≥1 quoted span of ≥6 chars matches the union) with a minimum-length guard so short spans never trigger false keeps (fonte: P1-065)
- When changing a feature's state-machine semantics (e.g., update flow becomes check→download→consent→install), grep every user-visible string that mirrors the old flow (tray labels, dialogs) and reword it in the same PR — stale copy like "r… (fonte: P1-050)
- When an HTTP route serves potentially large files on a process that shares its event loop with other duties (e.g., daemon's metrics server also carrying relay WS + /api/*), stream with `await stat()` for content-length plus `createReadStre… (fonte: P1-050)
- When a route must stay unauthenticated because a client can't attach tokens (e.g., electron-updater), contain the blast radius instead of loosening global auth: dedicated folder, per-segment charset validation, extension allowlist, and a r… (fonte: P1-050)
- When a global breaker (pause queue, halt deploys, throttle) fires on a sliding window of events, aggregate failures by the responsible entity id before counting — raw event counts let one stubborn entity (task retrying through maxAttemptsP… (fonte: P2-063)
- When adding an attribution field to persisted state samples, make the aggregation tolerate id-less entries (pipeline crashes, legacy pre-migration samples) via a synthetic per-event key (e.g., `?${at}`) so unattributed failures still count… (fonte: P2-063)
- When changing a trigger's semantics from a rate to a distinct count, re-audit invariants tied to the old metric — the "partial window never trips" guard existed because small-sample rates are noisy; a distinct-entity threshold is meaningfu… (fonte: P2-063)
- When serving paged responses through a frame-limited relay, size each page in exact UTF-8 bytes of the serialized body (Buffer.byteLength — CJK/emoji cost 3-4 bytes per char, so message counts lie), shrink by halving the limit, and degrade… (fonte: P1-064)
- When adding new behavior to an existing endpoint, gate it on new query params and keep the no-param response shape byte-identical (a plain array stays a plain array) — export, handoff and internal syncs depend on the legacy contract, so th… (fonte: P1-064)
- When testing size caps or paging contracts, assert against the body the daemon actually serves (capture the sealed op body or the audit envelope's byte count), never a pre-built synthetic array — a test on fixture data passes while the rea… (fonte: P1-064)
- When reaping WebSocket peers that vanished without a close frame, do terminate via socket.terminate() so the normal close handlers release rooms and per-IP slots — never duplicate cleanup logic in the reaper, or releases will drift from th… (fonte: P2-067)
- When adding time-based policy to the relay, do extract the decision as a pure helper with no ws/node imports (padrão ipcap.ts/ratelimit.ts) that receives `now` and returns victims — and never sweep entries whose lifecycle isn't stamped yet… (fonte: P2-067)
- When testing ws liveness/reaping end-to-end, do remember ws clients auto-pong by default — open the doomed peer with `{ autoPong: false }`, run the relay with a 1s RELAY_PING_INTERVAL_S via env for speed, and assert the reaped socket's clo… (fonte: P2-067)
- When detecting "new message arrived" in a list that gets refetched or has older pages prepended, compare first-element identity plus growth from a non-empty previous anchor (prev.len > 0 && length grew && bubbles[0] unchanged) — length che… (fonte: P3-053)
- When adding UI-derived state like unread counters, implement it as a pure reducer in a lib file (no DOM/Electron imports) so unit tests drive every transition, and confine the side effect (bridge push) to a single function that swallows fa… (fonte: P3-053)
- When main process mirrors renderer-pushed state for OS effects (dock badge), store the last pushed value as the verification surface instead of reading back from the OS, normalize hostile IPC payloads (non-finite/negative → 0), and clear t… (fonte: P3-053)
- When injecting prompt context into opencode sessions, remember `SessionPromptData.system` is per-turn — do inject on every message POST (idempotent via a unique dedupe marker) and audit every creation path (proxy tunnel, /api, scheduled ro… (fonte: P1-068)
- When a guarantee relies on an in-memory registry (Set cleared on restart), do not write docs claiming "every session" — document the restart caveat explicitly or reviewers will flag the README as overstating behavior (fonte: P1-068)
- When an acceptance criterion requires live/manual E2E evidence (e.g., a shot in `shots/builder/`), do actually run the live eval and land the file before merging — green typecheck/unit does not satisfy the AC and the reviewer will reject t… (fonte: P1-068)
- When gating side effects on event role/source (auto-open panes, notifications), do fail closed with an explicit `=== "assistant"` check — never `?? "assistant"`, which fails open and lets user-echoed text (parts can arrive with evicted/mis… (fonte: P1-072)
- When reading session identity from opencode SDK events, do derive it from the part payload (`part.sessionID`, guaranteed by installed types like `TextPart`) and fall back to top-level `properties.sessionID` — top-level-only reads silently… (fonte: P1-072)
- When adding logic to `apps/daemon/src/index.ts` (runs `main()` on import, so inline stream-wiring has zero unit coverage), do extract pure helpers into their own module (pattern: `preview.ts`, `localws.ts`) and pin semantics there with tes… (fonte: P1-072)
- When simulating a first boot in a session-keyed harness (OCR_DESKTOP_SESSION), derive a never-used session name from the date (digits-only, path-safe) so the keeper spawns fresh and hermeticEnv mints a virgin userData — and if `open` repor… (fonte: P1-071)
- When capturing an app's untouched first screen, make the launch command itself take the screenshot (open with a shot argument) — a standalone shot before the keeper/process is up fails. (fonte: P1-071)
- When adding a judgment rule to the constitution, update docs/CONSTITUTION.md and the `CONSTITUTION` prompt constant in lockstep, pin both in unit tests, and prefix the commit body with `constitution-change:` — touching only the docs file i… (fonte: P1-071)
- When a production origin (PWA/mobile endpoint) is at stake, never back it with a dev server — serve the built `dist` with a dependency-free static server under a supervisor (launchd + KeepAlive) so it survives reboots and shell death (fonte: P2-075)
- When a failure is only visible on one client (phone sees white screen because desktop loads the bundle from disk), add a `/healthz` probe plus a dashboard event/chip so the downtime surfaces within minutes instead of hours (fonte: P2-075)
- When adding a new service to the deploy pipeline that may not exist on every host, make its restart best-effort (`allowFail: true`) and emit a deploy event instead of failing — KeepAlive plus per-request static reads mean a skipped kicksta… (fonte: P2-075)
- When splitting failure handling into infra vs merit paths, bias the classifier's false-positive direction toward infra (the retry is free: no attempt burned, no block can land) and pin merit negatives (gatekeeper rejection, empty diff, rev… (fonte: P1-074)
- When a failure detail must be classified downstream, emit a unique tagged marker at the failure site (e.g. `[infra] builder timed out without output`) instead of inferring from log tails — and only for the unambiguous case: a timeout with… (fonte: P1-074)
- When adding an optional counter to PilotState, backfill it in loadState in both branches (same-day and date-rollover) so legacy state.json files never yield undefined/NaN — reuse the existing `merges` field pattern. (fonte: P1-074)
- When adding another read of the daemon state file (or any 0600 credential file), resolve the path through the existing shared helper (stateFilePath()/stateFile()) instead of re-deriving `join(homedir(), ...)`, so test-only env overrides (O… (fonte: P1-070)
- When a boot-time mode decision replaces a legacy UI flow (local auto-connect vs QR ceremony), stamp the mode into every degraded/intermediate state (daemon-down, reconnecting) and gate legacy fetches behind it, so a failed poll can never r… (fonte: P1-070)
- When an async bridge value (pairing state) feeds a mount-time auto-connect, mirror it into a sync ref updated in the same `.then()` and re-run the connect attempt when a state with the expected mode lands, because the effect's first run ra… (fonte: P1-070)
- When building LLM prompts for a provider with prefix caching, do assemble a byte-identical stable prefix first (role, rules, constitution, output contract using generic `<TASK-ID>` placeholders instead of interpolated ids) and the variable… (fonte: P1-077)
- When extending an existing totals-only token/cost store into a multi-field breakdown, do make the row-level query the core and reimplement the old totals function as a thin wrapper, accept both rows and legacy numeric injectors at the fold… (fonte: P1-077)
- When adding a new optional field to persisted state or a derived metric, do normalize/backfill it in BOTH `loadState` and `normalizePilotState` (legacy files must become `{}`, never crash) and guard derived ratios with a zero-denominator c… (fonte: P1-077)
- When fixing horizontal overflow in a flex layout, do apply `min-width: 0` (and `max-width: 100%`) on EVERY flex item along the row-axis chain down to the bubble — flex items default to `min-width: auto`, so a single wide child (long code l… (fonte: P1-080)
- When making pre/code blocks scroll horizontally, do pair `overflow-x: auto` with `max-width: 100%` + `min-width: 0` on the block itself — `overflow-x: auto` alone lets the block grow to its content width, so the page scrolls instead of the… (fonte: P1-080)
- When asserting layout geometry in desktop-flow tests, do freeze CSS animations/transitions (inject a `* { animation: none !important; transition: none !important }` style) before reading `getBoundingClientRect` — entrance animations like a… (fonte: P1-080)
- When rendering actionable UI (approval cards, confirm dialogs) from a WS event buffer, always reconcile against the server's current state (its pending list endpoint) as source of truth and use events only to trigger debounced re-fetches —… (fonte: P1-082)
- When the same logical request can emit multiple events, dedupe by its stable ID in a Map with last-occurrence-wins, normalizing legacy field variants (id vs permissionID) before keying, or N duplicates render N identical cards (fonte: P1-082)
- When a POST can hit a resource resolved concurrently (AutoMode, other device), map the 404 to a friendly "already resolved" inline note that also cleans local state instead of surfacing the raw error (fonte: P1-082)
- When writing orphan/cleanup killers that match processes by argv markers, make the kill decision symmetric — require an env second factor (e.g. test marker keys or HOME under `<tmpdir>/ocr-*`) for EVERY marker, never argv substring alone,… (fonte: P1-081)
- When parsing darwin `ps -wwE` output, argv and env share one line, so any argv token shaped `KEY=VALUE` (or a temp path in the arguments) gets absorbed as an env entry — read the plain command first and parse only the env suffix after it,… (fonte: P1-081)
- When booting test servers, bind port 0 and resolve the port from `AddressInfo.port` instead of fixed ports (zombies from previous runs hold them), and only trust a readiness probe while the child is still alive — a signal death (`signalCod… (fonte: P1-081)
- When a component is mounted persistently across entity switches (ChatView is deliberately not keyed by sessionId), do not assume its useState resets — restore state keyed by the active id via useState(() => get(sessionId)) plus a [sessionI… (fonte: P1-088)
- When an async callback (voice transcription, video note) mutates state that survives navigation, do pin the origin id at action start and write through the per-id store, touching the visible input only when origin === current id — never ca… (fonte: P1-088)
- When a state is easy for future edits to corrupt (user input living near stream/event effects), do declare a single-writer invariant in a comment at the declaration and route every write through wrappers so rg 'setInput\(' lists only the d… (fonte: P1-088)
- When two independent sources populate the same UI list (history fetch replaces, live event stream appends), do all merges keyed by a stable per-item id (messageID) via a pure idempotent merge function instead of comparing the last item's t… (fonte: P1-089)
- When a component holds cross-session transient state (streaming tail refs/text) above a `[sessionId]` switch effect, do clear it inside that effect on every session change, or an idle-finalize racing the switch appends the previous session… (fonte: P1-089)
- When a hermetic test relaunches an app session dir, do keep the session id short (macOS AF_UNIX bind paths truncate at 104 chars — a truncated keeper.sock fails EADDRINUSE forever on relaunch) and wait for the keeper PROCESS to exit after… (fonte: P1-089)
- When backfilling a slot scheduler from multiple call sites (main loop + worker `finally`), keep the pick-and-start path fully synchronous (sync exec, no `await`) so event-loop atomicity guarantees each task is picked exactly once — an asyn… (fonte: P1-099)
- When removing a global serialization lock for parallelism, first confirm every resource it protected has become hermetic/instance-scoped (ephemeral ports, unique session ids, per-workspace state) — the lock was a symptom; delete it only wh… (fonte: P1-099)
- When running logic inside a `finally` (eager-fill, cleanup, bookkeeping), wrap it best-effort: try/catch with a warn log and skip on failure (e.g. queue read failure) so the hook never throws out of `finally` and the main loop's next cycle… (fonte: P1-099)
- When emitting one notification per real write from fs.watch, debounce each changed path until it settles and dedupe by a size:mtime fingerprint — chunked writes, edit-in-place edits and atomic renames all fire multiple/coalesced events — a… (fonte: P2-090)
- When validating paths derived from filesystem events, resolve them against the watched root, reject anything that escapes it, and lstat (never stat) the target so a symlink planted in the tree is not followed or announced as a real file (fonte: P2-090)
- When adding auto-open behavior to a pane, keep auto state separate from the manual pick (render `manual ?? auto`), track dismissed files so a closed pane is never re-opened, reset pending auto state on session switch, and declare the consu… (fonte: P2-090)
- When an effect must re-fire for the same logical value (e.g. re-opening the same artifact from a picker), do pass a fresh object identity (clone `{ ...meta }`) into the state setter — effect deps compare by reference, so reusing the old ob… (fonte: P2-091)
- When routing a selection from one surface (global Artifacts list) into another view's behavior, do funnel it through the destination's existing state path (its manual pick) instead of adding a parallel mechanism, so established invariants… (fonte: P2-091)
- When calling Electron `<webview>.loadURL()` inside an effect, do guard it in try/catch with a `src`-attribute fallback — after a remount (mobile⇄desk flip) the fresh webview isn't dom-ready yet and loadURL throws; the src attribute carries… (fonte: P2-091)
- When styling Electron `<webview>` elements, keep the host as a flex container: the shadow root lays its guest iframe with `flex: 1 1 auto` and no intrinsic height, so forcing `display: block` on the host collapses the guest to its 150px de… (fonte: P2-092)
- When a container can change size without a remount (maximize toggle, split resize, hidden⇄shown flips), attach a ResizeObserver to the frame and re-assert explicit px width/height on the guest view, skipping updates when the box is under 1… (fonte: P2-092)
- When asserting "content fills the pane" in flow tests, measure both the element bounding box and the guest viewport via executeJavaScript innerWidth/innerHeight, and re-measure after a width change — a correct element box can coexist with… (fonte: P2-092)
- When a mode flag suppresses UI affordances (e.g. AutoMode hiding approval cards), ensure every server-side failure path in that mode emits a client-visible event (not just a `log warn`) so the operator gets a manual fallback instead of a s… (fonte: P1-093)
- When event-sourced UI state uses "last occurrence wins" dedupe, wire new failure events so a later success event clears the failure flag and other-session events are ignored entirely — otherwise recovery and cross-session isolation break s… (fonte: P1-093)
- When adding retries to daemon-side best-effort calls, bound them tightly (2 attempts, ~500ms) and prove it in E2E with an always-rejecting fake backend plus an exact POST-count assertion — this catches unbounded retry loops without slowing… (fonte: P1-093)
- When classifying failure kinds from text that embeds logs or reviewer findings, do classify from a structured field set at the failure's producer site instead of substring-scanning detail text — a merit finding citing "ECONNREFUSED" must n… (fonte: P1-094)
- When reasoning that a classifier's false positive is harmless because "the retry is free", do check whether it bypasses termination mechanisms (attempt budgets, circuit breakers, fever windows) — spared accounting turns a benign FP into in… (fonte: P1-094)
- When a text marker exists only for observability (e.g. `[infra]` in detail), do keep it for log greppability but never let control flow depend on it — propagate an explicit typed flag through intermediate result objects (RunResult → Pipeli… (fonte: P1-094)
- When a periodic job's trigger combines a fixed wall-clock hour with a contested condition (e.g. all slots idle), do verify reachability with real timestamps (`git log`) before trusting the code — if pipelines routinely span that hour, repl… (fonte: P1-095)
- When a scheduled pass can be silently missed, do record an observable "skipped" event (deduped per day in state + emitted to the dashboard) and require run evidence (commit prefix in git log or event line in events.jsonl) before accepting… (fonte: P1-095)
- When extending a state schema that rolls over daily, do add new fields to the preserved `shared` block of the loader with tolerant parsing (garbage → null, non-finite → undefined), and early-return in one-shot (`--once`) mode before spawni… (fonte: P1-095)
- When a function pairs an idempotency "already recorded → no-op" short-circuit with a cleanup/invalidation branch over the same state key, order the freshness check BEFORE the short-circuit — the record can outlive the condition it recorded… (fonte: P2-100)
- When a side effect must accompany a state checkpoint (event append + "recorded" marker), emit the side effect FIRST and checkpoint only after it succeeds — checkpoint-then-emit turns a transient append failure into a permanently dead signa… (fonte: P2-100)
- When the spec pins a numeric contract (e.g. a 10s fetch timeout), export the constant and assert its value in tests — `signal instanceof AbortSignal` passes for any duration, leaving the spec'd number unverified. (fonte: P2-100)
- When a per-session value (id, path, timestamp) would land in a system prompt shared by all sessions, split stable from variable: keep the `system` block byte-identical across sessions so the provider prefix-cache survives, and ride the per… (fonte: P1-096)
- When injecting one-shot per-session data into message bodies, pair an in-memory "already told" Set with marker-based idempotence inside the helper, and fail-open on malformed bodies (no `parts` array, no text part) so a later turn retries… (fonte: P1-096)
- When pinning prompt-builder output in tests, assert the invariant itself (two sessions produce `system === system`, no per-session id/dir anywhere in the block) rather than concrete per-session values, or the test keeps blessing the bug it… (fonte: P1-096)
- When serving unbounded user files into RAM (especially base64 over a tunnel, +33% bloat), check `st.size` via lstat before `readFileSync` and refuse with a distinct failure reason (413) instead of a generic null/404 — and hide UI actions l… (fonte: P2-097)
- When sandboxing an iframe that renders a blob you created in the same page, use `sandbox="allow-same-origin"` — a bare `sandbox` attribute strips same-origin and breaks the browser's built-in PDF viewer, while still blocking scripts/forms/… (fonte: P2-097)
- When a service worker caches fetch responses, only `res.status === 200` may enter the cache (wrap the async `caches.put` in `event.waitUntil`), and version the cache name on every SW change — a constant cache name plus caching errors/redir… (fonte: P2-097)
- When an idempotent installer regenerates a launchd plist, do recover every previously-set env var from the existing plist when its env override is absent — recovering only the primary URL (RELAY_URL) while silently dropping secondary ones… (fonte: P2-098)
- When adding a fallback source to feed/URL resolution, do gate it to the packaged default only — an explicitly configured override (e.g. OCR_UPDATE_FEED) must surface feed-unreachable instead of silently producing an outbound request to a t… (fonte: P2-098)
- When packaging a DMG with electron-builder on dmg-builder >= 1.2.5, do omit the relative `path: "*.app"` glob in dmg contents (FileNotFoundError at dmg time) and set `name: "<App Name>.app"` so the built .app is sourced via its absolute ap… (fonte: P2-098)
- When giving an agent bash/edit permissions in a scratch workspace, never symlink the production node_modules into it — cpSync the full tree (as done for dist dirs) so a shared read-write dependency tree can't turn a tampered bin/postinstal… (fonte: P3-101)
- When interpolating values into execSync/shell commands, route every one through the existing quoting helper (shq) and only interpolate values that pass a strict format check (e.g., /^[0-9a-f]{40}$/ for shas) — raw `${...}` template strings… (fonte: P3-101)
- When a long-lived daemon must pick up new code, don't rely on deploy-time self-reload alone (processes spawned before the fix keep the dead path in memory) — pin boot HEAD at startup and at a fully idle moment (no slots running, no deploy… (fonte: P3-101)
- When building LLM agent prompts that should hit the provider prefix cache, assemble them stable-first — role/rules/contract byte-identical, all per-run variables (task text, ids, workspace paths, session env, lessons) after the last stable… (fonte: P1-078)
- When deferring a scheduled pick through a wait window (stagger/backoff), emit a distinct "staged" phase instead of announcing "started", and after the wait re-check only gates that can genuinely flip (frozen/audit) — never re-count budgets… (fonte: P1-078)
- When parallel workers share an account-level provider cache, route same-shape work to the worker that last ran that shape within the cache TTL and stagger simultaneous starts (~20s) so the first cache-write completes; record per-worker cac… (fonte: P1-078)
- When referencing a new CSS custom property in a stylesheet, grep the token file for its definition first — an undefined `var()` fails silently at computed-value time (declaration dropped, no build/test error), so hover affordances like bor… (fonte: P3-083)
- When adding reduced-motion support, remember the CSS media query does not cover JS scrolls: gate every `scrollIntoView({behavior:"smooth"})` behind `matchMedia("(prefers-reduced-motion: reduce)")` and add `scroll-behavior: auto !important`… (fonte: P3-083)
- When extracting literals into design tokens, sweep the whole file for pre-existing inline duplicates of the same category (font stacks, durations, grays) and replace them in the same diff — converting only the rules you touch leaves one-of… (fonte: P3-083)
- When computing calendar-day boundaries (Today/Yesterday buckets), do step via date fields (`setDate(getDate() - 1)` or `new Date(y, m, d)`) instead of subtracting 86_400_000 ms, because DST-change days have 23/25 hours and fixed-offset mat… (fonte: P3-084)
- When classifying timestamps into recency buckets, do route unknown/garbage values (0, NaN, negative) into the oldest bucket — never into a recent one — since missing data can never be proven fresh and must not fake recency (fonte: P3-084)
- When building UI that e2e gates must verify across locales, do attach stable data-* hooks (e.g. data-group="today") to generated elements instead of asserting on translated label text (fonte: P3-084)
- When ending/freezing a streaming state on a generic event envelope (e.g. `message.part.updated`), gate the transition on the payload discriminator (`part.type === "text"`) — tool/file/step parts stream mid-turn and must not act as if the a… (fonte: P3-085)
- When custom follow-tail autoscroll owns scrolling while a live bubble grows, set `overflow-anchor: none` on the scroll container — Chromium's native scroll anchoring micro-adjusts scrollTop and fights the "at bottom" heuristic (fonte: P3-085)
- When accumulating streaming-derived state (reasoning, live text) in a session-scoped view, keep it in refs cleared on every sessionId switch and fold events through a pure reducer that drops foreign sessionIDs — a late event otherwise blee… (fonte: P3-085)
- When extracting testable logic from a React component (geometry, labels, formatting), do move the pure functions into a lib file so unit tests hold the contract without a renderer, and register the new test script in the `test:unit` chain… (fonte: P3-086)
- When implementing an auto-grow textarea, do read line-height/padding from `getComputedStyle` instead of hardcoding and floor the max-lines cap (`Math.floor(lh*maxLines + padY)`) so fractional computed line-heights can't leave the box 0.2px… (fonte: P3-086)
- When replacing a native `<select>` with a custom inline dropdown, do rebuild the full a11y/discipline layer it loses — `aria-expanded`/`aria-haspopup`/`role="listbox"`/`aria-selected`, stable `data-*` hooks so the desktop-flow gate can tar… (fonte: P3-086)
- When keeping a React component mounted to play a CSS exit animation (exit-phase hook or `.out` class hold), add `pointer-events: none` to the exiting/invisible variant — a fading scrim or sliding panel still intercepts clicks aimed at the… (fonte: P3-087)
- When an exit animation needs content to render during the hold, keep a "last value" ref (`if (value) ref.current = value`) so the unmounting tree shows the previous item instead of null, and short-circuit the phase timer to "closed" under… (fonte: P3-087)
- When a test beat probes computed `animation-name` in a shared e2e file, remove freeze styles injected by earlier beats (`#p1-080-noanim`-style `animation: none !important`) before reading computed styles, and emulate reduced-motion through… (fonte: P3-087)
- When two apps resolve the same provider/model catalog (pilot `contextWindowFor` vs daemon `buildWindowMap`), pin a test asserting they agree on EVERY key shape the session reports (bare id, `provider/model`, entry `id` alias, unknown) — on… (fonte: P1-079)
- When a gauge/metric effect refetches on a recurring event (agent idle, `idleCount` in deps), wipe the displayed sample only on identity change (sessionId) and keep the previous value until the fresh one lands — otherwise the panel unmounts… (fonte: P1-079)
- When a checkpoint recycles a builder session, drop the dead session id from resume state in the same transition (never advertise "fresh start" and "resume via -s" together) and fail open — unmeasurable pressure or unusable recap keeps the… (fonte: P1-079)
- When a reviewer's REQUEST_CHANGES has all findings mechanically dropped as unverifiable, treat the evidence as unproven-either-way and fail closed (escalate or keep the rejection) — never let "zero verified findings" degenerate into an eff… (fonte: P1-073)
- When a predicate is gated by round number, split orthogonal signals into separate conditions before the round check — a single `round !== 1 return false` silently disables signals (like all-unverifiable findings) that are dangerous in any… (fonte: P1-073)
- When fail-closing without an arbiter, append an actionable instruction to the rejection feedback (restate the concern citing verifiable path:line evidence from the diff) so fail-closed doesn't become an unresolvable builder loop (fonte: P1-073)
- When a daily-guarded flow (nightly pass, cron-like maintenance) must run once per day, do stamp the "last run" state guard only AFTER the work completes — pre-stamping turns any mid-run process exit/reload into a silently lost day (fonte: P1-075)
- When prompting an agent to distill lessons/summaries of a code change, do feed it only the artifact (the diff) — reviewer findings and gate tails in the prompt bias the output toward harness/process lessons instead of product-code ones (fonte: P1-075)
- When deduping lines by semantic similarity, do gate the threshold (e.g. Jaccard >= 0.6) behind a minimum token count and fall back to exact-key matching for short lines — similarity over small token sets is noisy (fonte: P1-075)
- When success depends on a deferred remote action (e.g. `gh pr merge --auto` only queues the squash until checks pass), do not report success at arming time — poll the external system until the end state is confirmed (state MERGED AND head… (fonte: P1-076)
- When a deterministic edit is retried against concurrent or deferred landings, model the edit as tri-state applied/noop/missing and converge "desired state already present" (noop) as success — a retry after a queued merge finally lands must… (fonte: P1-076)
- When retries rewind/rebase a shared branch before reapplying edits, re-derive everything that depends on file content (task ids, insertions, counts) inside the apply callback from the freshly synced copy — ids or pass results computed befo… (fonte: P1-076)
- When comparing agent-pasted command output against a re-run (evidence verification), capture stderr on success too (spawnSync + separate pipe buffers, not execSync): vite/tsc/esbuild print warnings on stderr, and dropping them makes honest… (fonte: P1-101)
- When deriving phase metrics from open/close events, put informational sub-events (retries, mid-phase failures) in an aux set that never opens a tracked phase, or they clobber the real opener and silently break the open→close pairing (fonte: P1-101)
- When adding flaky-retry to a cached gate step, evict the red cache entry, run exactly one more time, classify fail→pass as flaky deterministically (no LLM), and never retry pasted-output divergence — only command-execution failures (fonte: P1-101)
- When parsing structured verdict output from LLM reviewers, parse finding bullets ONLY under the marker that semantically carries them (REQUEST_CHANGES) and treat bullets after APPROVE as rationale — LLMs routinely append justification afte… (fonte: P1-102)
- When extracting quoted spans with a length floor, split on the delimiter and pair sequentially (odd indices) instead of putting the floor inside the regex quantifier — a short quoted span between two long ones mis-pairs the regex and swall… (fonte: P1-102)
- When mechanically verifying citations, check verbatim evidence (quote present in the diff) BEFORE positional evidence (path:line resolution) so real findings with imperfect line cites survive, but exclude path-shaped quotes from the verbat… (fonte: P1-102)
- When a thrown exception in a pipeline worker never produced a merit verdict, record it as infra evidence only (infraFails + doctor wake) and never touch the per-task attempt counter or circuit breaker — a crash loop must burn no attempts a… (fonte: P1-104)
- When a long-lived process self-reloads after deploy, never `process.exit` with slots mid-pipeline: hold new scheduler picks, poll until the running-slot counter drains to 0 (pipelines end by themselves via task timeout), and feed the watch… (fonte: P1-104)
- When a fix removes a dangerous call site (tripCircuitBreaker in a catch, immediate exit on reload), add source-level wiring-pin tests (readFileSync + includes on the call site) so a refactor cannot silently re-enable the old path (fonte: P1-104)
- When a machine-parsed review/verdict contract depends on structured severity tags rather than the verdict marker alone, make untagged/ambiguous input fail closed as the most severe class (e.g. `isBlockingFinding` treats anything without `[… (fonte: P1-103)
- When replacing an escalation or rejection trigger (e.g. round-1 divergence → repeated findings between rounds), preserve pre-existing fail-closed paths: all-unverifiable findings must still escalate in any round and a nit-only REQUEST_CHAN… (fonte: P1-103)
- When matching "same concern" across LLM outputs from different rounds, key on stable structural signals first (cited file paths, lowercased, order-insensitive) and only fall back to normalized text (strip severity tags, bullet markers, cas… (fonte: P1-103)
- When a pipeline step runs `git clean` between agent dispatches (e.g. landMetaCommit in metapush.ts:239), re-write untracked generated configs (workspace opencode.json via writeSandboxConfig) before the next headless agent runs — otherwise… (fonte: P2-105)
- When an automated review consumes file evidence, mandate stable per-step file names, capture the closest truthful state under that exact name even when the step fails, and only trust files that pass existsSync — an honest empty state is ev… (fonte: P2-105)
- When generating BACKLOG.md task lines programmatically, keep the trailing `(area: ...)` tag as the last thing on the line — the scheduler's AREA_RE (backlog.ts:28) anchors at line end and mis-schedules otherwise (fonte: P2-105)
- When accumulating or looking up maps keyed by arbitrary DB text (ex.: `session.model`, ids like `__proto__`/`constructor`), use `Object.create(null)` for the accumulator and `Object.hasOwn` against constant tables — plain object literals r… (fonte: P2-113)
- When deriving a $/rate metric from a per-key price table, group tokens by model BEFORE applying the table and route unmatched/legacy tokens into an explicit bucket (`unpricedTokens`) — never silently convert unknown keys to $0, which impli… (fonte: P2-113)
- When adding a sibling map to a capped store (`taskCosts`/`taskCache`/`taskUSD`), wire it into every prune loop and the final reassignment of `pruneTaskCosts` — a forgotten sibling falls outside the rolling window and grows without bound (fonte: P2-113)
- When a script will mutate a git checkout that doubles as a human's working tree (reset --hard, checkout), first probe `git status --porcelain --untracked-files=no` and abort on tracked modifications — untracked/gitignored files must never… (fonte: P2-114)
- When a guard probe fails and the next step would destroy state (reset/clean), fail closed (abort on unknown); reserve fail-open for guards of non-destructive operations, and state the choice explicitly in the code/docs (fonte: P2-114)
- When a system has a silent fallback path (e.g., tier-B model falling back to tier-A), add a boot-time probe of the dependency binary (`claude --version`, bounded ~1min timeout, never fatal) plus a runtime counter that alerts on every Nth c… (fonte: P2-114)
- When a guard validates LLM output and returns only a boolean, refactor it to a reason-returning twin (e.g. `specRejectReason` returning `string | null`) with the boolean as a thin wrapper — logs, terminal details and alerts can then carry… (fonte: P2-115)
- When a fail-closed guard can reject the same task round after round with the reason buried in logs, fold each rejection into an in-memory streak keyed by `(task, guard)` that emits an `alert` event + supervisor notify from the 2nd consecut… (fonte: P2-115)
- When composing event/alert details from unbounded LLM text, normalize to a single line and pre-truncate below the downstream emit cap (160 of 220 chars) so the informative prefix survives truncation, and inject emit/notify as hooks so herm… (fonte: P2-115)
- When storing user-facing error copy in component state, store the raw error object and format it via the translation function at render time — pre-formatting at catch time freezes the locale and a mid-error language switch shows stale text (fonte: P2-118)
- When a React-only hook (useT) is the sole path to the i18n dictionary, export a pure `translate(lang, key, vars)` reading the same dict so tests and non-React code resolve identical strings and per-locale copy tests become possible (fonte: P2-118)
- When de-hardcoding UI copy into a dictionary, add a regression test that reads the source files and asserts the old hardcoded strings are gone — otherwise the locale mix silently returns (fonte: P2-118)
- When executing scripts from a foreign/untrusted package.json, do run only an explicit allowlist of conventional names (typecheck, build, test:unit) — never lifecycle hooks like prepare/preinstall — and validate names against the tool's doc… (fonte: P2-116)
- When a gate/pipeline hardcodes home-repo steps (harness scripts, golden corpus, desktop smokes), do gate them behind a detected repo profile and fail closed with a dedicated early-return step when the target has no detectable battery, befo… (fonte: P2-116)
- When adding stack detection to a code path covered by older tests, do make it pure via injected fs deps, prove non-allowlisted entries never run with decoy scripts in a temp sandbox, and update existing fixtures to satisfy detection (marke… (fonte: P2-116)
- When an IPC/bridge handler backs a UI feedback loop (spinner, toast), resolve it with the real outcome — return the actual promise (catch → false) instead of a fire-and-forget `true`, or the UI can only lie about success (fonte: P2-112)
- When copy must distinguish "first contact" from "outage", stamp a persistent "dependency seen healthy" flag only AFTER positive evidence (never optimistically) so a first boot with the dependency down keeps calm copy instead of a red incid… (fonte: P2-112)
- When an async action can fail fast, enforce a minimum perceivable trying state (~2s) before showing the result toast, otherwise a fast failure still reads as "nothing happened" (fonte: P2-112)
- When adicionar setas/ícones de ação (chevron, fechar, editar) na sidebar ou em qualquer chrome desktop, do reutilizar o módulo central `components/icons.tsx` (IconChevronDown, IconX, IconPencil, IconUndo) em vez de glifos unicode — o gate… (fonte: P2-124)
- When uma segunda superfície precisar de um overlay/dialog que existe inline noutro componente (ex.: seletor de máquina do SessionsView virar footer de conta na sidebar), do extrair o markup verbatim pra um componente compartilhado paramete… (fonte: P2-124)
- When renderizar chrome dependente de dados async de boot (nome/inicial da máquina, plano), do tratar o estado vazio com fallback que preserva altura e legibilidade (ex.: prefixo do room id quando machineName é "") e extrair as regras de ex… (fonte: P2-124)
- When the desktop-flow gate asserts on copy that lives in i18n dictionaries, do select via a locale-independent class hook (`.pair-section-title`, `.pair-error-hint`, `.pair-overlay-later`) and match text with regexes accepting en and pt-BR… (fonte: P2-106)
- When restyling a screen that shares a wrapper with another journey (pairing vs P2-112's degraded-mode card in the same `.pair-wrap`), do scope the new layout CSS to a fresh screen-specific class (`.pair-screen`) instead of editing the base… (fonte: P2-106)
- When converting an inline error `<p>` into a styled block, do carry `role="alert"` + `aria-live="assertive"` onto the new container and gate any conditional recovery hint off the stable dictionary key (`error === t("invalidCode")`) so scre… (fonte: P2-106)
- When adding a new unit test script, wire it into the root `test:unit` chain in package.json in the same commit — an orphan test file never runs in the gate and gives false confidence (fonte: P2-107)
- When banning emoji/glyph-as-icon from copy, enforce it with a regex test over the specific Unicode blocks in the i18n dicts (whitelisting typographic arrows intentionally) instead of a one-off cleanup — otherwise emojis creep back in futur… (fonte: P2-107)
- When an inline SVG replaces a text glyph inside a text line or banner (status banners, notes, lists), set `display: inline-flex; align-items: center` on the container or `vertical-align` on the svg — bare SVGs sit on the text baseline and… (fonte: P2-107)
- When checking process liveness by PID, do not trust `process.kill(pid, 0)` alone — persist a `{pid, startedAt}` boot-record file (survives SIGKILL/crash) and cross-check the holder's start time via `ps -o lstart=` so a reused/younger PID s… (fonte: P2-069)
- When mirroring logs to stdout/stderr, attach a no-op `error` (EPIPE) listener to both pipes — once the reader dies (SIGKILLed keeper, closed terminal), every console write raises uncaughtException, and a crash handler that logs through the… (fonte: P2-069)
- When a watchdog must guarantee a child never leaks (keeper-death leash), do not rely on `app.quit()` alone — it can stall on busy handlers; back it with a hard `app.exit()` after a grace timeout registered with `.unref()` (fonte: P2-069)
- When two surfaces can render the same connection status (shell strip + in-chat banner), do suppress the inner one with an explicit visibility prop that defaults to "show", so surfaces without the shell (mobile PWA) keep their own banner —… (fonte: P2-108)
- When restructuring UI on i18n-driven screens (chips→menu, header→overline), do add locale-independent hooks (data-filter, aria-label via t()) for the desktop-flow gate, because selectors on visible text break the moment copy is translated… (fonte: P2-108)
- When swapping hardcoded glyphs (⟳ ⚠︎ ↓ ✔) for SVG icon components, do carry the behavioral classes (e.g. conn-banner-spin), size and aria-hidden onto the icon element itself, or animation and status semantics silently die in the swap (fonte: P2-108)
- When a NodeNext workspace is dev-run by tsx but prod-run as tsc emit under plain node, write every relative import with an explicit `.js` specifier and smoke-test the compiled dist — `tsc --noEmit` passes even when resolution fails at boot (fonte: P2-127)
- When runtime code reads files relative to its dist (e.g. `../../../package.json` for the version on /healthz), make the smoke test mirror the container's directory layout (package.json at the root above the dist) or the breakage only appea… (fonte: P2-127)
- When building multi-stage images from a TS monorepo, keep runners/compilers (tsx, typescript) in devDependencies so the `--omit=dev` runtime stage ships only real deps, and use node's built-in fetch in HEALTHCHECK probes instead of adding… (fonte: P2-127)
- When an external command can succeed asynchronously (e.g. `gh pr merge --auto` only arms the squash and may exit non-zero), do not trust the exec return code — confirm the end state fail-closed by polling (`gh pr view --json state,headRefO… (fonte: P2-125)
- When a best-effort/allowFail step can fail, capture its real output (last ~300 chars) into the failure detail and phase event — generic "merge failed" details hide the root cause and got P2-117/P2-123 blocked with zero code findings (fonte: P2-125)
- When classifying pipeline failures, tag tool/API noise with a structured infra kind (network = resource unresolvable, timeout = confirmation budget exhausted) so it burns no attempt and feeds no fever window, while merit anomalies (MERGED… (fonte: P2-125)
- When keying per-IP admission caps or rate limits behind a possible reverse proxy, treat `x-forwarded-for` as forgeable and only honor its Nth-from-the-right entry after the operator explicitly sets a trusted-hop count (default 0 = header i… (fonte: P2-128)
- When a trusted-proxy lookup can fail (missing header, chain shorter than the configured hops, malformed entry, octet > 255), fall back to the normalized TCP peer address instead of minting a key from garbage, so a degraded proxy chain degr… (fonte: P2-128)
- When an admission key can be derived from multiple sources (socket address vs proxy header), route every path through the same normalization function and stash the computed key once on the connection, guaranteeing admit() and release() act… (fonte: P2-128)
- When adding logic to apps/daemon/src/index.ts, extract pure state into a side-effect-free module with no ws/net imports because index.ts runs main() on import and unit tests must never boot a daemon (fonte: P2-129)
- When behavior depends on nondeterminism like jitter or backoff, inject the random/clock as a function parameter so tests pin the full schedule deterministically, and reset accumulated retry state on the success path (ws open) so stale atte… (fonte: P2-129)
- When extending API payloads like /api/health, add nullable additive fields (relayRetry: null when connected) instead of renaming or removing existing ones, and mirror the new shape in the SDK type and docs/api.md in the same commit (fonte: P2-129)
- When a CI job publishes artifacts to users (release upload, update feed), run every invariant check (tag vs both package.json versions) as the very FIRST step of the job, before any `gh release create`/upload — a version drift must fail ch… (fonte: P2-130)
- When a job uploads a packaged bundle, smoke-check the real packaged output between packaging and upload (green typecheck/build/unit tests do NOT prove the bundle contains web-dist/index.html — a blank DMG otherwise ships to users) (fonte: P2-130)
- When a gate script needs CI use and unit coverage, split it into a pure function (inputs in → problem list out, zero disk access) plus a thin CLI wrapper that reads files, prints ALL problems at once in the established format, and exits 1… (fonte: P2-130)
- When claiming a test file is covered, treat a script declared in package.json but never invoked as zero coverage — compute reachability only from commands actually executed by runners, expanding `npm run <name>` chains transitively from th… (fonte: P2-133)
- When a rule must keep failing future violations, keep the checker a pure function (no fs/network/process) with real-world inputs injected by the caller, then add a final assertion in the existing gate battery that runs it against the real… (fonte: P2-133)
- When tests legitimately cannot run in the gate sandbox (live daemon, real Electron, production RELAY_URL), register them in a committed registry file with runner + reason instead of leaving them uncited, and accept a file cited both in the… (fonte: P2-133)
- When adding configurability to a server boot (bind host, port, token), do extract the "should this feature start and how" decision into a pure module with zero protocol imports (no node/http/ws) that returns a structured result plus an exp… (fonte: P2-132)
- When a config combination would expose an unauthenticated endpoint beyond loopback, do fail closed: refuse to start the listener, log the reason once at boot instead of opening an insecure listener, and treat ambiguous values (unknown host… (fonte: P2-132)
- When comparing bearer tokens or secrets, do use a constant-time comparison that folds the length difference into the accumulator and coerces past-end `charCodeAt` NaN via `|0`, never trim or normalize the configured value (operator tokens… (fonte: P2-132)
- When a packaged feature resolves a platform-specific default (update feed URL, asset name), do accept `platform` as an injectable parameter defaulting to `process.platform` and treat env overrides as absolute — the override bypasses platfo… (fonte: P2-131)
- When a feed/asset format has no download engine (e.g. electron-builder yml vs Squirrel JSON), do short-circuit to a dedicated status (`update-available-manual`) or `disabled` BEFORE any network request, and call the download API (`setFeedU… (fonte: P2-131)
- When a background/boot check could trigger a user-visible side effect (opening a browser tab), do wire it as an optional sink that fires only on explicit user action (tray re-check) and dedupe it per item per session — never auto-open at b… (fonte: P2-131)
- When enabling macOS notarization for an Electron app, do declare `hardenedRuntime: true` plus an entitlements plist (allow-jit, allow-unsigned-executable-memory, disable-library-validation, allow-dyld-environment-variables for the ELECTRON… (fonte: P2-136)
- When a pipeline pins CSC_IDENTITY_AUTO_DISCOVERY=false, do treat any configured CSC_LINK/CSC_NAME as inert — electron-builder skips identity lookup entirely (any value other than the exact string "false" enables discovery), silently shippi… (fonte: P2-136)
- When adding a CI preflight that informs rather than gates a job, do implement it as a pure env→result function (mode, notarizes, problems) whose CLI always exits 0 and only writes GITHUB_OUTPUT/::warning::, and pin the cross-file wiring (p… (fonte: P2-136)
- When a confirmation poll loop can only fail as a generic timeout, enrich the query with fields that expose the root cause (e.g. `mergeable`/`mergeStateStatus` on a PR) and bail on the current poll with a structured, classified outcome, so… (fonte: P2-134)
- When rebasing a preserved/retry branch onto a moved main, fail closed on conflict: run `git rebase --abort`, keep the branch intact at its preserved tip, and let the builder resolve next round — never `reset --hard` over preserved history;… (fonte: P2-134)
- When a retry push follows a rebase that rewrote already-pushed commits, push with `--force-with-lease`: under `allowFail` a plain push is silently rejected non-fast-forward (origin still sits at the previous attempt's tip), leaving the PR… (fonte: P2-134)
- When classifying Node/undici fetch failures, walk the full `cause` chain collecting `code` at every link — the ECONNREFUSED-class detail lives only in `.cause` of the top-level `TypeError: fetch failed`, so matching the top-level message a… (fonte: P2-135)
- When adding decision/classification logic to a daemon whose index.ts runs main() on import, put it in a pure side-effect-free module (no http/fetch imports) so unit tests cover every failure mode — including the real wrapped error shapes —… (fonte: P2-135)
- When enriching a health/diagnostic endpoint, add the new detail object alongside untouched legacy fields (old clients keep working) and make reason/hint static classification strings — echoing bodies or error messages can leak tokens/secre… (fonte: P2-135)
- When an agent retry follows a deterministic guard rejection, do inject the exact machine-computed reject reason into the retry prompt as truncated, quoted data (labeled "not instructions") and classify it — format-repairable causes earn ex… (fonte: P2-137)
- When a first failure of a kind is classified as infra (free retry), do persist the per-task franchise counter in state that survives midnight rollover and clear it only on merge — a counter that resets daily turns every broken task into un… (fonte: P2-137)
- When adding variable blocks to a cache-aware prompt, do append them at the very end of the variable tail and add a test asserting the stable prefix is byte-identical across different reasons/attempts (fonte: P2-137)
- When adding fields to a shared health/pairing payload (desktop main → preload → renderer), declare them optional/additive and validate shape at the boundary (typeof checks, degrade to null) so legacy or malformed upstream payloads render n… (fonte: P2-138)
- When a task needs a new piece of health data, extend the existing poll and push channel instead of adding a second fetch: merging fetchDaemonVersion into fetchDaemonHealth returning one object kept one loopback call per poll serving both c… (fonte: P2-138)
- When rendering daemon-provided free-text (reason/hint) in the renderer, pass it only through JSX text interpolation (React-escaped), never as HTML, and map unknown upstream states to "no notice" in a pure classifier so the single-surface r… (fonte: P2-138)
- When classifying hosts as loopback to gate a security decision, do match 127.0.0.0/8 as a strict dotted-quad (each octet <= 255) — a prefix test would classify nip.io-style DNS wildcards like 127.0.0.1.evil.com as loopback and let plain-te… (fonte: P2-139)
- When adding pure decision logic used by a daemon entry file that runs `main()` on import, do put it in a separate module with zero ws/http imports so unit tests can cover it without ever booting the daemon (fonte: P2-139)
- When a config value must fail closed, do log the reason once at boot (never per retry), expose the verdict as an additive health field, and withhold downstream artifacts that depend on it (e.g. a pairing QR the client can never use) — and… (fonte: P2-139)
- When extracting hardcoded constants into an env-driven module, update regex/file-path-based invariant checks to scan the new location in the same commit and re-verify they still match, otherwise the invariant silently stops guarding the li… (fonte: P2-141)
- When making limits env-configurable, resolve them in a pure function returning values plus a problems list, and treat any present-but-invalid value (non-numeric, zero, negative, cross-field contradiction) as boot-fatal — log every reason o… (fonte: P2-141)
- When validating bounded config values, derive the hard ceiling from the underlying protocol constraint (ws maxPayload is an int32 → 16777216) and test the boundary from both sides — exactly-at-ceiling passes, one-above is a problem (fonte: P2-141)
- When a function mixes file I/O with a text transformation on markdown, extract the edit into a pure text-in/text-out function returning new text plus an applied/noop/missing verdict, and reduce the disk wrapper to read→edit→write — pure ed… (fonte: P2-142)
- When an automated edit appends a line to a shared markdown section (stop-loss moving a task to `## Blocked`), reuse the first existing header, treat "already present under ANY such section" as noop (not just the first one), and collapse du… (fonte: P2-142)
- When a doctor/validator detects legacy accumulated state that self-normalizes on the next real write, report it as a non-fatal warning instead of an ok:false problem, so the file is not flagged invalid while it heals itself. (fonte: P2-142)
- When a spawned child process (sidecar) can die for diagnosable reasons, capture a bounded rolling stderr tail while it lives and classify code/signal/stderr-markers into a verdict at exit time (exposed beside existing state accessors, addi… (fonte: P2-140)
- When classifying process exits, check stderr markers (EADDRINUSE, ENOENT) BEFORE signal/exit-code heuristics and route spawn-level errors (err.message from the `error` event) through the same pure classifier — the actionable cause must win… (fonte: P2-140)
- When propagating diagnostic verdicts over IPC to a renderer, transmit only a small kind enum with static reason/hint and map kind → i18n keys in the renderer with tolerant validation (absent/malformed fields → null, silence is safe) — raw… (fonte: P2-140)
- When a tool produces an artifact that a distant gate will verify later (screenshots, evidence files), validate the bytes at capture time with a pure checker that returns an exact reason — failure at the producer burns zero attempts, failur… (fonte: P2-144)
- When reading binary headers at fixed offsets (readUInt32BE etc.), check signature, minimum length and terminal marker first — unchecked offsets on a truncated buffer return garbage values that masquerade as success with ok:true (fonte: P2-144)
- When a failed write can leave a partial file on disk, delete the partial and retry exactly once before reporting failure, and route every producer of that artifact type through one shared validator module so the rules never diverge (fonte: P2-144)
- When a server has a graceful-shutdown flag (like `isShuttingDown`), wire it into BOTH the health probe (503) and the new-connection admission path in the same change — WebSocket upgrades bypass the `request` handler, so gating them needs `… (fonte: P2-145)
- When extending a health/telemetry payload for a degraded state, keep the healthy body byte-for-byte identical and add the new field only in the degraded case (additive field like `draining?: true`, `ok` flipped) — existing consumers (HEALT… (fonte: P2-145)
- When adding a tunable that delays a hard shutdown deadline, validate it fail-closed with a ceiling strictly below the hard window (grace 2000ms inside a 3000ms drain so grace + settle always fit) and skip queuing the timer when the value i… (fonte: P2-145)
- When gating tests for hardware-dependent Electron states (camera, mic), do add a test-only env hatch that denies `media` via `session.setPermissionRequestHandler` + `setPermissionCheckHandler` (e.g. `OCR_DESKTOP_CAMERA_BLOCK=1`) so the una… (fonte: P2-117)
- When the input device and the output device are the same screen (scanning a QR shown on another desktop), do invert the primary path — paste/deep-link becomes primary and camera the option, driven by a platform-aware prop like `preferPaste… (fonte: P2-117)
- When a camera feed is empty or the track ends, do classify it as the app's own "unavailable" state (reason `no-signal` after a grace period of no decodable frames) — never render the capture device's native OSD ("NO SIGNAL") as if it were… (fonte: P2-117)
- When adding a CI step on windows-latest that uploads artifacts with globs (`gh release upload ... apps/desktop/dist/*.exe`), force `shell: bash` on that step — pwsh (the default shell there) does not expand the glob and the upload fails or… (fonte: P2-126)
- When a CI job depends on optional credentials (e.g. CSC_LINK / CSC_KEY_PASSWORD for code signing), treat the empty secret as the expected no-signing path — log a "shipping unsigned" line and continue — instead of letting the missing secret… (fonte: P2-126)
- When the gate is green but the merge fails with "PR head is X, not our Y" / conflicting, the branch went stale while other tasks landed: before the next builder round, `git merge origin/main` into the pilot branch resolving both sides (the… (fonte: P2-126)
- When a second UI surface needs a capability the chat composer already has (voice transcription, model selector), extract it into a shared lib/hook (e.g. `transcribeBlob`, `useModelSelector`, `ModelMenuItems`) instead of copying ChatView in… (fonte: P2-123)
- When a click must open a NEW session with a pre-filled prompt, write the draft keyed by the freshly created session id BEFORE dispatching the view change (`setDraft(created.id, prefill)` then openChat) — anything written after mount races… (fonte: P2-123)
- When a toggle and an async start action both write the same persisted key (`ocr_agent`), the start handler must restate the value derived from the mode it was fired with at fire time — never trust the stored value, the user can flip the to… (fonte: P2-123)
- When a previously-fixed constant (like a daemon port) becomes resolved at runtime, do replace every read site — health polls, the env passed to the spawned child, IPC payloads, log lines — with a single getter resolved once per process; a… (fonte: P2-143)
- When walking fallback candidates for a resource, do run the identity probe ("is this OURS?") on every candidate, not just the preferred one — your own leftover instance on a fallback port must be adopted, not shadowed by a fresh spawn that… (fonte: P2-143)
- When decision logic needs system probes (net bind, HTTP health), do keep the decision module pure by injecting the probes as functions, treat a throwing probe as "discard this candidate" (never propagate), and on total failure return the p… (fonte: P2-143)
- When wiring Electron auto-update on macOS, do not point Squirrel.Mac at `latest-mac.yml` — it only consumes a JSON feed ({url, name, notes, pub_date}) and only installs from a zip, so add the `zip` target to the electron-builder mac block… (fonte: P2-146)
- When a CI-built update feed or release artifact is invalid, do fail closed: collect ALL problems and print them at once with exit code 1 and write no output file, so a half-valid feed never ships (fonte: P2-146)
- When asserting a packaging invariant in tests, do read the real config file (e.g. electron-builder.yml) instead of a fixture, and percent-encode artifact file names in download URLs since release asset names contain spaces (fonte: P2-146)
- When a slow/risky check (packaging, signing, upload) only exists in a tag workflow, add a PR-scoped job gated by a pure path-classifier that writes GITHUB_OUTPUT (no third-party actions) and keep the release invocation byte-for-byte identi… (fonte: P2-147)
- When one script must run both in CI and in unit tests, export the logic as a pure function and wrap only a thin CLI guarded by `import.meta.url === invoked` so importing the module never triggers the side-effecting GITHUB_OUTPUT mode (fonte: P2-147)
- When classifying changed paths for CI scope, normalize separators/./ prefixes, cover bare-dir entries and lookalike siblings (apps/desktop-runner), and pin the workflow wiring itself (needs/if/flags) with assertions reading the real yml so… (fonte: P2-147)
- When adding a first-run gate/onboarding screen to an app whose e2e suite boots many beats with fresh userData, update EVERY hermetic `open` beat to skip past it (grep the flow suite for all launches), not just the new feature's own beat —… (fonte: P2-148)
- When a test harness spreads `process.env` into its hermetic base env, explicitly delete session-scoped variables (e.g. `OCR_USER_DATA_DIR`) from the copy so one beat's setup can never contaminate subsequent boots; hoist to shared scope any… (fonte: P2-148)
- When persisting a one-time "done" flag, compare exact-match against a sentinel and treat absent/corrupt values as not-done (fail-open: a broken localStorage shows the onboarding again instead of soft-locking the app), and add an independen… (fonte: P2-148)
- Ao criar um módulo novo em apps/daemon/src, mantenha-o 100% puro (sem node:fs, node:child_process, node:path/os nem fetch), injetando todo I/O (env, platform, home, isExecutable) como parâmetros — index.ts roda main() no import, então scri… (fonte: P2-149)
- Ao dividir um ramo de classificador já existente (ex.: ECONNREFUSED), use campo opcional aditivo cuja ausência reproduz o veredito legacy byte a byte e mantenha o mesmo state + strings antigas, assim os testes e clientes existentes passam… (fonte: P2-149)
- Ao resolver estado da máquina (binário, portas, paths), cacheie com rate limit (resolução forçada no boot + no máximo uma vez por minuto apenas no ramo unreachable, nunca por tick saudável) e exponha em reason/hint/payload somente booleano… (fonte: P2-149)
- When adding OS-surface logic to an Electron main process that unit tests must cover, extract a pure planner module free of electron imports (same pattern as tray.ts/notify.ts) that takes platform + raw payload and returns the decision (kin… (fonte: P2-150)
- When main.ts loads an image at runtime (taskbar overlay, tray icon), list the file in electron-builder.yml `files` next to the other bundled assets — a script-generated PNG works in dev but silently vanishes once packaged into the asar — a… (fonte: P2-150)
- When replacing an inline IPC handler with a shared helper, route EVERY mutation site (the push handler and the clear-on-focus path) through the same plan and sanitize the raw payload at that single entry point (non-positive/non-finite → 0,… (fonte: P2-150)
- When a release workflow gains a publish/deploy step, keep the build step unconditional and gate only the push behind an exact-match opt-in variable (e.g. `if: vars.X == 'true'`), so every tag still proves the artifact builds but tagging ne… (fonte: P2-151)
- When a pure function feeds references into destructive consumers (`docker build -t`, publish targets), fail closed by returning empty outputs whenever ANY input problem exists, collect all problems in one pass, and make the CLI write nothi… (fonte: P2-151)
- When adding or editing a CI job, pair it with a unit test that reads the real workflow YAML and asserts the load-bearing wiring (job name, `needs`, `permissions`, publish condition), so workflow drift or revert is caught in test:unit inste… (fonte: P2-151)
- When persisting a "shown once" UX flag (welcome, close hint, badge), compare exact-match against a sentinel and treat absent/empty/corrupt values as "not shown" via injected read/write sinks that catch throws — fail-open so at worst the us… (fonte: P2-152)
- When e2e-testing Electron close-to-tray, do not close from the renderer DOM — window.close() there destroys the window without firing the cancellable close event; call win.close() in the main process (harness BrowserWindow.close) so the na… (fonte: P2-152)
- When a best-effort feature decorates a critical operation (hint notification after win.hide()), wrap it entirely in try/catch with log-only degradation and stamp the one-shot flag only AFTER the side effect went out (check Notification.isS… (fonte: P2-152)
- When matching published artifact names against an expected version, use a boundary-aware regex that rejects an adjacent digit/dot (e.g. `(?<![\d.])0.3.0(?!\.?\d)`) instead of a plain `includes`, so "10.3.0" never satisfies a "0.3.0" slot w… (fonte: P2-153)
- When reading GitHub release asset names (or any filename list that may contain spaces) from stdin or argv, split strictly on newlines after trimming — never on whitespace — or names like "OpenCode Remote Setup 0.3.0.exe" get shredded into… (fonte: P2-153)
- When a release workflow spreads uploads across independent packaging jobs, add a final verify job that `needs` every asset-producing job and checks the actually published release (`gh release view --json assets` → expected-asset checker),… (fonte: P2-153)
- When a transport/security mode is driven by a pair of env vars (e.g., RELAY_TLS_CERT + RELAY_TLS_KEY), never implement it as `if (a && b) else insecure-default` — resolve a plan module up front where one-alone, blank, or unreadable values… (fonte: P2-154)
- When a boot preflight reports config problems into logs that ship off-host, phrase each reason citing the failing variable name and never the file path or material, and keep the decision module pure with an injected readability probe (no n… (fonte: P2-154)
- When choosing fail-open vs fail-closed for a degraded path, pick by blast radius: "shown once" UX flags fail open (re-show the hint, P2-152), but anything that decides whether traffic is encrypted or capped fails closed (refuse the boot) —… (fonte: P2-154)
- When adding a periodic setTimeout loop in a long-lived Electron main process, always clearTimeout the previous handle before re-arming (a manual check during a scheduled wait must replace, never stack) and clear the handle in will-quit bef… (fonte: P2-155)
- When a decision depends on an async call that has both a return value and a status callback, derive scheduling from the return value (it also covers terminal states like "disabled" that never hit the callback) so event-driven emissions (e.… (fonte: P2-155)
- When a pure planner function takes injected randomness or counters, normalize hostile inputs (NaN/negative/fractional → 0) and clamp the final result with a hard Math.max floor so no combination of broken dependencies can emit an out-of-ra… (fonte: P2-155)
- When a WebSocket peer refuses connections with distinct close codes (1013 capacity, 4029 rate-limit, 1001 drain), classify by the numeric code and treat the reason text as corroboration only — empty or unknown reasons must still classify b… (fonte: P2-156)
- When adding a reconnect floor for a specific failure kind, compose it as Math.max(jitteredSchedule, floorMs) instead of replacing the schedule — the floor only ever lengthens the wait, so the existing backoff curve for transient drops stay… (fonte: P2-156)
- When extracting close/retry classification out of a daemon entrypoint, put it in a pure module with no ws/net imports (index.ts runs main() on import, like relayretry.ts/relayurl.ts) so unit tests exercise every kind without booting a serv… (fonte: P2-156)
- When a document embeds absolute URLs recorded at publish/stage time (feeds, manifests, pointers), resolve volatile fields like ports at serve time against the actually-bound address — a fallback-port boot makes every recorded port stale wh… (fonte: P2-161)
- When surgically editing one field inside a URL or JSON text, replace only the target substring in the raw text instead of round-tripping through `new URL()` (normalization percent-encodes chars like spaces and rewrites bytes that aren't yo… (fonte: P2-161)
- When adding testable logic to a server entry file that runs `main()` on import (daemon/index.ts), put the helper in a pure module with no node:fs/node:http imports (pattern: relayurl.ts, feedport.ts) and make it fail closed — return the or… (fonte: P2-161)
- When sanitizing a log tee for a secret that also feeds a functional path, redact only at the disk-write boundary and keep passing the raw chunk to the real consumer (e.g. pairing capture) — sanitizing upstream breaks the feature that needs… (fonte: P2-160)
- When redacting streamed stdout/stderr, never redact per-chunk: credentials get split across chunk boundaries, so buffer the lineless tail (byte-capped with a forced already-redacted flush) and redact only complete lines, preserving the tra… (fonte: P2-160)
- When a log carries multi-line rendered blocks (ASCII QR with ANSI SGR escapes), use a stateful suppression mode armed by the announce line and cleared by the first normal line, and test both ANSI-wrapped and bare-glyph variants plus the su… (fonte: P2-160)
- When a multi-platform release workflow signs artifacts, give each platform its own namespaced secret pair (e.g. WIN_CSC_* for Windows instead of reusing the Apple CSC_*) and keep a unit test that slices the real job block out of the workfl… (fonte: P2-159)
- When bundling ESM source into a single-file CJS sidecar, gate every top-level `import.meta`/`fileURLToPath` usage behind `typeof __dirname === "undefined"` (esbuild empties import.meta and CJS defines __dirname) or the code crashes at boot… (fonte: P2-159)
- When a workflow step treats a secret pair as optional, distinguish absent (Actions renders missing secrets as "") from present-but-blank/partial and abort the job on the latter via an exit-1 preflight — a half-configured signing profile mu… (fonte: P2-159)
- When a CI gate measures build artifacts from disk, keep the checker pure (entries + budgets → problems) and let the measuring layer omit missing/unreadable paths so the checker reports them as "expected entry missing" — a broken or absent… (fonte: P2-162)
- When a script is both CLI and unit-tested, guard main() with `import.meta.url === pathToFileURL(process.argv[1]).href` so test imports skip execution, and print all problems at once with exit 1 instead of failing on the first (fonte: P2-162)
- When setting byte budgets on build output, calibrate ceilings from a real measurement plus ~40% documented slack for platform/toolchain drift (local darwin vs CI runner) — an exact-fit budget fails CI with no real regression (fonte: P2-162)
- When persisting a state file that carries identity, keys or pairing data (daemon.json and the like), do write a sibling temp file created with the final mode (0600) and rename it over the destination instead of writeFileSync + chmodSync —… (fonte: P2-165)
- When a helper writes via tmp+rename, do unlink the temp file in a catch that rethrows the original error, so a failed write or failed rename leaves the previous destination byte-intact with zero .tmp residue and the caller keeps deciding h… (fonte: P2-165)
- When adding file-persistence helpers, do inject a structural subset of node:fs (StateFileFs-style, same pattern as desktop-log.ts) so unit tests simulate ENOSPC mid-write and rename failure against a fake fs and assert the invariants — old… (fonte: P2-165)
- When adding a validation step to a GitHub Actions job on a Windows runner (desktop-win), declare `shell: bash` explicitly because the default pwsh does not expand globs and can silently break the run command (fonte: P2-164)
- When closing a packaging/release gap on one platform job (e.g. desktop-dmg), audit the sibling platform jobs (desktop-win, linux) for the same missing gate — the identical smoke-check gap shipped broken Windows installers a full cycle afte… (fonte: P2-164)
- When a workflow step's ordering is the actual invariant (validate between package and upload), pin it with a real-repo test asserting indexOf ordering of the step names inside the job block, so refactors cannot silently drop or reorder the… (fonte: P2-164)
- When a support/diagnostic bundle reads log file tails, wrap every readFileSync in try/catch and render a placeholder line (e.g. "(sem log do sidecar)") for missing/unreadable files — a fresh install has no logs yet and the bundle must neve… (fonte: P2-163)
- When a pure builder receives arrays the caller already bounded (log tails), re-apply the slice with the exported constant inside the builder and pin the re-bounding in tests — callers pass unbounded data and the bundle must stay support-fr… (fonte: P2-163)
- When adding a new data source to a privacy-contracted bundle, only include files that are already redacted at write time on disk (P2-160) — redacting at bundle time is the pattern that leaks tokens/pairing URIs (fonte: P2-163)
- When a long-running daemon writes security-relevant logs (audit trails, auth failures), create the file with mode 0600 on the first append (mode rides only on creation, never chmod existing files) and cap it with rotation that unlinks the… (fonte: P2-167)
- When adding log rotation to a file that any reader serves to a UI, change every reader in the same commit to span the rotated files in chronological order (older `.1` first, active last) — otherwise the first rotation silently wipes the us… (fonte: P2-167)
- When a module does fs bookkeeping that must never crash the daemon, inject a structural subset of node:fs (the statefile.ts pattern), make every op best-effort (stat failure = empty file, failed rotation still appends to the oversized acti… (fonte: P2-167)
- When adding an audit/gate script (release-preflight style), split pure verdict logic from a thin CLI main and make the static data maps injectable parameters — test the verdict matrix purely, then test CLI wiring (exit 1 on fail, real pack… (fonte: P2-168)
- When hand-rolling semver comparisons for security verdicts, sort prerelease below release (44.2.0-alpha.1 is affected, 44.2.0 is not) and decide affectedness solely by version compare — unmapped metadata like Chromium "unknown" must never… (fonte: P2-168)
- When bumping the electron pin for a Chromium CVE, land it only with the full desktop battery green (dist:smoke + test:desktop-flow) and record the exact fixed electron + Chromium build numbers in the version-notes doc so future audits can… (fonte: P2-168)
- When a numeric env var guards a security or abuse boundary (rate limits, per-IP caps, trusted proxy hops), never resolve it with a helper that silently falls back to the default on bad input — validate it in a preflight that logs each prob… (fonte: P2-171)
- When you extract boot-time env validation into a new module, keep it pure (no node:fs/node:http/ws imports) with exported defaults, per-knob ceilings and a `problems` list, and make absent-or-blank the ONLY case that keeps the default with… (fonte: P2-171)
- When changing an env knob's semantics (e.g. "0 disables the cap" becomes "0 refuses the boot"), search tests and docs that pinned the old behavior and rewrite them — the integration test that asserted the disabled path must become one asse… (fonte: P2-171)
- When touching macOS packaging of an Electron app under hardened runtime, do pair every getUserMedia device with BOTH a `com.apple.security.device.*` entitlement in entitlements.mac.plist AND its `NS*UsageDescription` in Info.plist (extendI… (fonte: P2-169)
- When adding a new check to a shared smoke/preflight script, do keep it in a separate exported function that no-ops for bundles where it does not apply (win-unpacked, binary Info.plist) instead of folding it into the existing problem list,… (fonte: P2-169)
- When validating YAML/plist config by regex because the repo has no parser, do treat commented-out keys and explicit `<false/>` as absent, report "present but blank" separately from "missing", and emit all problems in one pass (fonte: P2-169)
- When adding a cap parameter that bounds a payload (list size, bytes, rows), treat missing, non-numeric, fractional, zero or negative values as "fall back to the safe default" — never as "disable the limit" (fonte: P2-173)
- When trimming an API response, keep the change additive: ship the reduced array in the existing field and attach `total`/`truncated` instead of renaming or removing anything, so lenient clients that ignore unknown fields keep working untou… (fonte: P2-173)
- When capping a list before a response, also cap every derived work that scales with it (per-item title lookups, upstream fan-out) by resolving only for the items actually shipped, and preserve the incoming sort order by slicing the tail in… (fonte: P2-173)
- When replacing raw personal data in logs with a derived identifier, mint a fresh random salt per boot and rename the field (ip → ipTag) so nobody mistakes it for the raw value; never use hash("") as the tag for absent/unknown input — it wo… (fonte: P2-174)
- When a privacy invariant depends on what log lines carry, add a source-pin test that reads the real caller source and sweeps every logging call's payload (regex over ev(...)) asserting the raw field never appears — unit tests of the pure h… (fonte: P2-174)
- When a factory consumes secret bytes (salt) whose entropy the output's irreversibility depends on, fail closed on short salts at creation and copy the input, so caller-side mutation cannot silently change outputs mid-process (fonte: P2-174)
- When persisting Electron window state on close, do save `win.getNormalBounds()` + `win.isMaximized()` instead of `win.getBounds()`, because on a maximized window `getBounds()` returns the full work area and reopening it produces a screen-s… (fonte: P2-172)
- When reapplying window state that must call `win.maximize()`, do it in the ready-to-show handler right before `show()` (and skip it in the hermetic hidden-window path), since Electron's `maximize()` also SHOWS a hidden window and calling i… (fonte: P2-172)
- When adding a new optional field to a sanitized/persisted state object, do validate it with a strict `=== true` check (anything non-boolean becomes false for legacy files with no migration) and preserve it across every fallback branch — ga… (fonte: P2-172)
- When a release pipeline depends on an external tool's verdict (codesign/spctl/stapler), capture each tool's stdout+stderr to a file (`2>&1 … || true`) and feed the text to a pure fail-closed checker that runs BEFORE the irreversible step (… (fonte: P2-170)
- When parsing native tool output as a gate, treat empty or unrecognizable output as a problem (Apple rewordings must fail loudly) and scope "expected failures" to the mode actually selected — spctl rejection and a missing staple are fatal f… (fonte: P2-170)
- When adding a CI step that gates an upload, pin it with a unit test that reads the real workflow file and asserts the step's existence, its position relative to the step it protects, and its declared `shell: bash` — workflow YAML has no te… (fonte: P2-170)
- When touching logging in a service whose contract is metadata-blindness (relay, proxies), treat per-message log lines as a privacy decision, not a verbosity tweak: a line per routed event with room/sender/target identity in provider-retain… (fonte: P2-177)
- When adding a new env-configurable knob to the relay, extract the decision into a zero-import pure module (limits.ts/knobs.ts/loglevel.ts pattern) returning a `problems` list, resolve it in index.ts before anything logs or listens, and exi… (fonte: P2-177)
- When changing the level of one log line among many, pin the wiring with source-reading assertions: test that the real source contains the exact new call (`ev("debug", "frame in"`), that the old call string is gone, that every rejection lin… (fonte: P2-177)
- When Electron-native chrome (app menu, tray) is built inline in main.ts and is unreachable by tests, extract it as a pure data-spec module with zero electron/node:fs imports (pattern of tray.ts, badge.ts, closehint.ts) and reduce main.ts t… (fonte: P2-176)
- When translating native menu chrome to the product language (pt-BR), keep native roles (editMenu, windowMenu, quit, about) so the OS translates them itself and override only the labels of custom items; also keep product terms that the UI i… (fonte: P2-176)
- When one action appears in two native surfaces (tray + menu bar), resolve the menu item by id to the exact same shell handler instead of adding new IPC or preload surface, and rebuild both surfaces at the same call site (where refreshTrayM… (fonte: P2-176)
- When a workflow assembles a user-visible artifact across multiple jobs (release page + installers attached minutes later), create it hidden (`--draft`) and flip visibility in a final gate job that `needs` every verification job — a mid-run… (fonte: P2-179)
- When a gate controls a state transition that workflow re-runs will encounter again, make "already done" a clean no-op verdict (publish=false, zero problems, exit 0) instead of a failure, so retries are idempotent and walk straight past com… (fonte: P2-179)
- When pinning a checksum or URL to a published artifact in the repo, compute it from the artifact downloaded back from the release (`gh release download`) in a step that runs after publication — never from a local build file or a not-yet-pu… (fonte: P2-179)
- When untrusted content (chat messages, artifacts, agent output) can reach an OS-level handler like shell.openExternal, route every call site through one pure allowlist decision (http/https/mailto only, scheme compared lowercased so "FILE:/… (fonte: P2-178)
- When logging refused URLs or other untrusted payloads, log only the scheme and a stable reason, never the raw value, because desktop.log is a persistent file that would otherwise leak conversation content (fonte: P2-178)
- When a security gate must hold across future edits, pair the pure module with a unit test that reads the real caller source and asserts no bypass call sites exist (no shell.openExternal outside the decision path), so a newly added direct c… (fonte: P2-178)
- When enforcing a request-body size limit on a stream that yields UTF-8 strings, count bytes (Buffer.length) per chunk and reject the instant the total crosses the ceiling — never accumulate everything and truncate afterwards, because a tru… (fonte: P2-180)
- When an env var configures a safety limit, resolve it once at boot and fail closed (log one line per problem, exit 1 before opening the listener); only a missing or blank value may fall back to the default without a problem — an invalid va… (fonte: P2-180)
- When extracting testable logic (body limits, pagination, close handling) from a file that runs main() on import, put it in a pure module with no node:http/node:fs imports, duck-type errors by a `code` property instead of instanceof, and ke… (fonte: P2-180)
- When a CI job picks a signing mode via preflight, treat that as intent only: add a verdict check on the actual packaged artifact (parse Get-AuthenticodeSignature output for the built exe) positioned before the upload/attach step, so an exp… (fonte: P2-183)
- When a gate parses external tool output for a go/no-go decision, fail closed on empty or unrecognizable output and explicitly exempt the documented no-secrets mode (unsigned/ad-hoc) so the legitimate unsigned path stays green (fonte: P2-183)
- When adding a verify step to a release job, make it a pure function + CLI pair (all problems reported at once, exit 1) so the same logic is unit-testable without running PowerShell or producing a real installer (fonte: P2-183)
- When guarding an in-memory staging area for chunked uploads (or any payload that arrives outside readBody, e.g. via E2E tunnel frames), enforce an incremental per-id byte ceiling at every chunk instead of checking the total only at the fin… (fonte: P2-181)
- When several routes share a staging/expiry map, run the expiration sweep BEFORE admitting new work on every route that touches it — a sweep living inside only one handler leaks abandoned entries for the process lifetime — and on a limit re… (fonte: P2-181)
- When client-supplied input indexes into arrays (entry.parts[idx]) or unbounded concurrent ids can accumulate in a map, validate the index before use (finite integer, non-negative, <= max → 400) and cap concurrent staging ids (429), carryin… (fonte: P2-181)
- Quando um handler de segurança (permissões do Electron, openExternal, etc.) só é registrado dentro de bloco condicionado a env de teste, o build empacotado cai no default permissivo do runtime — registre handlers de segurança sempre, incon… (fonte: P2-182)
- Ao criar gate de permissão/origem, faça allowlist default-deny que nega também nome de permissão desconhecido, URL não parseável, origem opaca (`origin === "null"`) e input não-string, comparando scheme em caixa baixa — uma versão futura d… (fonte: P2-182)
- Ao logar recusas de segurança em log local compartilhado (desktop.log), registre apenas nome da permissão + scheme do requester + reason estável, nunca a URL completa — navegação do usuário não pode vazar no log; e implemente a decisão em… (fonte: P2-182)
- When a renderer declares security attributes of an embedded webview (`webpreferences`, `preload`), do not trust the declaration — force the safe set in the main process via `will-attach-webview` (contextIsolation+sandbox on, node integrati… (fonte: P2-184)
- When validating URLs of an embedded guest, guard ALL navigation paths (`will-navigate` AND `will-redirect`, plus `setWindowOpenHandler` → deny), not just the address typed in the URL bar — redirects, meta refresh and clicked links bypass s… (fonte: P2-184)
- When writing a security decision, put it in a pure module (no electron, no node builtins, no fetch) so unit tests exercise the real code, register it at exactly ONE hook in main (single `web-contents-created`), compare schemes lowercased (… (fonte: P2-184)
- When a CI step hashes a release's own assets back (gh release download → hash → gh release upload --clobber), do keep a skip-list so the manifest itself and intermediate files (ex.: entries.json) are never hashed into the manifest — a re-r… (fonte: P2-186)
- When adding a new release guard/verify script, do import shared invariants (ex.: the required-assets list from release-assets.ts) instead of copying them — duplicated lists drift and a new required asset silently escapes one of the verifie… (fonte: P2-186)
- When generating checksum manifests, do emit the canonical coreutils format (hash + two spaces + name, sorted by name, unix line endings, trailing newline) so the OS-standard check (`sha256sum -c`) works without any translation step (fonte: P2-186)
- When a settings surface replaces an env-only knob that feeds a user-facing artifact (e.g. a pairing QR), keep the precedence env > stored > default and never let an invalid stored value silently fall back to the default — return an explici… (fonte: P2-187)
- When a child-process env var must hold across every spawn path (initial start, backoff respawns, manual restarts), inject it inside the single function all spawns funnel through and resolve its value once before the first spawn — per-path… (fonte: P2-187)
- When you need a small validation predicate from a module that runs side effects at import time (e.g. a daemon whose main() boots on import), duplicate the pure function locally with a comment and pin parity in unit tests against a shared t… (fonte: P2-187)
- When implementing any time-limited trust window (bootstrap pairing, token TTL, grace periods), do treat a "window opened at" instant in the future as window-not-extended, so an advanced clock can only tighten the rule, never loosen it (fonte: P2-190)
- When gating an auto-trust bootstrap on operator intent, do re-arm the window at the exact authenticated UI read that proves someone is looking (the pairing-screen route) instead of only at boot, because a daemon idle for days must never tr… (fonte: P2-190)
- When parsing a security-relevant env knob, do accept only absent-or-blank as "keep default" and emit a fail-closed problem for every other malformed value (non-numeric, negative, zero, fractional, above cap) in the shared problems format,… (fonte: P2-190)
- When a resolved file path must stay inside a configured root, do the containment check on canonicalized paths with a separator boundary (`realpathSync(target).startsWith(realpathSync(root) + sep)`) — a bare string-prefix `startsWith(root)`… (fonte: P2-188)
- When validating untrusted request path segments for static serving, do reject in one rule every segment starting with `.` (covers dotfiles and `..` simultaneously) and run exactly one `decodeURIComponent` per raw segment, rejecting decoded… (fonte: P2-188)
- When adding an optional feature behind an env toggle on an existing hot path, do keep all decisions in a pure module (node:path only, fs I/O injected by the caller) and preserve the pre-toggle behavior byte for byte when the variable is ab… (fonte: P2-188)
- When resolving a user-facing address from stored + derived candidates, fail closed on both: an invalid stored value and an unreachable derived value (relay loopback) must return origin "unavailable" with an explicit reason, never a silent… (fonte: P2-189)
- When generating QR data URLs for a derived address, gate minting on the problems list being empty and withhold the QR whenever it is not (e.g. derived http:// on a public host returns the URL WITH a problem), so no scannable artifact ever… (fonte: P2-189)
- When two workspaces (apps/desktop, apps/daemon) need the same validation rule like isLoopbackHost, duplicate it locally and prove parity with a shared host-table fixture in unit tests instead of importing across workspaces — the cross-work… (fonte: P2-189)
- When accepting an env override for a security header like CSP, do validate it in a pure boot preflight (must be a string, free of newlines/control bytes, under a length ceiling, and declaring `default-src`) and exit 1 without opening the l… (fonte: P2-192)
- When adding security headers to a served document, do gate strict-transport-security on the request actually arriving under TLS and apply the header map only to 200 document responses, keeping /healthz and 4xx/5xx bodies byte-identical — H… (fonte: P2-192)
- When writing a default CSP for a built bundle, do derive it from the real build artifact (assert dist/index.html has no inline scripts or event handlers) instead of assuming — the bundler decides what needs `'unsafe-inline'` (e.g. injected… (fonte: P2-192)
- When adding a rate limiter to a server that shares a listener with a load balancer health probe, do answer the probe (and drain 503) before consulting any budget and budget every other route before 404/405 checks, because a LB starved of i… (fonte: P2-195)
- When refilling token buckets from wall-clock timestamps, do treat a previous state timestamp in the future as zero elapsed time (`prev.lastMs < nowMs` guard) so a clock step-back or skewed host can never loosen the cap (fonte: P2-195)
- When keeping per-client state keyed by address, do derive the key from the same proxy-hops-aware identity helper the rest of the server uses, then tag it (never store raw IPs), and bound the map twice — inactivity pruning in an existing sw… (fonte: P2-195)
- When moving a query string between URL formats (e.g., a custom-scheme URI into a hash-route fragment), slice the raw string instead of re-parsing with URL/URLSearchParams — they re-encode "+" and "%2F" and silently corrupt base64url key ma… (fonte: P2-193)
- When transporting a credential in a URL, put it in the fragment (browsers never send it to any server) and call history.replaceState the moment the app consumes the link, so it leaves both the address bar and history (fonte: P2-193)
- When fusing a multi-step flow into a single artifact, mint the new artifact only when the problems array is empty and keep the old fallback path intact — a problem-bearing link must never be rendered as a QR (fonte: P2-193)
- When building desktop installers on a CI runner whose CPU may not match your users (e.g. arm64 macos-14), explicitly declare every architecture (`arch: [arm64, x64]`) on each electron-builder mac target — electron-builder silently produces… (fonte: P2-191)
- When splitting an auto-update feed (or any installed-base artifact) per architecture/variant, keep writing the legacy file name as a byte-identical alias of the default variant and resolve it in the client by `process.arch` (injected as a… (fonte: P2-191)
- When matching architecture/variant tokens inside artifact file names, strip the extension first and match on `[-_.]` boundaries (`(^|[-_.])arch([-_.]|$)`) so "x64" never matches inside "x86_64" and arch-less legacy names match nothing, and… (fonte: P2-191)
- When CI builds an artifact that will be published (container image, bundle), boot it on an ephemeral port and run behavioral probes against it before any push — a build that compiles proves nothing about booting (CMD pointing at a dist the… (fonte: P2-196)
- When writing a verdict/smoke function, return one problem per failed probe and never short-circuit so the operator sees every failure in one round, and treat an empty probe list as a failure — a smoke that ran nothing proves nothing (fail-… (fonte: P2-196)
- When a workflow step starts an ephemeral container/process, register cleanup so it is removed no matter how the step ends (`trap 'docker rm -f …' EXIT` with `shell: bash`), and keep the publish step strictly after the smoke step so a red s… (fonte: P2-196)
- When adding a helper module to apps/daemon/src that unit tests will import, keep it pure with no node:fs/node:http/ws imports (follow pairwindow.ts/relayurl.ts) because index.ts runs main() on import and the test chain would boot a real da… (fonte: P2-194)
- When persisting per-event metadata (stamps, counters) into a safety-critical state file like daemon.json, throttle writes through a pure decision function with the clock injected, returning write on missing/unreadable values and skip on fu… (fonte: P2-194)
- When extending a protected registry (allowlist) with a new field, make it optional/additive and mutate the parsed object in place through the existing atomic save so unknown fields survive the round trip, and add a source-level assertion r… (fonte: P2-194)
- When serving negotiated variants (gzip/identity) of the same asset, write `Vary: Accept-Encoding` on BOTH branches — the identity one too, and only skip Vary when the resource can never have a second variant (non-compressible extension, si… (fonte: P2-198)
- When memoizing derived bytes (gzipped bundles) in memory, key the cache by path + size + mtime so a redeploy at the same path never answers with the previous build's bytes, cap it in BOTH entries and total bytes with oldest-inserted evicti… (fonte: P2-198)
- When parsing Accept-Encoding, `q=0` means refused (identity), the `*` wildcard counts as accepting gzip, explicit tokens win over the wildcard, and a malformed q-value poisons the whole header to identity fail-closed; the extension allowli… (fonte: P2-198)
- When probing or logging a URL that embeds a secret (e.g. a pairing credential in the URI fragment), do probe only the ORIGIN and never the credentialed link nor attach credential headers, so diagnostics can't leak the secret (fonte: P2-197)
- When adding a field to an event/payload guarded by real-source assertions over its exact shape, do append it at a documented position after existing fields (additive, never rename/remove) and treat absence as "unknown" in consumers, so pri… (fonte: P2-197)
- When a host-side health check fails (DNS, TLS, relay down), do degrade to a calm advisory line with retry while keeping the QR/UI fully functional — local failure never proves the remote side will fail, so never block or hide the main flow (fonte: P2-197)
- When serving content-negotiated responses (gzip vs identity) that caches may store, make the ETag differ per encoding (fold the chosen encoding into the validator) so no shared cache ever serves compressed bytes to a client that asked for… (fonte: P2-200)
- When answering 304 Not Modified, emit only validators (etag, cache-control, vary) and never content-encoding, content-length or content-type, and skip reading/compressing the file in that branch so revalidation costs almost no CPU (fonte: P2-200)
- When adding validators to one route, derive them from the stat already in hand after the encoding decision (no new disk access) and keep sibling responses (healthz, 404, 405) byte-identical so load balancers and monitors see no behavior ch… (fonte: P2-200)
- When adding a new diagnosis to a UI tick that already polls a health endpoint, extend the parser of the SAME response (one request per tick serves every consumer — no new fetch, no new timeout) and extract each field tolerantly (typeof-gua… (fonte: P2-199)
- When generating user-facing copy from an upstream health snapshot, emit static messages keyed on the classification and never echo or interpolate the upstream's raw reason text or numbers — the reason may carry host/port and the copy lands… (fonte: P2-199)
- When collapsing a health snapshot into one verdict, order the checks so root-cause signals (relay address refused at boot) outrank in-flight transient symptoms (retry/backoff in progress), and let an orthogonal mode (local, where the relay… (fonte: P2-199)
- When a UI feature depends on an optional host capability (STT, TTS...), add a status route mirroring the existing capability-status pattern and probe it in the client BEFORE enabling the action — never let a lay user record/upload first to… (fonte: P2-201)
- When a detector returns null for more than one distinct cause (binary absent vs binary-present-but-model-missing), expose the raw detection facts (toolType + modelPresent) additively without changing the existing function's signature — col… (fonte: P2-201)
- When gating a control on a client probe, an unknown/null verdict must fail OPEN (control stays usable): the probe mounted on load races the first WS connect, so retry inside the hook (same tts probe lifecycle) and only disable the control… (fonte: P2-201)
- When validating a packaged artifact (DMG, installer, container), do boot/execute it once hermetically before upload instead of only inspecting its layout — runtime-only regressions (main throwing at boot, missing asar assets, entitlement k… (fonte: P2-204)
- When asserting "zero errors" from a console/log collector, do inject a known canary first and require the collector to have seen it, ranking canary-missing as a distinct failure above captured errors — an unverified collector voids the emp… (fonte: P2-204)
- When a validation script depends on an optional driver (Playwright, CLI, browser), do fail closed with exit 1 and a clear message when it cannot load — a silently skipped check is a vacuous pass that ships broken bundles (fonte: P2-204)
- When adding decision logic to a daemon whose `index.ts` runs `main()` on import, isolate it in a pure module with no `node:fs`/`node:child_process`/`node:http`/ws imports (all I/O stays in the caller), so unit tests exercise the real logic… (fonte: P2-207)
- When writing anything that plans file deletions, refuse silently — never plan — every entry whose path does not resolve strictly under the received root (`..` traversal, the root itself, empty path) and every segment failing the app's exis… (fonte: P2-207)
- When a safety guarantee lives in wiring, not in the pure function (e.g. a sweep must receive only the artifacts root, never `uploads/` or `clips/`), add a unit test that reads the real caller source and asserts the call site — and export t… (fonte: P2-207)
- When handling OS wake events (powerMonitor resume/unlock-screen fire in close bursts), put the debounce rule FIRST in the decision order and keep debounced/unknown events fully silent (no action, no log line), so a single wake never causes… (fonte: P2-209)
- When a decision in one module needs another module's internal bookkeeping (e.g., the sidecar's pending respawn timer), expose a read-only additive accessor (like nextRespawnInMs) and put the decision table itself in a pure module with no e… (fonte: P2-209)
- When adding event-driven reactions to the shell, apply the verdict only through paths that already exist (the pairing tick's health probe, the already-exported restart) — never a new periodic probe, extra per-tick request or changed timer… (fonte: P2-209)
- When adding per-platform packaging layout logic (macOS .app vs Windows win-unpacked/exe), extract path computation into a pure module with zero node:fs/os imports so tests run on any host OS, and have the caller keep a single disk-touching… (fonte: P2-208)
- When computing child paths from a user-supplied root, mirror the caller's separator style, refuse traversal stems ("", ".", "..") returning an empty list instead of escaping, and unit-test that every candidate equals or descends from the r… (fonte: P2-208)
- When a runtime smoke guards a release artifact, wire the workflow step between signature verification and the upload step with its own timeout, declare `shell: bash` explicitly on Windows runners (pwsh does not expand globs), fail closed w… (fonte: P2-208)
- When adding a status/probe route in apps/daemon/src/index.ts (which runs main() on import), derive the verdict from data fetches that already happen — hook the existing cache via an observation recorder (noteProviderCatalog) and have the r… (fonte: P2-210)
- When a verdict function is needed by both index.ts and scripts/unit.test.ts, put it in a pure module with zero node:fs/child_process/http/ws imports (pattern of voicecap.ts, pairwindow.ts, modelready.ts) — otherwise importing it in the uni… (fonte: P2-210)
- When interpreting a provider catalog, treat "listed" and "usable" as different things: only `connected` (credential-configured) providers count, model counts come from the `all` entry (connected-but-absent = 0 models → no-model), and a cat… (fonte: P2-210)
- When a task introduces a new artifact variant (e.g. per-architecture feed files like update-mac-arm64.json/update-mac-x64.json), do extend every existing validator/consumer of the old variants in the same change — otherwise the newest, mos… (fonte: P2-212)
- When a feed/manifest points to artifacts, do verify the target carries the expected architecture token (percent-decoded last URL segment) and that legacy aliases are content-identical to their source document — "asset is attached" (release… (fonte: P2-212)
- When extending a fail-closed checker with more inputs, do keep collecting ALL problems across feeds in one run (exit 1 listing everything) and only short-circuit on upstream shape errors (invalid tag) whose comparison would produce noise,… (fonte: P2-212)
- When adding a new diagnostic or verdict to the desktop shell, extract it as a pure classifier module (no electron, no node:fs, no fetch, no I/O) that maps an input snapshot to a state plus one static message, and reuse that message verbati… (fonte: P2-211)
- When a flow cannot actually complete in the current environment (e.g. the updater can never swap a bundle running from a mounted DMG or a quarantine-translocated copy), gate the offer behind a pure exported predicate that fails open on unk… (fonte: P2-211)
- When extending IPC payloads consumed by the renderer, add fields additively (new field placed after existing ones, never renamed or removed), compute the value exactly once at boot instead of adding periodic probes, and make the renderer t… (fonte: P2-211)
- When probing an external binary's state for a health/status surface, split it into a pure verdict module (no node:fs/child_process/http/ws imports, the voicecap.ts/modelready.ts pattern) plus a ONE-SHOT probe at boot on the already-resolve… (fonte: P2-213)
- When parsing version strings out of arbitrary tool output, extract the first dot-separated number run tolerating a `v` prefix, pre-release suffix and extra segments, compare numerically segment by segment (absent/empty/garbage → unknown),… (fonte: P2-213)
- When UI must show a warning derived from a mutable or uncertain probe, fail open on purpose: render the calm actionable line only for the definite bad state (nothing on ok/unknown), never disable controls or block sending, write that ratio… (fonte: P2-213)
- When adding a new verdict/diagnostic to a tick that already runs a probe (reach, health, pairing), widen the existing probe's return additively — carry the raw data you need (e.g. the `Date` response header, elapsed ms) off the SAME answer… (fonte: P2-214)
- When comparing the local clock against a server time reference (HTTP `Date` header), widen the tolerance by half the probe's elapsed ms because the header is written when the server handles the request and round-trip latency biases the off… (fonte: P2-214)
- When appending a field to a state payload whose structural assertions read real sources (e.g. `ocr:pairing-state`), place it additive-only at the END of the object with absent = unknown fallback in the renderer, and keep explanatory diagno… (fonte: P2-214)
- When adding a health/readiness verdict to the daemon, put the verdict function in a pure module with zero I/O imports (node:fs/child_process/http/ws) because index.ts runs main() on import and unit tests must never boot a daemon — keep all… (fonte: P2-215)
- When a resource can be judged by both an absolute threshold and a fraction/ratio threshold, always evaluate both rules and take the MORE SEVERE state — a huge nearly-full volume must never read ok just because the absolute number still pas… (fonte: P2-215)
- When surfacing machine health to a lay user, map every unreadable reading (null, zero total, negative, non-finite) to a neutral "unknown" that never accuses or blocks, show the message only for confirmed problem states, and never disable c… (fonte: P2-215)
- When generating user-facing guidance that names build artifacts (release assets, feeds, checksums), do extract every file name from the actual published list passed in at runtime and emit one problem per missing/invalid item without short-… (fonte: P2-216)
- When a CI step edits shared text like a release body, do make the edit idempotent via explicit begin/end markers (replace the existing block, insert above the body otherwise, preserve the rest byte-for-byte) and run it before the irreversi… (fonte: P2-216)
- When adding a GitHub Actions step that handles file names or arbitrary text verbatim, do declare `shell: bash` explicitly because the runner default shell mangles quoting and spaces in asset names (fonte: P2-216)
- When protecting a relay/proxy forwarding loop from memory abuse, do gate each send on the target's accumulated outgoing bytes (`bufferedAmount`) with a per-socket cap — a per-frame size cap alone never bounds memory when a peer stops readi… (fonte: P2-217)
- When routing payloads you cannot read or re-send (blind E2E router), do make the only two outcomes of a send "deliver" or "close the socket" — silently dropping a frame corrupts the stream while both ends still look healthy, and closing is… (fonte: P2-217)
- When a socket verdict depends on runtime accounting, do fail open on missing/negative/non-finite input (never close a healthy connection for absent telemetry) while keeping config-knob parsing fail-closed, reporting every violated rule at… (fonte: P2-217)
- Quando uma feature liga um padrão de sistema automaticamente no primeiro boot (item de login, permissão, serviço), persista um flag "dono já decidiu" nos DOIS sentidos (o toggle do usuário desligando também conta, via o MESMO caminho que o… (fonte: P2-218)
- Ao acrescentar campo num payload já assertado por testes de fonte real, posicione-o DEPOIS dos campos âncora existentes (append, nunca insert/rename) e trate campo ausente no renderer como estado desconhecido — a ordem dos campos é contrat… (fonte: P2-218)
- Quando a evidência visual de um estado é de difícil reprodução (acontece só no primeiro boot empacotado), exponha env var de teste documentada que força o estado no payload/diagnóstico SEM executar o efeito colateral real, viabilizando scr… (fonte: P2-218)
- When adding CI jobs for windows runners, declare `shell: bash` explicitly on every run step that touches paths, globs, or variables (pwsh default does not expand globs) and give each heavy step its own `timeout-minutes` so a hung runner ca… (fonte: P2-219)
- When a packaging surface is only exercised inside the release workflow, mirror it in CI as a cheap `--dir` target package plus the existing deterministic smoke — never installer, signing, or notarization — so packaging/layout regressions f… (fonte: P2-219)
- When adding costly platform-specific CI jobs, condition them on the existing scope job's path indicator so PRs not touching that area pay zero cost, and since the gate cannot run real CI, assert the workflow structure with unit tests that… (fonte: P2-219)
- When adding any modal/interactive confirmation to the desktop shell's quit or shutdown path, do evaluate the hermetic-harness rule FIRST (before packaged/dev checks) so `OCR_DESKTOP_SESSION` sessions always proceed silently, and add an env… (fonte: P2-221)
- When building a native quit-confirmation dialog, do fail open: set cancelId/Escape to "keep running in the tray", guard against re-entrancy (`quitDialogShown`) and route tray + menu quit items through one shared `explicitQuit()` that consu… (fonte: P2-221)
- When reading a user-preference file inside a quit/teardown handler, do make the read totally tolerant (missing, unreadable, corrupt JSON or wrong-typed field → default to silent quit, never throw) and persist the "don't ask again" choice a… (fonte: P2-221)
- When a validation surface (Docker image, installer packaging) is only exercised by the release workflow, mirror it as a PR-CI job gated by a pure path-scope classifier with its own output (sister of the existing one, never overloading it),… (fonte: P2-222)
- When building a container image in a PR job, tag it with an ephemeral local-only name (never a registry ref) and write the publishing prohibition — no registry login, no third-party login/push actions, no secrets, no artifacts — into the j… (fonte: P2-222)
- When smoke-booting a container in CI, bind it to an ephemeral loopback port, wait for the health endpoint with a bounded retry ceiling (log container output on timeout), run the same probe battery the release uses, and remove the container… (fonte: P2-222)
- When a critical surface (Docker image, installer, packaged service) is only exercised by a tag-triggered release workflow, mirror that gate as a PR-scoped CI job conditioned on path-scope detection, so something that compiles but does not… (fonte: P2-220)
- When building artifacts in pull-request CI, never log in to a registry, never use third-party login/push actions, never publish or attach artifacts and never read secrets, and write that prohibition as a comment on the job so building on a… (fonte: P2-220)
- When a CI step boots a container or any long-lived process, declare shell: bash explicitly, give the step its own timeout-minutes, bind an ephemeral loopback port and register trap-based cleanup (docker rm -f ... || true on EXIT) so every… (fonte: P2-220)
- When platform-specific logic (path separators, normalization, backslashes) is only exercised by CI on one OS, it breaks silently on user machines of the other OS — carve out a portable test subset (no Electron, no unix socket, no chmod, no… (fonte: P2-224)
- When a test subset exists to be run by a different runner, keep it as a named, header-commented list validated by a pure plan function (bad extension, duplicate, outside directory, empty list each yield one problem) plus a unit test assert… (fonte: P2-224)
- When a service's boot preflight only checks that the entry document (index.html) exists and is readable, extend it to read that document and verify every local asset it references (script src, stylesheet/modulepreload link href) against th… (fonte: P2-225)
- When verifying referenced assets at boot, resolve each one through the exact same path resolver and allowlist the request path uses (same rigidity for traversal, dotfiles, encoding, extension allowlist, root containment), so boot-time and… (fonte: P2-225)
- When scanning HTML with a plain-text scan instead of a parser, encode the scope explicitly: only quoted attribute values, require a whitespace before the attribute name so `data-src` is never read as `src`, ignore refs with an explicit sch… (fonte: P2-225)
- When adding any desktop feature that can open a native modal dialog, put the hermetic-harness/test-session check FIRST in the verdict function, before thresholds, budgets or packaging flags — tools/desktop.mjs, the test:desktop-flow batter… (fonte: P2-223)
- When watching transient Electron/OS events like unresponsive/responsive, write the handler against structural types with injected clocks/timers, make the start handler idempotent (the event re-emits while frozen), and cancel the episode's… (fonte: P2-223)
- When an automatic recovery path gives up (reload budget exhausted, retries exceeded), never leave it silent — route the give-up through the same verdict planner that handles the ongoing failure so the user gets a log line, a tray tip and,… (fonte: P2-223)
- When adding an overload cap that consults runtime accounting (live sockets, queue depth, active count), fail closed on configuration (invalid knob → refuse boot) but fail OPEN on the accounting itself (missing/negative/non-finite count → a… (fonte: P2-227)
- When refusing a connection at an admission gate, release every per-identity budget the attempt already took (e.g. the per-IP slot) and close only that one socket, or a flood of refusals leaks the per-IP budget and DoSes legitimate clients… (fonte: P2-227)
- When adding a new knob or guard to a codebase with established resource-cap modules, mirror the existing pattern exactly — pure decision module with zero I/O imports, `problems[]` fail-closed env parsing, additive metrics (new counter, nev… (fonte: P2-227)
- When adding decision/planning logic to apps/daemon, put it in a new pure module with zero node:fs/node:path/node:child_process/fetch imports — index.ts runs main() on import, so any unit test importing impure code boots the whole daemon; k… (fonte: P2-228)
- When adding a background sweep to the daemon, ride the existing janitor's hook and RETENTION_INTERVAL_MS timer (first pass via `setTimeout(..., 0).unref()` so boot is never delayed) instead of creating a new periodic timer, and treat per-c… (fonte: P2-228)
- When writing retention/cleanup planners or operator env knobs, order protections so grace-period files and the newest N always survive before age and byte-cap rules run (delete oldest-first with a deterministic mtime tie-break), and parse… (fonte: P2-228)
- When a capacity cap protects a finite resource (live sockets, per-IP slots, memory), bound not only how many holders are admitted but how long a holder may keep its slot without using it — transport-level liveness proves nothing about appl… (fonte: P2-230)
- When a reaper decides from bookkeeping fields (openedAt, lastSeen, joinedRoom), stamp the baseline only after every admission check passed and make the verdict skip any peer with an undefined field — missing data must protect the peer, nev… (fonte: P2-230)
- When adding a new periodic decision, hook it into the existing sweep instead of creating a new timer, keep the verdict in a pure import-free module with a fail-closed parser that reports ALL violated rules at once (exit 1 at boot instead o… (fonte: P2-230)
- When adding a feature that mutates system-wide state (global shortcuts, listeners, hooks), do consult the test-harness session flag FIRST in the decision order — before env vars, packaging or validation — because test runs on the operator'… (fonte: P2-229)
- When validating user-supplied config from environment variables, do fail closed: reject invalid values (no modifier, wrong type, unknown tokens, over-ceiling) by registering NOTHING with a logged reason, never silently falling back to the… (fonte: P2-229)
- When a registration can fail because another app owns the resource, do fail open with one log line and no dialog, keep the existing fallback path (tray) working, show the plan's reason in informational UI instead of a lying value, and unre… (fonte: P2-229)
- When a daemon feature depends on an external binary, announce availability BEFORE the request: probe candidates once at the same readiness hook existing capabilities use (no new timers, no per-request probing; probe failure = one log line,… (fonte: P2-231)
- When a tools/*.mjs script hardcodes install paths of an external binary, move the per-platform path lists and preference order into a shared pure module — a macOS-only list silently leaves Windows installs with no route even when the binar… (fonte: P2-231)
- When a verdict picks between capability tiers (full-fidelity converter / native fallback / none), implement it as a pure function receiving platform + already-probed candidate results and table-test every rule (full-fidelity always wins, p… (fonte: P2-231)
- When adding any behavior that can fetch internet bytes (update download, feed fetch, telemetry), do make the harness/test-session rule the FIRST rule of the decision function, consulted before platform, packaging or version checks, so herm… (fonte: P2-233)
- When a side effect must happen only on explicit user action, do wire the sink/callback ONLY at that call site and pass `undefined` in every other path (boot, timers, scheduled rechecks), so background triggering is structurally impossible… (fonte: P2-233)
- When verifying a downloaded artifact against a published digest, do measure-and-compare fail-closed, delete the diverging file, and keep the pre-existing manual flow intact as fallback for network failure or missing digest (fonte: P2-233)
- Quando um payload de saúde/readiness do daemon chega à UI (parcial, malformado ou de daemon antigo), parseie num módulo puro e tolerante: campo ausente ou de tipo errado é ignorado sem virar linha inventada, payload não-objeto devolve list… (fonte: P2-232)
- Quando renderizar lista derivada de dados polados (health polls), ordene por pior severidade primeiro com desempate por ordem fixa e documentada de chaves (ex.: índice de build estável), senão a lista "dança" entre dois polls idênticos (fonte: P2-232)
- Quando exibir frases de diagnóstico da máquina, renderize a frase do daemon verbatim (nunca reescreva ou invente) e mantenha-a livre de caminhos absolutos, esquema de URL e segredos; já todo texto autoral da UI (rótulos, cabeçalho, estado… (fonte: P2-232)
- When boot code reads a file whose loss destroys user state (identity keys, pairings), do distinguish "read failed" from "file missing" before any recovery decision: a transient FS error (EACCES, EBUSY) treated as missing would silently min… (fonte: P2-234)
- When a state file is too corrupt to parse, do move it aside under a sortable timestamped quarantine name (mode 0600, never delete) and exit with a documented code, so the next run takes the existing first-run path instead of the process cr… (fonte: P2-234)
- When writing log or audit messages about files that contain secrets, do use static sentences only — never interpolate file content, absolute paths or URL schemes — and keep the decide-vs-act split in a pure module (no node:fs imports) so t… (fonte: P2-234)
- When a state marker is only cleared by an external event that may never arrive (session event, callback, webhook), add a lease/timeout that reclaims it inside an already-existing periodic sweep instead of a new timer — and stamp the start… (fonte: P2-236)
- When introducing a lease/expiry retroactively over pre-existing un-timestamped state, treat the first observation as the start (stamp now, never kill on that same pass) — otherwise the upgrade itself murders legitimate in-flight work from… (fonte: P2-236)
- When adding an env-configured knob, follow the fail-closed contract of sibling knobs: missing/blank keeps the default, the documented off keyword disables it, and every invalid value (non-numeric, 0, negative, fractional, above ceiling) ac… (fonte: P2-236)
- When maintaining a hand-written list that gates behavior (like a portable test suite list), pair it with a fail-closed coverage check that reconciles the declared list against a real directory read at gate time — every new file must land i… (fonte: P2-237)
- When writing validation logic that depends on real-world state, keep the module pure with data injected by the caller (no fs/child_process/network imports), then wire the real directory read in the command and add a separate real-repo asse… (fonte: P2-237)
- When a validator reports problems, emit one problem per cause without short-circuit, in stable order, each naming the affected file with a one-sentence fix instruction, so a single gate run reveals all causes at once instead of forcing one… (fonte: P2-237)
- When adding any native UI affordance (context menu, dialog, popup) to the desktop shell, evaluate the harness-session rule FIRST and return an empty/no-op result before any packaged/selection/link consideration, and pin that rule order in… (fonte: P2-235)
- When exposing an already-gated capability (like opening external links) through a new UI surface, route the decision through the existing verdict module (extlink.ts `externalOpenDecision`, P2-178) instead of re-implementing validation, and… (fonte: P2-235)
- When wiring Electron main-process behavior that unit tests cannot execute, keep the decision logic in a pure spec-builder (no electron imports, no I/O) emitting plain-data items, and add repo-real assertions reading the wiring file to pin… (fonte: P2-235)
- When decision logic must live in a classic service worker that tests cannot import, split it into a pure classic script (no I/O, no listeners, published on `self`) pulled via `importScripts` as the file's FIRST instruction, so unit tests l… (fonte: P2-239)
- When precaching a PWA shell for offline opens, serve cache-first only assets whose URL is hash-versioned (changes every publication) and keep the navigation document network-first — a cached root doc still references per-build asset URLs t… (fonte: P2-239)
- When a publication replaces cached assets, bump the versioned cache name so the new build never inherits old entries, sweep stale hash-named leftovers at activate (never the root doc, never current targets), and treat each failed precache… (fonte: P2-239)
- When adding any persisted preference that changes the desktop shell's visual framing (zoom, font size, pane layout), make the harness-session rule the FIRST decision in the startup path — with OCR_DESKTOP_SESSION apply the default and writ… (fonte: P2-238)
- When extending an existing JSON state file with a new field, do it additively: optional typed field, tolerant pure sanitizer on read (non-numeric/non-finite/out-of-range → default or clamped edge, never throws), and legacy files without th… (fonte: P2-238)
- When swapping Electron native menu roles for shell-owned items to gain state control (tracking, clamping, persistence), put the policy in a pure module (no electron, no I/O) returning a verdict with a limit flag, apply it on every `did-fin… (fonte: P2-238)
- When slicing a markdown file into sections by heading, end each section at the NEXT heading of the same level, never at end-of-file — cutting from the first heading match to EOF makes every unmarked line in all later sections leak into the… (fonte: P2-240)
- When classifying backlog lines into lists (ready/blocked), apply an ordered rule set driven by section context — only unmarked lines, every repeated occurrence of the heading counts, done/unknown sections excluded and counted separately as… (fonte: P2-240)
- When fixing a parsing bug in an HTTP route, extract the logic into a pure I/O-free module (no node:fs, node:child_process, fetch) tested with a full table-driven battery plus assertions against the real repo files, keeping the response bod… (fonte: P2-240)
- When adding any new Electron-native surface (downloads, dialogs, notifications), route it through a pure decision module (no electron imports, no I/O) whose FIRST rule is the hermetic harness-session check (`OCR_DESKTOP_SESSION` → refuse,… (fonte: P2-241)
- When trusting announced download metadata, sanitize fail-closed and never "fix up": reject path separators (`/` `\`), `..`, `:`, control chars, Windows reserved names (`CON.txt` counts), empty/space-only names and sizes above a documented… (fonte: P2-241)
- When a download completes, never execute or open the file (untrusted bytes until a human decides): only reveal it via `shell.showItemInFolder`, log one path-free/scheme-free static phrase, and register exactly ONE `will-download` listener… (fonte: P2-241)
- When adding a cumulative/quota control next to instantaneous ones (per-frame cap, rate bucket, backpressure), consult its verdict at the exact same forwarding point as the sibling verdicts and zero the accumulated state on window expiry BE… (fonte: P2-243)
- When enforcement can terminate a session based on a measured quantity, fail OPEN on dubious measurements (non-numeric, non-finite or negative frame size → follow without accumulating, because closing a room on an untrusted count is worse t… (fonte: P2-243)
- When adding per-key accumulated state to a long-lived server, delete it in the same code path that removes the key's entity so the map never grows forever, store any once-per-window flag inside the state itself so the warn log fires at mos… (fonte: P2-243)
- When CI only inspects a packaged artifact while the real execution check lives in the release workflow, add the actual boot/run step to the CI packaging jobs (after packaging + inspection smoke) so runtime-only regressions (main throwing a… (fonte: P2-242)
- When adding a runtime check to CI, reuse the exact script and hermetic contract the release workflow already uses (temp userData, run-own session, no sidecar, Playwright missing fails closed) instead of writing a thinner CI-only variant —… (fonte: P2-242)
- When an invariant spans two workflows or files (CI packages → must boot; release boots), encode it as a pure dependency-free module (narrow parser that never throws on malformed input + rule checker returning one problem per cause, no shor… (fonte: P2-242)
- When generating package-manager manifests (winget/Homebrew) in CI, keep the builder AND its verifier as one pure module (no fs/child_process/fetch) whose verifier returns one problem per cause in a fixed order without short-circuiting — a… (fonte: P2-245)
- When adding a step to a release job, insert it after the checksum-manifest upload and BEFORE the step that publishes the draft, give it explicit `shell: bash` plus its own `timeout-minutes`, and fail the job on any verification problem — v… (fonte: P2-245)
- When a workflow step must call a TS module, write a throwaway `.mts` driver to disk (run via the pinned `npx tsx@<version>`, `rm` it afterwards) and derive all facts from already-published artifacts (`gh release view --json assets`, sha li… (fonte: P2-245)
- When an Electron setting only takes effect before the app is ready (disableHardwareAcceleration and friends), do resolve the decision from persisted state before `app.whenReady()` and register the single crash listener at boot beside it, n… (fonte: P2-244)
- When persisted state gates a recovery action, do sanitize every read (missing, corrupt, wrong type, negative, non-finite, future windowStart) into the neutral default and zero expired windows BEFORE any threshold comparison, so a corrupted… (fonte: P2-244)
- When a test-harness session shares the decision path with production, do put the harness-session rule as the FIRST branch of the pure decision function — before saved-state, packaged or platform checks — and gate disk persistence behind th… (fonte: P2-244)
- When a service-worker lifecycle decision can break a tab that is already open (skipWaiting, activate-time cache sweeps), do gate it on controlled-client presence and treat any doubtful count (non-numeric, NaN, ±Infinity, negative) as "clie… (fonte: P2-246)
- When a guard rule (live client) must dominate cleanup rules, do declare the rule order as an explicit contract and add a test where both conditions co-occur (client alive AND leftovers exist → empty sweep), because testing each rule in iso… (fonte: P2-246)
- When adding decisions to a classic-script service worker, do put them as pure functions in sw-policy.js consumed by sw.js (orchestration only, decisions pinned by source assertions) and rely on the browser's own activation of a waiting wor… (fonte: P2-246)
- When adding lifecycle listeners to a shell (Electron main or similar), pair every success event you observe with its failure counterpart (e.g. `did-fail-load` beside `did-finish-load`) — crash/hang/GPU guards do not intercept a load that n… (fonte: P2-247)
- When a recovery policy has several ignore conditions, keep it in a pure I/O-free module with an explicit rule-order contract and treat Chromium's ERR_ABORTED (-3) as a deliberate cancellation that ignores without accumulating — it fires on… (fonte: P2-247)
- When logging or displaying anything derived from an event's address/URL, pass it through a tolerant sanitizer that never throws and emits static strings where only a validated scheme survives — full URLs, paths and file names must never re… (fonte: P2-247)
- When adding a retention/planning module under apps/daemon/src, keep it pure (no node:fs, node:path, node:child_process or fetch imports) with all I/O in the index.ts caller, because index.ts runs main() on import and any unit test importin… (fonte: P2-248)
- When bounding a directory of generated artifacts, decide deletion per GROUP (each immediate child of the root, totaled bytes + most recent mtime inside it), not per file, and order rules as grace → N most-recent protected → oldest-first wh… (fonte: P2-248)
- When adding a periodic janitor to the daemon, reuse the existing shared retention interval instead of creating a new timer, keep each sweep's config parse fail-closed (collect ALL env problems at once, invalid config disables the sweep and… (fonte: P2-248)
- When a daemon entry file runs `main()` on import, do put new decision logic (refresh plans, knob parsing) in a pure module free of node:fs/node:path/node:child_process/fetch and unit-test that module instead of trying to boot the daemon (fonte: P2-250)
- When a capability verdict can go stale after boot (tool installed, binary updated), do re-probe lazily at the point of use — right before answering the refusal — through a pure redo/reuse planner where a ready verdict is never re-probed an… (fonte: P2-250)
- When adding env knobs, do parse them fail-closed collecting ALL problems at once (reject non-numeric, zero, negative, fractional and over-ceiling values instead of silently defaulting) so a typo fails the boot loudly rather than misconfigu… (fonte: P2-250)
- When adding an OS-level integration that outlives the app process (e.g. "start at login" via app.setLoginItemSettings, which on Windows writes a HKCU Run-key entry pointing at the installed executable), add the matching uninstaller cleanup… (fonte: P2-249)
- When planning deletion of files/registry values (uninstall, retention, janitors), model it as a pure allowlist classifier: remove only names in a documented vocabulary of the app's own artifacts, PRESERVE unknown names instead of guessing… (fonte: P2-249)
- When the deliverable is a build/packaging artifact that CI never executes (NSIS macro, electron-builder.yml options), assert on the real file contents in unit tests — macro name present, only registry values the app owns deleted, no Docume… (fonte: P2-249)
- When an existing smoke deliberately disables a component (boot smoke points the daemon entry nowhere) or only checks file presence, that component ships having never executed — add a dedicated smoke that spawns it exactly as production doe… (fonte: P2-251)
- When CI must verify a process boots and serves, split the verdict into a pure module with zero I/O imports (no fs/child_process/fetch) that takes injected facts and returns every failing rule's problem at once (no short-circuit) — unit tes… (fonte: P2-251)
- When spawning a child whose stderr gets echoed into job logs, allowlist env keys instead of passing process.env wholesale, discard stdout that carries credentials (e.g. pairing URI) without reading it, and before spending an auth token on… (fonte: P2-251)
- When a smoke that executes a packaged artifact runs only in release.yml, mirror the exact same step (same script, same invocation, same resolution of the bundle) into every CI job that builds that artifact, so packaging regressions surface… (fonte: P2-253)
- When you tighten what a parity/guard checker requires, update its header comment and ordered rule list in the same change and delete any sentence stating the old scope, so the documented contract never contradicts the enforced one (fonte: P2-253)
- When adding a workflow step that runs a smoke script, keep the guard checklist honest: place it after the packaging/boot step that produces the bundle, declare `shell: bash` explicitly, and give it its own `timeout-minutes` (fonte: P2-253)
