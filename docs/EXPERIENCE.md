# Experience memory (IER)

Lições destiladas pelo pipeline (role SCRIBE) após cada merge bem-sucedido.
Cada lição é uma linha `- When <situação>, do <ação> (fonte: <ID>)`. Os prompts
de planner, builder e strategist recebem o top-5 de lições relevantes (keyword-match,
mais recentes primeiro); o red team noturno deduplica e poda acima de
60 lições.

## Lessons
- When a second surface shows the same live connection verdict as an existing screen, reuse that screen's i18n keys, the verdict's own static message and its warn class, and expose a copy-independent `data-*` state attribute for the harness… (fonte: P3-453)
- When a status line can be unknown or absent (e.g. daemon predates the field), keep unknown/missing neutral with no warn styling — absence of data is never an accusation; only known bad states paint warnings (fonte: P3-453)
- When a periodic pass emits an alert tied to a metric, do dedupe by comparing the value between passes (equal count stays silent) and reset the memo when it returns to baseline so cleanup followed by regrowth alerts again — in-memory memo i… (fonte: P2-341)
- When a test reads external dependency sources as compile-time truth, do also pin the dependency's version (its package.json) so a lockfile drift fails loudly and forces human re-verification instead of silently validating stale behavior (fonte: P3-456)
- When configuring destructive cleanup in NSIS installers, do guard the wipe on the upgrade signal the template exposes (`--updated` / `${isUpdated}`) because installing over an old version runs the previous uninstaller silently — unguarded… (fonte: P3-456)
- When adding a user-triggered action that mirrors an existing automatic behavior (a click duplicating the wake path), reuse the exact same internal helper and keep the legacy call path byte-for-byte by only adding a return value to it, lett… (fonte: P2-340)
- When exposing a main-process network answer to the renderer over IPC, funnel it through a pure sanitizer that maps only the documented enum pairs to a closed set and fails closed to a single "unavailable" value on non-200 status, null/malf… (fonte: P2-340)
- When a UI control awaits an async backend call, guarantee a terminal outcome: one static i18n phrase per closed-set verdict (both locales, path/URL/port-free) with a rejected promise or unknown value degrading to the failure phrase, so the… (fonte: P2-340)
- When gating a fleet-critical path like update offers by percentage, make every missing, non-numeric, fractional or out-of-range input fail open to today's behavior (holding a machine by doubt freezes the fleet) and keep exactly one explici… (fonte: P2-342)
- When assigning installs to rollout cohorts, derive the bucket from hash(installationId + offeredVersion) % 100 — a random UUID persisted once in userData with mode 0600, never hardware/keys/pairing-derived, regenerated silently when illegi… (fonte: P2-342)
- When wiring a pure verdict module into main.ts, resolve its side-effecting inputs lazily (cache the id read so only machines with a pending update ever touch the file) and short-circuit hermetic harness sessions before any I/O (the pure ru… (fonte: P2-342)
- When two surfaces can be mounted at the same time and expose the same kind of action, give each its own copy-independent data attribute (overlay data-pair-relay-redial vs Settings data-relay-redial) so e2e selectors never collide across pa… (fonte: P2-343)
- When several components must render identical closed-set verdict phrases, export the phrase map from one module and import it in the other instead of duplicating keys — a single source prevents phrase drift. (fonte: P2-343)
- When an async bridge-backed action can reject or return an unexpected value, degrade it to a terminal static failure phrase (catch to unavailable, map fallback to the failure key) and render the action only when the caller hands the handle… (fonte: P2-343)
- When a release-side writer feeds a value that the installed client parses fail-open (garbage or missing percentage offers the update to everyone), make the writer fail closed instead — validate through one shared exported validator and exi… (fonte: P3-458)
- When an optional env/CLI input must leave artifacts byte-for-byte identical to before, treat absent/blank as a distinct documented no-op (not an error, not zero) and pin it with a byte-identical-fixture test, so the no-input path can never… (fonte: P3-458)
- When rewriting one field inside a structured feed via text surgery (JSON/YAML), do re-parse the result and diff it against the original modulo the target field (stable stringify with the field deleted) so any collateral byte change — diges… (fonte: P3-460)
- When mutating published remote artifacts in stages (download → transform → re-upload), do validate inputs and verify every precondition (complete asset set, alias byte-identity) BEFORE the first write, collect all problems at once, and on… (fonte: P3-460)
- When a release carries aliased duplicate assets that must stay byte-identical (update-mac.json ↔ update-mac-arm64.json), do verify the identity before rewriting and apply the same rewrite to both so it survives, refusing a divergent pair r… (fonte: P3-460)
- When duplicating a constant or grammar across apps to avoid cross-app import coupling, add a unit test that reads the real source of the canonical module (regex-extract the constant) and asserts byte parity, so the copy can never silently… (fonte: P2-344)
- When adding a new fault-detection state to an existing verdict classifier, make every failure mode (missing, null, out-of-grammar input, failed or stranger extra probe, fewer than two valid values) preserve today's verdict unchanged — the… (fonte: P2-344)
- When extending a shared verdict union consumed by a UI view, put the new state's static phrase into the existing sibling table so current rendering paths (phrase pick + default danger styling) cover it with no new branch, keeping every oth… (fonte: P2-344)
- When adding series to an existing scraped metrics endpoint, append them after their sibling series in BOTH formats (Prometheus line order and JSON key order), publish zero as zero — never omitted, so an operator alert distinguishes a healt… (fonte: P3-461)
- When a failure mode keeps each replica's aggregate metric looking healthy (two replicas behind one address each show normal active rooms), publish the SHAPE of the units, not just the total — occupancy buckets computed per scrape from the… (fonte: P3-461)
- When counting inputs into published buckets, skip unclassifiable entries entirely (missing, non-number, non-integer, zero, negative, NaN, Infinity) instead of coercing — an invented bucket is a dishonest metric — keep the counter a zero-im… (fonte: P3-461)
- When a report needs state another subsystem already computes (verdicts, ticks, getters), mirror it into module-level variables at the existing computation points and reuse the existing getter — never add a timer/IPC/route/request — and sto… (fonte: P2-345)
- When adding lines to a bundle that carries a privacy contract (no installation ids, buckets, URLs, hosts, ports), pass only closed-set states/decisions plus a stable reason id and enforce the contract with a test asserting no new line cont… (fonte: P2-345)
- When a UI must surface a machine-readiness verdict, ride the request the view already performs (mirror the /api/health additive fields onto the already-polled /__ocr/settings channel and slice only that piece into state) instead of adding… (fonte: P3-462)
- When an additive field can arrive in two wire shapes (flat sibling like diskState vs. object like disk:{state,message}), prefer the flat sibling when present, but a present-but-out-of-set flat value must never fall through to the object va… (fonte: P3-462)
- Render a server-supplied phrase verbatim only while it is a short single-line string (length ceiling, trimmed, no control characters); absent, non-textual, overlong or multi-line phrases fall back to static per-locale copy so a hostile or… (fonte: P3-462)
- When classifying OS-level failures (errno codes, probe results) into user-facing copy, keep the raw I/O in the caller and inject only the result into a pure, import-free classifier module that maps to a closed verdict set and degrades ever… (fonte: P2-346)
- When two apps must share a closed value set but cannot import each other (desktop shell + web), duplicate the set deliberately on both sides and pin the parity with a test that reads both real source files and fails the moment either drifts (fonte: P2-346)
- When probing filesystem writability, run the probe exactly once at boot before the dependent subsystem starts (an unwritable folder does not heal without a restart) and name the probe file with randomUUID plus exclusive-create flag "wx" so… (fonte: P2-346)
- When a new daemon capability needs a closed-set verdict (free/total bytes → state), delegate to the one existing classifier (diskguard.ts's diskVerdict) instead of duplicating its thresholds; the new module only decides WHEN to re-read and… (fonte: P2-347)
- When a write depends on a readiness verdict that can go stale between boot and use (free space only ever shrinks), re-read it lazily at the point of use at most once per the shared OCR_READINESS_MIN_MS/OCR_READINESS_DISABLE knobs with no n… (fonte: P2-347)
- When resource exhaustion would otherwise fail a write mid-file (ENOSPC reaching the phone as a generic error), refuse the operation up front (HTTP 507 carrying the verdict's own phrase, the same one /api/health serves) BEFORE any byte is w… (fonte: P2-347)
- When deferring an Electron window's first show (quiet login boot), never call maximize() in the ready-to-show handler of a hidden window — maximize() also SHOWS it; carry the pending owner choice in a flag applied by the first user-driven… (fonte: P2-348)
- When distinguishing automatic login boots from user launches, read the OS report on macOS (wasOpenedAtLogin) and register a dedicated argv argument next to openAtLogin on Windows at the single setLoginItemSettings call point, detecting it… (fonte: P2-348)
- When adding a boot-time decision, keep it in a zero-import pure module (no electron, node:fs, timers, network) returning a closed-set verdict with short static path-free reasons, computed exactly once in onReady before the first window cre… (fonte: P2-348)
- When adding a second trigger (probe tick, CLI route, cron) for a side-effectful action already gated elsewhere, extract the decision+action into one shared helper (anticipateRelayRedial) and route every caller through it — then pin in the… (fonte: P2-349)
- When a threshold derives from a timer's period (wake = gap above 3 intervals of the 60s probe), define the period once as a named constant feeding BOTH the setInterval and the threshold, and pin with a source test that no literal duplicate… (fonte: P2-349)
- When inferring an event (machine slept/woke) from indirect evidence (tick gap), fail closed on ambiguous inputs — clock moving backwards (NTP correction, VM resume), NaN/Infinity inputs, and a non-positive interval must all map to the no-o… (fonte: P2-349)
- When intercepting an Electron/Chromium callback-style event (e.g. app.on "login"), call the callback in EVERY branch immediately after preventDefault — an unanswered callback hangs the request far past the cancellation you intended, so rep… (fonte: P2-350)
- When logging observations of high-frequency events (auth challenges, retries, ticks), write the log line only on a STATE transition — compare against the previous verdict and emit one line per change, never per event, or a burst of 407s fl… (fonte: P2-350)
- When a pure module classifies an external payload into a closed verdict set, degrade every unreadable input (non-object, non-boolean flag, non-string field) to "unknown" fail-closed and never echo raw input values (host, scheme, realm) int… (fonte: P2-350)
- When asking the browser to persist data (navigator.storage.persist in a PWA), call it exactly once and only right after the user action that created the data (a successful pairing), with the once-guard reading the live persisted flag itsel… (fonte: P3-463)
- When wrapping a capability API that may be absent, reject or answer oddly, funnel every failure mode (null, non-function, sync throw, rejected promise, non-boolean answer) into a classifier that never throws and resolves to the closed set'… (fonte: P3-463)
- When data lives in evictable browser storage, stamp a minimal one-word marker (dotted `ocr.` namespace, no key material) on every write of that data and clear it on deliberate wipes, so a later boot can distinguish "browser evicted your da… (fonte: P3-463)
- When redacting crash or log lines from a process that handles tenant data, apply the same redaction (paths reduced to basenames, room-id/IP redaction, truncation ceiling) to the error MESSAGE and not only the stack — Node fs errors embed a… (fonte: P2-351)
- When registering uncaughtException/unhandledRejection listeners in a serving process, emit exactly one structured line and metric, guard the graceful-drain call with a handled boolean so later fatal events only log (calling the drain a sec… (fonte: P2-351)
- When a log formatter runs inside a dying process, make it total: guard every field read (name/message/stack can have throwing getters or a crafted Symbol.toStringTag), reduce non-Error values to a closed kind vocabulary, wrap the whole bod… (fonte: P2-351)
- When drawing focus rings or status dots in apps/web CSS, never rely solely on box-shadow or background-color — forced-colors (Windows high contrast) discards the former and repaints the latter with the system palette, so pair every such af… (fonte: P3-464)
- When auditing forced-colors coverage, also derive the second lost-cue class: `:focus` rules that pin `outline: none` and escalate only a border-color, because forced colors flatten focused and resting borders to the same CanvasText — but k… (fonte: P3-464)
- When acceptance demands structural CSS guarantees, prove them by parsing the real stylesheet — strip comments, walk rules with a brace-aware parser that skips @keyframes and recurses into @media/@supports — and assert fail-closed invariant… (fonte: P3-464)
- When a renderer action can arrive via OS relaunch (Jump List task, second-instance argv) or a cold start, do queue it as a pending flag and flush it on the window's `did-finish-load`, because a `webContents.send` broadcast fired before the… (fonte: P2-353)
- When adding a native shell surface (Dock menu, Jump List, tray), do keep its spec in a pure module with no electron/node:fs/timer imports and make main.ts the single translation point that attaches Electron-only fields and click wiring, so… (fonte: P2-353)
- When adding a timing ratchet to a smoke that already gates on a functional verdict, keep the measurement fail-open with a closed-set verdict (ok/over-budget/unknown, never a boolean) and let only the existing verdict set the exit code — ti… (fonte: P2-354)
- When a pure verdict module feeds a printed line, keep the line's frame constant and render a missing/invalid number as the bare token `unknown` (a unit suffix like `ms` belongs only to a real number) so assertions reading the output never… (fonte: P2-354)
- When an Electron app whose UI lives in the renderer needs the OS-visible window title (Alt+Tab, taskbar, macOS Window menu, screen readers) to reflect app state, write `document.title` from the renderer (Electron forwards it to the Browser… (fonte: P3-465)
- When deriving user-visible strings from user-controlled data (conversation titles auto-generated from the first message), sanitize in a pure no-DOM module — strip control characters, collapse whitespace, truncate with ellipsis, fall back o… (fonte: P3-465)
