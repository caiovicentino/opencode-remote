{
  "name": "@ocr/desktop",
  "version": "0.2.0",
  "private": true,
  "description": "OpenCode Remote desktop shell (Electron) — loads the @ocr/web UI",
  "type": "commonjs",
  "main": "dist-electron/main.js",
  "scripts": {
    "build": "tsc && npm run build:daemon",
    "build:daemon": "node scripts/bundle-daemon.mjs",
    "typecheck": "tsc --noEmit",
    "start": "electron .",
    "dist": "npm run build --prefix ../web && npm run build && electron-builder"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/qrcode": "^1.5.5",
    "electron": "44.1.0",
    "electron-builder": "^26.0.12",
    "esbuild": "^0.25.12",
    "typescript": "^5.6.0"
  },
  "license": "AGPL-3.0-only"
}
